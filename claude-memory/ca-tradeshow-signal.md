---
name: ca-tradeshow-signal
description: "/ca-tradeshows engine built 2026-07-07 — discover companies exhibiting at upcoming trade shows (bulk booth-swag buyers on a deadline), score by buy-now window, feed /ca-outbound with a show-themed mockup opener. Ryan's chosen highest-value motion."
metadata: 
  node_type: memory
  type: project
  originSessionId: 4f2057a5-da32-467d-af31-39ca7205edd4
---

Built 2026-07-07 as `/ca-tradeshows` (`scripts/ca_tradeshow_scout.py` + `config/ca_tradeshows.yaml`). Ryan's strategic pivot: **CA makes money on BIG BULK orders → target trade-show exhibitors** (any company with a booth = guaranteed bulk swag buyer on a hard deadline; mockup something fun themed to THAT show). Not niche-locked — broadest, highest-order-value motion. Full playbook: `Creative-Alternatives-AIOS/plans/tradeshow-signal-playbook-2026-07-07.md`.

**Signal-FIRST discovery (reverse of /ca-signals):** find companies BECAUSE they have a booth.
- Pick shows: `config/ca_tradeshows.yaml` (name/date/city/exhibitor_url/platform/mockup_theme). Best = crowded mid-size-heavy: RSA(649 exh)/Black Hat(400+) cyber, HIMSS health, NRF retail, Money20/20 fintech. Skip CES (giant-dominated).
- Scrape exhibitors: Firecrawl (owned) the public directory. **Map Your Show powers most big US shows** — one pattern `{event}.mapyourshow.com/8_0/explore/exhibitor-gallery.cfm` (CES/NRF/HIMSS/InfoComm/NAB/NADA/MODEX). **VERIFIED LIVE: 85 exhibitors off one InfoComm gallery via Firecrawl** (regex markdown links `exhibitor-details.cfm?exhid=`). RSA=RainFocus, Swapcard=Informa shows. Cvent/Brella gated=skip. Robust fallback = Apify `skython/exhibitor-list-scraper` (~$5/1k, 30 platforms, owned Apify).
- Score by buy-now window: **outreach sweet spot 8-12 wks pre-show** (order lands 4-10 wks out). peak 8-12w→priority, 4-8w→priority, <4w→rush ("we can still hit your deadline"), >16w→nurture, past→drop. Repeat exhibitors (65-80% rebook) = proven buyers.
- Output: `leads.json` (companies + top_signal_detail = themed mockup pitch) → feeds /ca-outbound. Opener: "Saw {company} is exhibiting at {show} — here's a mockup of {themed item} for the booth."

**Economics (verified):** mid-size booth swag order = $1,500-$10,000+ (bulk). Buyer = **Event Marketing Manager / Field Marketing Manager** (fallback Trade Show Coordinator / Brand / Marketing Mgr). Themed mockups: cyber→data blocker/RFID sleeve/engraved webcam cover; general→recycled power bank/premium tumbler/branded socks.

**Downstream (companies → contacts, reuses existing):** scout gives COMPANIES (no contact). Chain: ca_lead_waterfall resolve-domains (Places) → AI Ark people_search (company+event-marketing title, cheap list op) OR harvest → email → /ca-outbound themed opener.

**Pagination + enrich chain BUILT 2026-07-07 (both):** (1) extraction now merges gallery + flat exhibitor-list.cfm → **221 exhibitors on InfoComm** (2.6x the 85/page), plus `--via apify` (all-in-one actor `skython~exhibitor-list-scraper`, config `apify:` block, ~$5/1k, owned) for 100% coverage of gated/paginated platforms — Apify path built but NOT live-tested (verify actor input schema on Apify). (2) `run --show <key> --enrich` chains extract → resolve domain (Places, reuses ca_lead_waterfall._places_lookup) → published email (Firecrawl, reuses _fc_scrape/_emails_in) → score, emitting /ca-outbound-ready leads.json (company+domain+email+themed opener). TESTED LIVE: 5/5 domains resolved (aja.com etc.); 0 published person-emails on those AV vendors (contact-form companies — honest: harvest email coverage is thin for non-law; domain resolution is the credit-saver, AI Ark people_search finds the event-marketing person at the known domain). **Last-mile contact+email WIRED 2026-07-07 (`scripts/ca_tradeshow_contacts.py`):** attaches the event-marketing buyer + email per exhibitor. KEY REALITY: AI Ark's reliable path = the **MCP (in-session), NOT headless** — REST probe returned Unauthorized (`/api/developer-portal/v1/people` X-TOKEN) / 404 (`/v1/people`), and bulk export needs a webhook. So this step runs IN a Claude session with the ai-ark MCP; the helper does only the deterministic parts. Flow: `targets` (scripted, free — worklist of top-band exhibitors w/o published email + prints AI Ark recipe) → per domain Claude runs `mcp__ai-ark__people_search`(companyDomain+department=marketing+title="event marketing", FREE — pick best by title priority event>field>trade-show>brand>marketing-mgr) then `mcp__ai-ark__email_finder`→poll `email_finder_results` (COSTS credits, one contact/firm) → `merge --contacts <json>` (scripted, writes back, preserves published emails). TESTED LIVE: people_search on aja.com returned 14 real marketing contacts (Frank Walker/Barry Goch etc.); targets+merge deterministic parts verified. Credit-guarded to the hot slice only. Verify emails via SmartLead's built-in verifier before send.

**Apify path + polish DONE/VERIFIED LIVE 2026-07-08:** actor `skython~exhibitor-list-scraper` (All-In-One, 1502 runs) confirmed; real input schema = `start_url` + `output_type` required, `result_limit` caps credits (config default 500; 0=all). Live run returned rich rows: `__company_name`, `_company_website` (domain INLINE — --via apify skips Places), `_hall_stands` (booth), `_company_phone`, `company_description`. Script `_apify_exhibitors` mapped to those exact fields (20/20 had domains: 7thsense.one, adder.com, adiglobal.com). Also a MapYourShow-specific actor `skython~map-your-show-exhibitor-list-scraper` exists. POLISH done: booth-noise filter `_is_booth_noise` (AVIXA Lounge/Press Room/pavilion/lounge/etc. → filtered, applied in both extractors) + generic-email fallback `_generic_email` in enrich (marketing@>events@>info@, email_status="generic" when no person email published). AI Ark REST stays dead headless (Unauthorized/404) — contact step still MCP-only in-session.
**Trade-show pipeline now COMPLETE:** ca_tradeshow_scout (shows/exhibitors[--via apify]/enrich[Places+published/generic email]/score/run) → ca_tradeshow_contacts (targets→AI Ark MCP people_search+email_finder→merge) → /ca-outbound themed mockup. All stages tested live.

**FIRST FULL LIVE RUN 2026-07-08 (two real shows):**
- **InfoComm (Apify path):** 25 exhibitors w/ domains+booths inline; enrich 8 emails; AI Ark named contacts: Darius Seabaugh (SVP Marketing, ACT Entertainment), Kayleigh Waters (22Miles), Eric Harper (SVP Product Mktg, ADI→snapone.com parent domain). Past show (score 0) — used to prove the machine.
- **CEDIA Expo 2026 (LIVE TARGET, Sept 1, ~8wk = PRIORITY band 22pts):** added to config, platform=`directory`, URL=`https://cedia2026.smallworldlabs.com/exhibitors`. Extracted 62 real AV/smart-home exhibitors; enriched 15 (11 domains, 4 site emails); AI Ark found **Sue Yap — "Marketing and Event Manager" @ Blackwire Designs (sueyap@blackwiredesigns.com, VALID)** — the exact booth-swag buyer — + Jenifer Walsh (CMO, WAC Lighting) + alarm.com marketing team. Real actionable lead sheet produced.

**New `directory` platform extractor (`_extract_directory`)** handles a2z (eBooth.aspx anchor text) + smallworldlabs/A2Z (`/co/{slug}` links) via Firecrawl — unlocks A2Z/smallworldlabs shows the Apify actor doesn't support. Route: config `platform: directory`.

**LAST-MILE PACKAGE BUILT 2026-07-09 (the "deepen, don't add" session):** audit found every discovery engine (openings/gyms/tradeshows/triggers/signals) over-produced raw JSON — 13 run dirs, only 4 with enriched leads, **0 with outbound drafts, 0 mockups ever made** — despite the skill saying "the mockup wedge is what lands any of them." Ryan chose to DEEPEN existing engines over building new channels (pain-in-public/seasonal/RFP all deferred). Fix = new reusable **`scripts/ca_tradeshow_package.py`** (the missing convert step): reads a scored run's leads.json, renders house-style **SVG booth-swag mockups** (tumbler/tool-pouch/cap, company name on each) per target + a self-contained `proof_page.py` HTML proof. CEDIA run pushed all the way through: refined 15→**4 contactable targets** (A: Blackwire/Sue Yap + WAC/Jenifer Walsh named; B: Acoustic Innovations + Airzone role-inbox), dropped Alarm.com (big/agency-locked) + CEDIA-the-org (noise). Authored `outputs/ca-outbound/tradeshow-cedia-2026/outbound.json` (drafted Maclaine-voice email #1 + turnkey A/B/C mockup brief per target) + `README-REVIEW.md`. SVGs are the structure/stand-in; the real photoreal trio (Higgsfield nano_banana_2→Gamma, needs interactive higgsfield auth) is the human 15-min finish. Confirmed lesson #5: I now prefer named-AI-Ark over generic-published emails at refine time (Alarm.com wecare@ dropped). Deliverability rule enforced: email teases, mockup shows on reply/LinkedIn. Ryan chose HAND-SEND (not SmartLead) + name the 2 role inboxes via AI Ark. **AI Ark pass done 2026-07-09:** (a) **Airzone caught a bad domain** — the enrich domain-resolver mis-mapped "Airzone" → `zoneairconditioning.com` (an unrelated local HVAC shop); real exhibitor = **Airzone Control** (`airzonecontrol.com`, 402 staff, $10-25M) w/ named buyer **Matt Yilmaz, NA Marketing Manager** (Miami) — LESSON: verify exhibitor identity/domain before sending, the resolver can silently pick a wrong-but-plausible domain. Matt's AI-Ark email was personal-only (`matt@live.it`, free) → LinkedIn-first. (b) **Acoustic Innovations** = tiny shop (design/eng, production, front-office Robin Miller), no marketing/event role → keep published `sales@`, don't spend a credit. NET: Airzone upgraded B→A-, Acoustic stays role-inbox. Final package: Blackwire (Sue Yap, email) + WAC (Jenifer Walsh, email) + Airzone Control (Matt Yilmaz, LinkedIn) + Acoustic (sales@). NEXT: Ryan reviews copy in outbound.json → render photoreal trio for the A-list → hand-send.

**Real-world lessons from the run:** (1) Black Hat NOT scrapable — its 10times mirror (10times.com/hat/exhibitors) has only stale 2015 data, its own directory is gated, and it's not on a supported Apify platform. (2) Apify actor enforces `result_limit >= 20` (min batch). (3) AI Ark narrow title filter (`title="event marketing"`) frequently returns 0 — DEFAULT to `department=marketing` broad, then pick best-fit by title. (4) CEDIA's A2Z URL unsupported by Apify actor but its smallworldlabs mirror scrapes via Firecrawl `/co/` slugs. (5) Minor: merge preserves a generic published email (e.g. wecare@alarm.com) over a better AI Ark named contact — consider preferring named ai-ark over generic-published later. Config seeded with infocomm-2026 (proven template, past), blackhat-2026 (Aug 3, rush), money2020-2026 (Oct 25, sweet spot) — Ryan curates/updates show URLs as directories open. Related: [[ca-buying-signals]], [[ca-outbound-pipeline]], [[creative-alternatives-aios]].
