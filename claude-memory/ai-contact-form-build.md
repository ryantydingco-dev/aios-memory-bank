---
name: ai-contact-form-build
description: State of the AI Contact Form product (IdeaBrowser
metadata: 
  node_type: memory
  type: project
  originSessionId: 4843f26d-7dfa-43e1-b841-579d4004d84f
---

Ryan's primary Dealthreads offer: the AI contact form that pre-builds a buyer profile on submit (IdeaBrowser #7856). Goal: ~40 clients → $1M ARR; 8 clients × $2,500/mo = $20k/mo.

**Build status as of 2026-06-01: ~90%, demo-ready, past the "half-baked" line.**

Core files in `AI GTM Engine/Operations/scripts/`:
- `buyer_profile_enricher.py` — the engine. Domain (+name/email/form) → buyer profile. FREE/cheap path: Firecrawl scrape + search + Claude (claude-sonnet-4-6). NO Apollo/Hunter (both keys dead/401). Person-level enrichment added 2026-06-01: `enrich_person()` does "<name> <company> LinkedIn" Firecrawl search → Claude extracts title/seniority/linkedin_url/role_in_buying/confidence. Works because a contact form gives you the name. Honest: returns confidence "none" + no invented title when it can't find someone; refuses role inboxes (info@). Tested high-confidence on Charles Craigmile/Revenova + Tejas Manohar/Hightouch.
- `demo_server.py` — end-to-end live demo: smart contact form → live enrich → renders rep dossier → writes local CRM (demo_leads.jsonl) + upserts REAL HubSpot contact + note (when HUBSPOT_TOKEN set). HubSpot CRUD verified (create/note/delete all 200/201/204). Portal id 246275995. Has rate-limit + honeypot + daily cap for public safety.
- `teardown_page.py` — turns a profile JSON into the free before/after teardown lead magnet (Loom asset). Now includes the person block.
- `RUN_DEMO.md` — how to launch local + hosted, redeploy, add sandbox token.

**LIVE hosted demo:** https://ai-contact-form-demo-production.up.railway.app (`/` form, `/crm` inbox). Railway project `ai-contact-form-demo` id 14047de1-596e-4489-b56e-d85a0c6dfc26, workspace ryantydingco-dev, deploy source `aios-starter-kit/ai-contact-form-demo/`. Hosted = enrichment LIVE, HubSpot STUBBED (public link must not carry prod token), rate-limited 6/ip/hr + 150/day, ephemeral /crm. Verified: live submission enriched person+company in 27s.

**Honest gaps to true 95-100%:** (1) per-client install = host form on THEIR site + THEIR HubSpot token (~1 day, happens AFTER they say yes). (2) person-match coverage ~60-80% est, UNVALIDATED on rank-and-file (non-exec) leads — only real client leads give the true rate. (3) 30s/lead = fine for async "dossier before callback", too slow for in-chat real-time (v2).

ICP correction (Ryan is "a nobody", no logos): go SMALL — 5-40 employees, founder-IS-buyer, NO RevOps team (the wedge), $8k+ deals. Lead targets: Revenova (~74), Tai Software (~60), Ketch. Deprioritize CaptivateIQ/Sendoso to post-case-study. The free teardown IS the credential (give-first makes logo irrelevant). See [[dealthreads-gtm-experiment-engine]].

HubSpot has NO native external-webhook→create-contact; must upsert via CRM v3 API (notes assoc typeId 202). v1 = smart form (enrich on submit), chat widget = v2.

**GTM build (2026-06-01, via IdeaBrowser GTM skills seeded with our context):** Locked offer = **$2k one-time setup + $1k/mo**, **pay-when-it-works guarantee** (free install, pay only once profiles land in CRM), **book-only / no price on the page**. NO separate product name — dropped "FormIQ", sold under **Dealthreads** brand (dealthreads.io). Coverage validated ~95% person-match on findable buyers (n=20, non-circular). Assets in `dealthreads-gtm-agent-repositioning/ai-contact-form/`: `OFFER.md` (canonical Grand-Slam offer = brief for everything), `dealthreads_landing.html` (the new dealthreads.io homepage: VSL hero + interactive wedge wired to the live demo w/ ?website= prefill + book-a-call, no price), `VSL_Script.md` (mechanism = "Submit-to-Profile", demo-as-proof screen-recording, 3 hooks + 3 ad scripts), `Person_Coverage_Validation.md`. GTM motion: free teardown wedge → top 25 get 1:1 LinkedIn+Loom, long tail gets cold email (Smartlead+Vantage+AI ARK) → both → dealthreads.io. IdeaBrowser GTM skill IDs: Landing Page Architect fb23c945, VSL Writer 7bfb6a5c, Offer Coach fb06d125, Email Wizard a6098c56, Lead Magnet Legend 8acd9d8e, Funnel Architect d9cc19d7 (run_skill returns an expert prompt I execute; no project_id used — fed our OFFER.md instead). Ryan's launch to-dos: record the VSL, add Calendly link (page placeholder [YOUR_CALENDLY_LINK]), deploy page to dealthreads.io.

**LANDING PAGE BUILT 2026-06-01** (note: the `dealthreads_landing.html` referenced above was never created at that path — THIS is the real, built page): The dealthreads.io homepage is live-built at `aios-starter-kit/apps/deal-threads-landing/index.html`. The OLD real-estate "DealThread workflow layer" page (Zillow/FUB/Dotloop) was replaced IN PLACE per Ryan's call — backup at `index.real-estate-backup.html`; index.html is the served homepage (server.js line ~1963 + Netlify `publish='.'`). It's the AI-contact-form "Submit-to-Profile" page on a full 10-section blueprint: dark threaded hero + live-building dossier console (Acme Robotics / Series C $150M / VP Marketing / ICP 92), market-data-AS-proof (IdeaBrowser Pain 9/10, $4–5B@12–15% CAGR, WTP 8/10, 27,400+, Drift/Qualified gap — NO fake testimonials, Ryan's explicit call), **book-only / NO price**, pay-when-it-works guarantee, A/B hero headlines (`?v=a` default / `b`), inline domain→demo widget (`DEMO?website=<domain>`), all CTAs → cal.com/ryantydingco/30min. Self-contained (embedded CSS+JS), mobile-first, reduced-motion-aware. Brand kept per BRAND.md (DealThread, teal #0f766e + gold #d99a2b, Inter/JetBrains Mono). Imagery via **Higgsfield MCP** (f0075740…, nano_banana_pro): `assets/brand/lp-hero-bg.webp`, `lp-submit-to-profile.webp` (form→dossier diagram in How-it-works), `lp-og-cover.jpg`; hero motion loop via seedance_1_5 → `lp-hero-motion.mp4/.webm` (704K, gated desktop+fast-conn). Copy via a Workflow (3 drafters + 3-lens adversarial QA); QA fixes applied (cut fabricated "market timing 9/10" + "~$280k" salary figures, killed "threads the gap" pun, dropped Follow Up Boss → 4 B2B CRMs HubSpot/Salesforce/Pipedrive/Close, added mid-CTA band + footer + GDPR/CCPA in compliance FAQ). **OPEN FOLLOW-UPS:** (1) create `privacy.html` + `terms.html` — footer links currently 404, privacy needed to back the GDPR/CCPA/compliance claims; (2) confirm the live demo actually accepts `?website=`; (3) confirm footer contact `ryan@dealthreads.io` is real. Local preview: .claude/launch.json config "dealthread-landing" (:8099).

**DEPLOYED LIVE 2026-06-01 → https://dealthreads.io** (verified rendering: hero, animated dossier, leak scene, motion loop, all assets 200). Host = Railway project `dealthreads` (id 8005e6c5-ff5f-4852-b4fd-ffd154d7623d), service `dealthread-landing`, env production, custom domain dealthreads.io. **Deploy flow = git push:** the app is its OWN git repo (`github.com/ryantydingco-dev/dealthread-landing`, branch `main`) — `git push origin main` auto-triggers a Railway build (~100-150s: `next build` + boot). Railway CLI is authed (ryantydingco@gmail.com); `railway status` shows live deploy state. **CRITICAL GOTCHA:** prod runs **Next.js** (`npm start` = `node server.mjs`), which serves static files ONLY from `public/` — NOT the repo root. Root `index.html` IS served at `/` (via the Next app), but **all referenced assets must be copied into `public/assets/...`** or they 404 in prod (root `assets/` is only used by the local python preview). We hit exactly this: lp-*.webp/.mp4 were committed to root `assets/brand/` → 404 live → fixed by copying to `public/assets/brand/`. So for any new landing asset: put it in BOTH `assets/brand/` (local preview) AND `public/assets/brand/` (prod), commit both, push. The 4MB source PNGs are intentionally NOT committed (only the webp/jpg/mp4/webm web versions). **STILL OPEN:** (a) Higgsfield image endpoint had a multi-min outage 2026-06-01 ~15:12+ — the 3 extra story visuals (before/after split, "giants left mid-market", 4 pain glyphs) are designed + prompted but NOT yet generated; retry when it recovers, then copy to public/assets/brand + push. (b) privacy.html/terms.html still 404. (c) confirm demo `?website=` param.

**UPDATE 2026-06-01 (later, same session):** Page TIGHTENED & redeployed ("cut hard"): problem section = leak image + 4 timeline beats + 2 pains (was 8 rows + punchline + 4 pains); pull-quote section deleted; features 6→4 (2x2 grid); proof evidence 4→2 + reserved slot removed; founder 3→2; `.section` padding → clamp(40px,5.5vw,72px). **privacy.html + terms.html NOW LIVE** (built in BOTH root + public/, footer links resolve, GDPR/CCPA backed; honest practices: public-signal enrichment, no fabrication, processor-for-clients; Terms has a bracketed [governing state/venue] placeholder for Ryan to finalize). **IMAGE WORK STILL BLOCKED:** Higgsfield generation endpoint down account-wide since ~15:12 (35+ min; ALL models incl nano_banana_pro AND z_image return "Something went wrong"; balance OK 3720cr ultra; no stuck/pending jobs; nothing charged). User wants: restyle the 3 existing images bolder + add 2 new (before/after split, "giants left the mid-market") → generate when endpoint recovers, copy to public/assets/brand, push.

**RESOLVED + SHIPPED 2026-06-01:** The "outage" was actually a FAILED SUBSCRIPTION PAYMENT on Higgsfield (banner on higgsfield.ai: "Payment failed… Unlimited generations are paused") — NOT a platform outage; the READ endpoints (balance, show_generations) kept working, only `generate` was paused, which is why all models failed identically. Diagnosed by taking over the user's logged-in browser. User fixed payment → generation resumed instantly. Generated + DEPLOYED: **2 new scenes** — `lp-before-after.webp` (WITHOUT vs WITH-DealThread CRM-record split, in the solution section) and `lp-midmarket.webp` (SMB/MID-MARKET/ENTERPRISE "giants left the lane open", in why-now) — plus **bolder image-to-image restyles** of leak + hero-bg (`lp-leak.webp`, `lp-hero-bg.webp`; originals backed up locally as `*.orig.webp`) and a **regenerated hero motion loop** (`lp-hero-motion.mp4/webm`, seedance from the new bg, ffmpeg-compressed ~780K). **DIAGRAM kept as the verified original** (`lp-submit-to-profile.webp`) — couldn't visually verify the restyle's text labels because the image-read API saturated ("many-image request" 2000px limit) late in the session; the unused diagram restyle sits at `lp-submit-to-profile-v2.webp` if wanted later. Bold restyle palette: bg `#0b0e14`, teal `#14b8a6`/`#2dd4bf`, gold `#f0a92b`. **REPO NOTE:** the landing app got parallel commits from another process — a `/blog` "Field Notes" SEO feature (`app/blog/`, `content/blog/*.md`, `components/lead-leak-fix-page.tsx`, +1 nav link in index.html) — so I had to `git pull --rebase origin main` to integrate before pushing (rebased cleanly; both changes coexist). Expect to rebase on future pushes. Live commit: f3fa65f.

**VISUAL CLEANUP 2026-06-01 (supersedes the "2 new scenes shipped" claim above):** Ryan said the page "doesn't look good" + images "taking up so much real estate." CUT both Higgsfield story scenes — `lp-before-after.webp` (dark/busy AND redundant with the how-it-works diagram right below it) and `lp-midmarket.webp` (neon SMB/ENTERPRISE decoration; the "wide-open lane" point is already in the adjacent why-now text). Page now keeps ONLY the 2 clean visuals: `lp-leak.webp` (the leak diagram, 598×254) + `lp-submit-to-profile.webp` (form→dossier diagram, 598×401), both capped `max-width:600px; margin:auto`. **Real bug fixed:** `.how-diagram img` had `width:100%` with NO `height:auto`, so the browser fell back to the HTML `height="848"` attr and rendered the diagram stretched to ~2× height (848px vs the correct 401px) — added `height:auto;display:block` to both `.how-diagram img` and `.leak-figure img`. LESSON: when you cap an img's width in CSS while leaving its HTML width/height attrs, you MUST set `height:auto` or it distorts. Live commit **d39aea4** (verified live: before-after/midmarket refs = 0, `height:auto` ×2 present, home + both imgs 200). This deploy needed NO `public/assets` copy (only index.html changed) and divergence was 0/0 (no rebase this time, unlike the prior push). Local preview gotcha: this embedded preview's screenshot decouples from JS-driven scroll (jump-scrolling past IntersectionObserver `.reveal` triggers → blank frames); trust `preview_eval` getBoundingClientRect measurements over `preview_screenshot` for layout checks here.

**PRODUCT EVOLVED → CHAT WIDGET + LANDING REPOSITIONED (2026-06-01):** Codex (OpenAI CLI, logs at `~/.codex/sessions/`) was building the real product in the background at `AIOS-Memory-Bank/AI GTM Engine/replede-dev/` while the landing page was being built here. The mechanism CHANGED: it's no longer a "smart contact form that enriches on submit" — it's a **conversational chat widget that REPLACES the static form**. A `<script>` tag drops a chat that asks the buyer's goal first, then qualifies (intent, company, role/authority, timeline, budget, stack) over ~2-4 min, enriches the company (internal-first: website scrape, NO paid vendors), scores ICP 0-100, routes with SLA, and writes a rep-ready profile to the CRM. Canonical flow: `AI GTM Engine/Product/Replede Conversation Flow v1.md`. `replede-dev` = working Node app (`server.js` ~4,776 lines, zero deps, JSON store, HubSpot stub adapter, internal enrichment, `/crm` rep inbox, beta-client multitenancy, passing regression test, ~65-75% to beta; gaps: live HubSpot test, Postgres, real LLM-default, deploy). Advanced scripted demo at `AI GTM Engine/dealthreads-demo.html` (fake "NorthAxis" host site + chat → buyer profile reveal). **IMPORTANT — two separate products in that folder, don't conflate:** (1) DealThreads = the inbound chat-widget product (replede-dev); (2) "AI GTM Engine" / Packaging = a separate OUTBOUND service run on Oloxa as client-0. **LANDING PAGE REPOSITIONED to match** (commit a6d9c5f): mechanism **Submit-to-Profile → Conversation-to-Profile**; hero "Your contact form can only collect. A conversation qualifies."; hero console gained a "Said in chat" row; how-it-works 3 steps now chat-based; "why now" sharpened (Drift/Qualified WERE the mid-market's conversational front door, both left); FAQ added "Isn't this just a chatbot?"; all metas/footer rewritten. **NEW `demo.html`** (landing repo root + `public/`) = the chat-widget demo rebranded teal (`#0f766e`; original was indigo `#6366f1`) + an added authority question; all "Try the live chat" CTAs → `/demo.html`. The hero domain-widget still → the enrichment/teardown demo (ai-contact-form-demo, domain→dossier) for "see enrichment on your own company". **DROPPED** the form-themed how-diagram image (`lp-submit-to-profile.webp`, now unused/contradicts). **VISUAL DEBT (follow-ups):** (a) regenerate a chat→profile diagram for how-it-works; (b) the cinematic before/after clip (`outputs/deal-threads-demo/clip/demo-cinematic.mp4`) still shows a name/email FORM, now slightly off-narrative — refresh to a chat opener when revisited.

**GTM PIVOT → security/compliance services + DRATA EDGE (2026-06-01):** Target moved from freight/vertical-SaaS to **security/compliance SERVICE firms** (vCISO firms, boutique security consultancies, compliance MSPs/MSSPs). Driver: Ryan works at **Drata** (compliance automation) → native industry credibility = the answer to his "I'm a nobody" problem. **⚠️ DRATA CONFLICT GUARDRAIL:** lean on his KNOWLEDGE of the space (his to use), NOT Drata's customers/partners/data/relationships (moonlighting/IP/non-solicit risk — he should check his employment agreement; build on own time/equipment; public-source prospecting only). Bias to CMMC/DoD + pentest firms (low Drata overlap); **Bright Defense flagged = Drata Gold partner → skip or clear first.** Top macro "why-now" hook = **CMMC 2.0 Phase 2, Nov 10 2026** (hard cert wall, ~8,350 DoD contractors). Moat thesis = vertical focus + done-for-you service + free-teardown-as-distribution + reachability + Drata-world credibility — NOT the tech (replicable).

**First batch + recorded-video engine (2026-06-01):** Prospect batch + verified LinkedIn connect list at `aios-starter-kit/outputs/dealthreads-security-compliance-batch-2026-06-01.md` (5 send-this-week: ATX Defense, MAD Security, Securafy, Ridge IT Cyber, Redbot Security; 15 verified LinkedIn URLs, each w/ a dated signal + a warm no-pitch connect note). Motion: LinkedIn connect (warm, no pitch) → free Dead-Form Teardown → book. Built a recorded-video lead-gen system (public brand = **DealThread**, never "Replede" on camera): `AI GTM Engine/Product/DealThreads Recorded Video Kit.md` (3 layers: flagship ~12min + clip engine + Teardown-Reaction series), `AI GTM Engine/Product/DealThreads Flagship Video — Word-for-Word Script.md`, + a 14-asset branded graphics kit at `aios-starter-kit/outputs/deal-threads-demo/video-kit/` (4 hero graphics, 5 section cards, 3 thumbnails, lower-third, caption) reusing the Higgsfield intro/outro stings + demo b-roll, + a **Gamma deck** ("Your Contact Form Is Killing Good Leads", 14 cards, theme `founder`). **Gamma gotcha:** setting imageOptions `themeAccent` + textMode `preserve` = bare, image-less skeleton cards; use Gamma's defaults (aiGenerated + generate) or `source:aiGenerated`+`stylePreset:photorealistic` for a full, designed deck.

**ICP locked + Prospecting engine built (2026-06-01):** ICP = founder-led B2B 10-40 ppl, $8k+ ACV, real "Request a Demo" form, sales-led, no RevOps. HARD GATE: must already get inbound form fills (kills referral-only). Beachhead COMBO = boring vertical SaaS (freight/construction/fieldservice/mfg/healthcare-ops/legal-ops) + B2B running paid ads to a form. Signals stacked 0-4: hiring sales/RevOps (Greenhouse/Lever/Ashby) + recently-funded (strict, excludes DB pages, flags $25M+/Series C+ as too-big) + runs-ads (pixel scan) + chat tool. Docs: `ICP.md`, `AI_ARK_Pull_Brief.md`, `Outreach_Sequences.md` (1:1 LinkedIn+Loom for HOT, cold email for WARM, + form-test wedge), `First_Batch_signal_scan.csv`. Engine = `AI GTM Engine/Operations/scripts/prospecting_engine.py` (source modes: directory [harvest small vendors from 'best X for small business' listicles — best yield] / search / --infile to score any list incl. AI ARK export; score → size-gate → form-gate → rank HOT/WARM/COOL). **KEY LEARNING: you CANNOT get employee-count from a website (homepage size-gate guesses wrong both ways); firmographic data is required — that's why AI ARK's employee filter is essential and complementary. The enricher's funding/size search DOES get real headcount, so size truth lands at the teardown stage.** First real layups found (freight): quotefactory.com (~13 emp, STRONG fit = the standout), mvmnt.io (~28), tai-software.com (60-87, bigger) — teardowns generated in `ai-contact-form/teardowns/`. GTM build COMPLETE (offer+landing+VSL+prospecting); now execution: Ryan records VSL, adds Calendly, deploys page, runs AI ARK pull, sends teardowns.
