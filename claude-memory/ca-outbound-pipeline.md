---
name: ca-outbound-pipeline
description: The /ca-outbound one-command cold-outbound pipeline built 2026-07-07 — AI Ark → crawl → personalize → gates → SmartLead DRAFT → Telegram. Draft-only. All 28 GrowthEngineX skills installed. Data engine = AI Ark only.
metadata: 
  node_type: memory
  type: project
  originSessionId: 4f2057a5-da32-467d-af31-39ca7205edd4
---

Built 2026-07-07 in the CA workspace. Full docs: `Creative-Alternatives-AIOS/scripts/CA-OUTBOUND-README.md`. This is the game-plan weeks-5-6 outbound-v2 build, automated into one command.

**Install:** all 28 GrowthEngineX coldoutboundskills are now in `Creative-Alternatives-AIOS/.claude/skills/` (was 6, added 22). Data engine = **AI Ark ONLY** (Apollo + LEAD411 removed from memory per Ryan 2026-07-07; the standalone lead411 memory was deleted).

**The pipeline — `/ca-outbound <financial|law> [--limit N]`:**
1. AI Ark pull (MCP: industry_search/location_search to resolve enums → email_finder → poll email_finder_results). REST base `https://api.ai-ark.com`, key `AI_ARK_API_KEY` in CA .env.
2. `scripts/ca_outbound_prep.py crawl` — site crawl → company_description.
3. `/ca-personalization-subagent` (NEW skill, adapted from personalization-subagent-pattern) — Task-tool fan-out, per-lead `situation_line` (opener) + mockup-tease `cta_line`, Swag Handled offer + named proof (Thermo Fisher/Trinity Health/Farm Credit East), approval loop then scale.
4. Gates: `/spam-word-checker` + `/list-quality-scorecard`.
5. `ca_outbound_prep.py assemble|variants` → leads.csv + variants.yaml.
6. Upload DRAFT via the repo's proven `upload.ts` (npx tsx; node v22 available) — VALIDATED end-to-end (parsed clean, died only at fake-key 401, nothing created).
7. `ca_outbound_prep.py notify` → Telegram review ping.

**Key files:** skill `ca-outbound` (orchestrator) + `ca-personalization-subagent`; `config/ca_outbound.yaml` (ICP AI Ark filters from the proven 2,918-match batch-1 query; NY/NJ/CT, 25-500 emp, office-mgr/marketing/ops/HR/EA/partner titles); `config/ca_variants_template.yaml` (Maclaine's Swag Handled copy, MUST stay compatible with upload.ts's restricted YAML parser — quoted `\n` bodies, no `|`/`{}`); `scripts/ca_outbound_prep.py`.

**Guardrails (hard):** DRAFT-ONLY, never sends/activates · per-run cap ≤100 leads · cold inboxes only (never the reactivation/warm pool — deliverability firewall) · exclude existing customers/competitors · only the opener is personalized, proof+mockup-tease stay static · email teases the mockup (no embedded image in #1).

**Prerequisites before it can actually run a real campaign:** (1) tag SmartLead cold inboxes `financial`/`law` via /smartlead-inbox-manager (else upload.ts throws "no healthy inboxes"); (2) run once interactively to tune+lock the personalization prompt; (3) upload.ts does NOT set stop-on-reply/tracking-off → set those in the SmartLead UI at review.

**Scheduling:** `scripts/ca_outbound_cron.sh` + `~/Library/LaunchAgents/com.aios.ca-outbound.plist.disabled` (biweekly ICP rotation, Mon 7:30) exist but are OFF by default — enable only after the 3 prerequisites. Runs `claude --print` headless, still draft-only.

**Cheap lead waterfall (built 2026-07-07, `/ca-leads`):** `scripts/ca_lead_waterfall.py` self-sources leads so AI Ark is gap-filler only. Stages: registry-ny-law (FREE NY Attorney Registrations Socrata `eqw2-r5nb` → real attorney+firm+phone+address, NY-metro + currently-registered) → resolve-domains (Google Places, owned, firm→domain+main phone, ~1000 free/mo) → **harvest (Firecrawl, owned — THE key stage: /v1/map finds attorney bio pages, scrape → regex REAL published emails; published=no verification needed; infer firm pattern → pattern-fill colleagues free)** → permute (only firms harvest couldn't crack) → verify (only the permuted gap; SKIPS published/pattern) → build (→ `outputs/ca-outbound/<run>/leads.json` email-ready = published+pattern+verified, + `call-list.json` phone-only). TESTED LIVE end-to-end: registry 300 real attorneys free; Places real domains+phones; **harvest got all 4 Schlam Stone attorneys' REAL emails (dgoldsmith@/bsimon@… ) + inferred pattern {f}{last}, ~5 Firecrawl credits**; permute skips solved leads; build emits published w/ no verifier. `/ca-outbound --leads <path>` consumes it and SKIPS AI Ark.
**Verifier problem SOLVED by harvest:** published/pattern emails (the majority) need NO verification. Only permuted-gap addresses need a verifier — and SmartLead has a BUILT-IN one (credit add-on, ~$15/6k, inside the tool Ryan already pays for). ZeroBounce key is dead (-1); no longer a blocker — pipeline emits published+pattern even with no verifier (exit 0). DIY SMTP probing NOT worth building (M365/Gmail block it: port 25, false 250-OK, catch-all). Full playbook: [[self-sourcing-leads-playbook]].

**VOLUME MANDATE + CAPACITY AUDIT (2026-07-10):** Ryan's division of labor: reactivation/inbound = his weekend project; **his main job = new-customer revenue via cold at "insane volume"; all motions run simultaneously.** Live SmartLead audit found **100 inboxes, ALL warmup-active, 97 @ 100% reputation, 24 domains, ~2,140/day theoretical cap** — but only ~14 CA-brand inboxes (alternatecreativity/creativealternativesthree/imaginativechoices/innovativealternativesolution, 420/day) presumably carry the 3 active Swag campaigns. **77 warmed inboxes sit IDLE from the retired AI-consulting era: calendargroup* ×10 domains (48 inboxes/960 day) + vantageoutbound* ×6 (29/580), from_name "Ryan Tydingco"** — insane volume is a REPOINTING job (from_name→CA identity, CA signature, tracking domain, 301 domain→creativealternatives.com, ramp 10/day +5/wk), not a buying job. Plus restoration/rh ×9 (Adam Slipakoff identity, old Dealthreads) — repoint last. Full plan: `Creative-Alternatives-AIOS/plans/cold-volume-scale-plan-2026-07-10.md` — target 1,500–2,000/day (~35-45k/mo) in 4 weeks (+25%/wk ramp), lead-flow quota ~3,500 verified/wk (the REAL constraint; signal engines > persona pulls > waterfall), 13 drafted vertical campaigns to load+stagger, bounce >3% = pause, kill rule 1,000 sends/0 positives. Benchmarks from live data: camps 68% open/~10% reply vs cold Financial 49%/0.4%, Law 43%/0.8% (6 days in) — fit-heavy blend matters even at volume. (API quirk: get_all_email_accounts showed is_connected=0/sent=0 for all inboxes despite active campaigns — likely day-boundary/endpoint quirk, verify per-campaign if it matters.) **EXECUTED 2026-07-10 ("let's go"):** all **29 vantage inboxes repointed LIVE** — CA signature set via REST (`POST /email-accounts/{id}` w/ `signature`, needs `User-Agent: Mozilla/5.0` or Cloudflare 1010-blocks; the smartlead MCP update tool does NOT expose signature, and `tags` is API-rejected → tag in UI or attach IDs directly at campaign creation). Fleet manifest `config/planners_fleet.json` (29 IDs, 580/day). from_name already "Ryan Tydingco", local-parts all ryan-variants = clean. **calendargroup 48 PARKED: local-parts are ALL "Steven Laitmon" variants (third-party name) — do NOT send CA mail from them until Ryan confirms ownership; if owned, create new CA-named mailboxes on those warmed domains rather than renaming over.** **SUPERSEDED same day — Ryan's decision: use NEITHER calendargroup NOR vantage for CA; buy fresh CA-branded domains + warm them** (brand coherence; the repointed vantage sigs are harmless ballast/retire candidates, `config/planners_fleet.json` is now moot). New fleet spec in the scale plan §2: 30 CA-adjacent .com domains × 3 inboxes = 90 new (never >3/domain), Ryan+Maclaine identities, SPF/DKIM/DMARC + tracking domain + 301 day-1, 2-3wk SmartLead warmup before cold then 10→20/day ramp, staggered 10 domains/wk. Infra provider = **INFRAMAIL (Ryan's existing, key in CA .env since 2026-05 — flagged for rotation, was pasted in chat).** Their API (docs behind dashboard: testapp.inframail.io/docs, auth header w/ `inf_` key) does **inbox create/delete/list only** — needs Profile ID + Customer ID + Host Order ID from *Subscription → View API Access* (not yet in .env: add INFRAMAIL_PROFILE_ID/CUSTOMER_ID/HOST_ORDER_ID); **domain ORDERING is dashboard-only** (auto-DNS: SPF/DKIM/DMARC handled by their flow). Split: Ryan orders 30 CA domains in dashboard + grabs API vars → Claude scripts 90-inbox bulk create + SmartLead connect (create_email_account w/ Inframail SMTP/IMAP) + warmup + signatures. (Zapmail/Dynadot skill exists in workspace but unused — no keys.) Revised curve: full ~2,000/day lands week 5-6 (warmup shifts it right ~3wks); meanwhile a small planners pilot (~50 leads) can share 4-5 existing CA-brand inboxes to validate the ICP. First /ca-outbound event_planners run still needs the interactive 10-lead personalization approval loop. **RE-REVERSED → FINAL (2026-07-11): vantage 29 is IN** (Ryan: "rotate the Vantage outbound ones and use those" — after the TAM-burn discussion; `config/planners_fleet.json` is live again). **Usable fleet = 43 (14 CA-brand + 29 vantage) ≈ 900/day ≈ 27k/mo, no purchase.** calendargroup + restoration stay OUT. **New-domain buy DEFERRED to data trigger** (replies absorbed + converting at ~900/day, or domain-health dip; ~Sept). TAM-burn answer that settled it: ~13-15k new people/mo vs 3-5M contactable universe = years of national runway, but narrow ICPs burn in weeks (law NY/NJ/CT ~2,900 ≈ 1wk) → portfolio rotation + 6-12mo re-approach + **suppression/touch ledger BUILT + WIRED 2026-07-11: `scripts/ca_touch_ledger.py`** (check/log/seed/stats; ledger `data/outbound/contacted.csv`; suppresses existing QB customers via latest customers_master.csv + anyone touched <180d by any motion; FIRST gate in ca-outbound skill step 5, log after upload; tested. Seeded w/ 1,500 real contacts — PARTIAL 500-lead pages of Financial/Law/camps; seed full campaign exports before heavy ramp). Planners pilot pull attempted 2026-07-11: **AI Ark FULLY down (email_finder AND people_search both 503 by evening) — retry; "event coordinator" title added to the ICP config per Ryan.**

**DAILY BRIEF LIVE (2026-07-11, Ryan's "make it automated so I don't think about it"):** `scripts/ca_daily_brief.py` + launchd `com.aios.ca-daily-brief` (7:35am daily, loaded + first send verified ✓). ONE plain-text Telegram (Markdown chokes on _ in filenames): active campaigns, drafts awaiting his Start click, mockup-queue reply count (found 4 sitting unnoticed), ledger count, and the cockpit todo list — **`data/outbound/cockpit_todos.json` is THE single human to-do surface**: sessions add items, remove when done; brief says "nothing needs you" when empty. Remaining path to full-auto outbound: one 10-min personalization-prompt approval per ICP → enable `com.aios.ca-outbound.plist.disabled` (headless biweekly draft loop, already built).

**PERFORMANCE WIRING SESSION (2026-07-12, "wire it up so it performs"):** (1) **Live-campaign tuning applied + verified via REST:** follow_up_percentage 30→55 on ALL 3 actives (`POST /campaigns/{id}/settings` — but `max_leads_per_day` must go via `POST /campaigns/{id}/schedule` as `max_new_leads_per_day`, send full schedule object); Financial throttled to 50 new leads/day (was 500) — capacity flows to Law National (43% unique opens, best). (2) **All 45 replies scored + categorized via `POST /campaigns/{cid}/leads/{lid}/category`** (category ids: Interested=1, Meeting=2, NotInt=3, DNC=4, Info=5, OOO=6, Wrong=7, Uncat=8, Bounce=9; fetch via /leads/fetch-categories). Result: **2 Interested (⭐ Wil Antonides/Miller Johnson = OPEN QUOTE REQUEST ~60x Yeti Rambler 20oz + padfolio for retreat, comparing AnyPromo; Christina/INVST said "send them over" — INVST mockups ALREADY EXIST in pillars/2-customer-acquisition/mockups/invst/)**, 29 OOO, 8 wrong-person, 2 NI, 1 DNC (wmichaelson@corrcronin.com — categorized + global-blocklisted via /leads/add-domain-block-list), 3 bounces. True positive ≈ 2/8k sends — validates fit>volume. Reply-body extraction: strip <style> blocks BEFORE tag-strip (VML/CSS hides real replies — Wil's quote + a clean "no thanks" were hidden that way). (3) **Reply loop AUTOMATED:** `com.aios.ca-mockup-inbox-poll` (poll 9:00/12:30/16:00 wkdays) + `com.aios.ca-reply-triage` (headless `claude --print "/mockup-inbox"` 10:00/14:00 wkdays via scripts/ca_reply_triage_cron.sh, skips when queue empty, runbook is draft-gated). (4) **Daily brief now shows per-campaign sent/replies/interested** (analytics call per active). (5) **Planners pilot pull FIRED** (AI Ark recovered): email_finder trackId `21973897-d47f-4979-8950-f8413e575b3f`, 50 leads, filters incl. exclude assistant/wedding — was still PENDING at session end; **collect via email_finder_results, trackId saved in outputs/ca-outbound/2026-07-12-event_planners/pull.json** → then crawl → 10-opener approval → DRAFT on the vantage 29.

**LAW CAMPAIGN #3 BUILD (2026-07-14, Ryan re-upped AI Ark credits, chose law as best ICP):** angle = **FALL RECRUITING SEASON** (Ryan picked over retreat-new-geos and practice-group; hard ~6wk OCI deadline), 500 leads in 5×100 batches. NEW buyer persona (legal recruiting mgr/coordinator/director — untouched by Law US/National) + NEW geos (TX/FL/IL/GA/PA/DC). ICP `law_recruiting` in ca_outbound.yaml; **reuses empty DRAFTED shell campaign 3582356** ("Law Firm Custom Apparel & Gifting" — rename in UI); inboxes = 20 of vantage 29 (tag law3/attach by ID). Copy `sequences/law-recruiting-cold-copy.md` — **escalating follow-ups by design** (the 2026-07-14 audit fix: live law steps 2-3 get 47-53% opens/~0 replies because they re-ask; new seq: tease → "I already made it" → proof+deadline → close). **Batch 1 fired: trackId `12c9991e-d903-4b77-85e6-94ab745de9d9`** (pending at session end; metadata in outputs/ca-outbound/2026-07-14-law_recruiting/pull.json). Flow per batch: collect → **ledger check (critical: ~6k law contacts already touched)** → crawl → (batch 1 only) 10-opener approval → gates → DRAFT into 3582356. **Audit context (2026-07-14):** OOO flood = July vacations (mechanical, ~65% of replies; OOOs mined into retouch dates + referral paths — 5 new replies categorized 7/14 incl. LB Realty internal-forward + Lipe Lyons retiree naming 3 replacements); positives thin because broad-cold lacks triggers; deliverability excellent (<0.5% bounce, 40-53% opens). Follow-up rewrite for the two LIVE law campaigns offered, not yet approved.

**FOLLOW-UP REWRITES PUSHED LIVE (2026-07-14, Ryan approved):** Law US: step 2 += recruiting-season urgency line, step 3 replaced w/ "recruiting season math" deadline email (dropped the cold 15-min-call ask). Law National: NEW step 3 "retreat date math" (+5d) inserted, old close moved to step 4 (+6d) — sequence now ends ~day 14 (was day 7, before the retreat decision window). Both verified live. **CRITICAL API LESSONS:** (1) `POST /campaigns/{id}/sequences` **REPLACES THE WHOLE SEQUENCE** — always post ALL steps or the missing ones are DELETED (Law National steps 1-2 briefly wiped, restored from the pre-edit dump — ALWAYS snapshot sequences to a file before editing). (2) GET after POST serves **stale cache** for ~a minute — a failed-looking verify may be lies; re-read after 10s+. (3) Write key = `seq_variants` (GET returns `sequence_variants`; extra fields like email_campaign_seq_id are rejected; minimal variant = subject/email_body/variant_label, omit id for new). (4) POST may time out on read yet still apply. (5) **Law US step 1's variants are INVISIBLE to the GET** (returned empty pre- and post-edit) — my full-sequence save posted step 1 with empty seq_variants; can't verify via API whether step-1 copy survived → cockpit flag for Ryan's 10-sec UI check (no new leads pending on Law US so blast radius ≈ 0; backup + reconstruction source = swag-handled-cold-copy.md if blank). Earlier step-reply diagnosis CORRECTED: step-1 reply concentration is largely OOO-auto-reply artifact (they fire on first touch only); live follow-up copy was already partially escalated by a prior session.

**FIRST FULLY-AUTOMATED MJ-STYLE MOCKUP PACKAGE (2026-07-16, Ashley Osborn @ Joseph Hollander & Craft, "Sure, send over the mockups" on Law National step 1):** complete reply package in queue/drafts/joseph-hollander/ awaiting Ryan's send approval. **KEY UNLOCK: the `higgsfield` CLI (~/.local/bin/higgsfield) works HEADLESS** — export HIGGSFIELD_API_KEY/SECRET from aios-starter-kit .env; the "needs interactive MCP auth" assumption was wrong. Flow that worked: Clearbit/site logo blocked → **Google favicon fallback (google.com/s2/favicons?domain=X&sz=256) gave a clean JHC mark** → PIL upscale onto 1024 white card → `higgsfield upload create` → 3× `higgsfield generate create nano_banana_2 --image <id> --aspect_ratio 4:5` (UNDERSCORE flags; --aspect-ratio fails; result in `result_url` field NOT the first url in JSON; `generate wait <id>` polls) → QC'd letter-perfect → CloudFront URLs → Gamma lookbook. **Gamma image-drop bug CONFIRMED twice + SOP retry CONFIRMED working:** first preserve-mode generate dropped all markdown images; retry with additionalInstructions listing EACH image URL + its target card embedded all 3 (final: gamma.app/docs/o4l1u0gkgj8ggff, marine theme). Also: text-only Gamma PDFs are legitimately ~100KB — the small-PDF failure test only applies to image decks; verify via read_gamma instead. Ashley published her direct line (913) 215-9311 → recommend Maclaine calls ~30min after send. 3 more replies same day (Carney Law, Safe Harbor Estate, Brooke Maule) — retreat campaign warming.

**PII note:** fresh AI Ark lead pulls + waterfall lists go to `outputs/ca-outbound/` which is now gitignored. Pre-existing: `outputs/gtm-sprint-2026-07-06/reorder-rescue-call-list.csv` (117 customer contacts) IS tracked in the private CA repo — legitimate synced business data, but be aware it's in git. Related: [[ai-usecase-research-2026-07]], [[creative-alternatives-aios]], [[two-machine-sync]].
