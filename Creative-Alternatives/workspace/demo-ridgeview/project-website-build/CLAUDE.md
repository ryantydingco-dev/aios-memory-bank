# Ridgeview Marketing Consultancy — Website Build (SANDBOX)

> This is a **training sandbox** from the AAA "Per-Client Digital Employee" workshop,
> installed 2026-07-03 so the L1/L2/L3 demo agents (`@pm-ridgeview-l1/l2/l3`) have a
> live folder to run against. Nothing here is a real client. Use it to learn the
> pattern before building real CA agents with `/create-agent`.

## The engagement

Ridgeview Marketing Consultancy hired us to rebuild their website. Client contact:
**Rachel** (see `../profile.md`). Approach: template-first build, then custom
components only where the template falls short.

## Working rules

- `tasks.md` is the source of truth for the work; newer emails in `emails/` win
  over `tasks.md` when they conflict.
- Decisions get logged to `decisions/` with the why.
- Anything needing a developer gets a `[dev]` prefix.
