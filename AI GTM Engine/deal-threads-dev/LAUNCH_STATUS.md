# Deal Threads Launch Status

Last updated: 2026-06-09 13:02 UTC

## Deployed Environment

- Railway project: `ai-contact-form-demo`
- Railway service: `ai-contact-form-demo`
- Environment: `production`
- Public URL: `https://ai-contact-form-demo-production.up.railway.app`
- Latest hardened deployment: `1cf07e32-3789-4f58-be3b-516b26c49dc8`
- Persistent volume: `/data`
- Data store: SQLite at `/data/deal-threads-dev.sqlite`
- Public buyer demo room: `/`
- Public pilot launch hub: `/pilot-launch`
- Public first-five beta invite: `/first-five-beta`
- Public AI-powered lead enrichment webinar: `/webinar`
- Public beta pilot intake: `/pilot-intake`
- Public pilot close kit: `/pilot-close-kit`
- Public pilot agreement/order packet: `/pilot-agreement`
- Public pilot approval room: `/pilot-approval-room`
- Public mutual action plan: `/mutual-action-plan`
- Public buyer confirmation guide: `/buyer-confirmation-guide`
- Public stakeholder forwarding kit: `/stakeholder-forwarding-kit`
- Public pricing and packages: `/pricing`
- Public sales enablement packet: `/sales-enablement`
- Public providerless enrichment strategy: `/providerless-enrichment`
- Public comparison guide: `/compare`
- Public proof preview: `/proof-preview`
- Public business case calculator: `/business-case`
- Public implementation guide: `/implementation-guide`
- Public CRM handoff guide: `/crm-handoff-guide`
- Public procurement packet: `/procurement-packet`
- Tokenized public buyer confirmation form: `/confirm/:token`
- Tokenized public buyer approval brief: `/confirm/:token/brief`
- Tokenized public buyer confirmation status room: `/confirm/:token/status`
- Tokenized public buyer confirmation request kit: `/confirm/:token/request-kit`
- Tokenized public pilot acceptance room: `/confirm/:token/acceptance`
- Public security center: `/security`
- Public sample profile room: `/sample-profile`
- Protected launch operations command room: `/launch/ops`
- Protected first-five beta board: `/launch/first-five-board`
- Protected first beta launch drill: `/launch/first-beta-drill`
- Protected first beta next-action room: `/launch/next-action`
- Protected launch confirmation command center: `/launch/confirmation-command`
- Protected launch confirmation watchroom: `/launch/confirmation-watch`
- Protected first profile capture room: `/launch/first-profile`
- Protected profile handoff bridge: `/launch/profile-handoff`
- Protected rep feedback command room: `/launch/rep-feedback`
- Protected first beta execution packet: `/launch/first-beta-execution`
- Protected first proof packet workbench: `/launch/proof-packet`
- Protected market launch readiness gate: `/launch/market-ready`
- Protected market launch kit: `/launch/market-kit`
- Protected launch proof ledger: `/launch/proof-ledger`
- Protected launch proof owner handoff: `/launch/proof-handoff`
- Protected first-five outreach room: `/outreach`
- Protected activation intake triage: `/activation/intake-triage`
- Protected activation follow-up queue: `/activation/follow-ups`
- Protected activation outbox: `/activation/outbox`
- Protected activation close desk: `/activation/close-desk`
- Protected buyer confirmation workbench: `/activation/prospects/:leadId/confirmation-workbench`
- Protected pilot acceptance API: `/api/v1/activation/prospects/:leadId/pilot-acceptance`
- Protected confirmation reply preview: `/activation/prospects/:leadId/confirmation-reply`
- Protected stakeholder handoff pack: `/activation/prospects/:leadId/stakeholder-handoff`
- Protected install follow-up queue: `/launch/install-queue`
- Protected providerless account preflight: `/enrichment` form and `POST /api/v1/enrichment/preflight`
- Protected providerless enrichment review queue: `/enrichment/review-queue`
- Protected providerless lead evidence packet: `/crm/:leadId/providerless-evidence`
- Launch verifier: `npm run qa:launch`
- Local public buyer-confirmation-guide claim-audit verification passed, but deployment and full launch-verifier proof after this change remain pending. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). The buyer-facing `/buyer-confirmation-guide` packet now exposes `guide_claim_audit` as `deal_threads.public_buyer_confirmation_guide_claim_audit.v1` and `confirmation_prework_checklist` as `deal_threads.public_buyer_confirmation_guide_prework_checklist.v1`, renders `Guide claim audit` and `Confirmation prework checklist` in HTML plus `## Guide Claim Audit` and `## Confirmation Prework Checklist` in Markdown, and keeps `/api/v1/buyer-confirmation-guide` JSON/Markdown exports aligned with the same confirmation-prework-only contract. Public safety now proves the guide is read-only, guide-only, confirmation-prework-only, uses public links only, links to no protected surfaces, exposes no operator forms, CRM profiles, or tenant exports, contains no buyer-specific token, submits no confirmation, captures no signature, collects no payment, charges no buyer, creates no beta client, mutates no buyer state, sends no email, transmits no external data, runs no paid lookup, claims no live results, market-ready status, customer ROI, or guaranteed lift, and requires the tokenized confirmation form, buyer confirmation, pilot acceptance/order approval, install proof, first beta profile, CRM handoff proof, rep feedback, and protected market-gate clearance before stronger confirmation or outcome claims. A temporary production-mode server was started on `4174`, but `npm run qa:launch` against `4174` and the fallback run against `4173` both failed all checks with `fetch failed` before reaching the app; `lsof` showed the temporary listener but `curl` could not connect, so this is recorded as a local loopback verifier blocker rather than an app-contract regression. The temporary `4174` server was stopped, and no listener remains on `4173` or `4174`. Railway deploy was not performed because `railway status` still returns `invalid_grant`/unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public mutual-action-plan claim-audit verification passed and local launch-verifier proof is green; deployment remains pending Railway re-auth. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), `npm test` (`15/15`), and SQLite-backed `npm run qa:launch` (`38/38`, hardening `100/100`). The buyer-facing `/mutual-action-plan` packet now exposes `map_claim_audit` as `deal_threads.public_mutual_action_plan_claim_audit.v1` and `execution_checklist` as `deal_threads.public_mutual_action_plan_execution_checklist.v1`, renders `MAP claim audit` and `Execution checklist` in HTML plus `## MAP Claim Audit` and `## Execution Checklist` in Markdown, and keeps `/api/v1/mutual-action-plan` JSON/Markdown exports aligned with the same pilot-timeline-planning-only contract. Public safety now proves the plan is read-only, timeline-planning-only, pilot-planning-only, uses public links only, links to no protected surfaces, exposes no operator forms, CRM profiles, or tenant exports, captures no signature, collects no payment, charges no buyer, creates no beta client, mutates no buyer state, sends no email, transmits no external data, runs no paid lookup, claims no completed milestones, live results, market-ready status, customer ROI, or guaranteed lift, and requires buyer confirmation, tokenized pilot acceptance or equivalent buyer-approved order, client-domain install proof, first beta profile, CRM handoff proof, rep feedback, proof review, and protected market-gate clearance before stronger timeline or outcome claims. The stale PID `11981` listener was terminated, the production-mode local server was restarted for verification, `npm run qa:launch` passed, temporary PID `19513` was stopped, and no listener remains on `4173`. Railway deploy was not performed because `railway status` still returns `invalid_grant`/unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public pilot-approval-room claim-audit verification passed, but deployment and final launch-verifier proof remain pending. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). The buyer-facing `/pilot-approval-room` packet now exposes `approval_claim_audit` as `deal_threads.public_pilot_approval_room_claim_audit.v1` and `approval_routing_checklist` as `deal_threads.public_pilot_approval_room_routing_checklist.v1`, renders `Approval claim audit` and `Approval routing checklist` in HTML plus `## Approval Claim Audit` and `## Approval Routing Checklist` in Markdown, and keeps `/api/v1/pilot-approval-room` JSON/Markdown exports aligned with the same buying-committee-routing-only contract. Public safety now proves the room is read-only, stakeholder-routing-only, pilot-planning-only, uses public links only, links to no protected surfaces, exposes no operator forms, CRM profiles, or tenant exports, captures no signature, collects no payment, charges no buyer, creates no beta client, mutates no buyer state, sends no email, transmits no external data, runs no paid lookup, claims no live results, market-ready status, customer ROI, or guaranteed lift, and requires stakeholder-owner alignment, buyer confirmation, tokenized pilot acceptance or equivalent buyer-approved order, pilot proof, and protected market-gate clearance before stronger approval or outcome claims. SQLite-backed `npm run qa:launch` was not rerun because PID `11981` still appears to listen on `4173` while `curl` cannot connect to `127.0.0.1:4173`, and prior new listener attempts in this sandbox returned `EPERM`. Railway deploy was not performed because `railway status` still returns `invalid_grant`/unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public pilot-agreement claim-audit verification passed, but deployment and final launch-verifier proof remain pending. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). The buyer-facing `/pilot-agreement` packet now exposes `agreement_claim_audit` as `deal_threads.public_pilot_agreement_claim_audit.v1` and `commercial_review_checklist` as `deal_threads.public_pilot_agreement_commercial_review_checklist.v1`, renders `Agreement claim audit` and `Commercial review checklist` in HTML plus `## Agreement Claim Audit` and `## Commercial Review Checklist` in Markdown, and keeps `/api/v1/pilot-agreement` JSON/Markdown exports aligned with the same commercial-review-only contract. Public safety now proves the packet is read-only, uses public links only, links to no protected surfaces, exposes no operator forms, CRM profiles, or tenant exports, captures no signature, collects no payment, charges no buyer, creates no beta client, mutates no buyer state, sends no email, transmits no external data, runs no paid lookup, claims no live results, market-ready status, customer ROI, or guaranteed lift, and requires buyer owner confirmation, tokenized pilot acceptance or equivalent buyer-approved order, pilot proof, and protected market-gate clearance before stronger commercial or outcome claims. SQLite-backed `npm run qa:launch` was not rerun because PID `11981` still appears to listen on `4173` while `curl` cannot connect to `127.0.0.1:4173`, and prior new listener attempts in this sandbox returned `EPERM`. Railway deploy was not performed because `railway status` still returns `invalid_grant`/unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public first-five beta claim-audit verification passed, but deployment and final launch-verifier proof remain pending. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). The buyer-facing `/first-five-beta` packet now exposes `first_five_claim_audit` as `deal_threads.public_first_five_beta_claim_audit.v1` and `cohort_forwarding_checklist` as `deal_threads.public_first_five_beta_forwarding_checklist.v1`, renders `First-five claim audit` and `Cohort forwarding checklist` in HTML plus `## First-Five Claim Audit` and `## Cohort Forwarding Checklist` in Markdown, and keeps `/api/v1/first-five-beta` JSON/Markdown exports aligned with the same contract. Public safety now proves the invite is beta-invite-only, pilot-planning-only, read-only, uses public links only, links to no protected surfaces, reveals no target accounts, mutates no buyer state, creates no beta client, sends no email, transmits no external data, runs no paid lookup, claims no live results, market-ready status, customer ROI, or guaranteed lift, and requires buyer fit confirmation, buyer confirmation, signed pilot terms, client-domain install proof, first beta profile, CRM handoff proof, rep feedback, and protected market-gate clearance before stronger proof or outcome claims. SQLite-backed `npm run qa:launch` was not rerun because PID `11981` still appears to listen on `4173` while `curl` cannot connect to `127.0.0.1:4173`, and prior new listener attempts in this sandbox returned `EPERM`. Railway deploy was not performed because `railway status` still returns `invalid_grant`/unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public webinar claim-audit verification passed, but deployment and final launch-verifier proof remain pending. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). The buyer-facing `/webinar` packet now exposes `webinar_claim_audit` as `deal_threads.public_webinar_claim_audit.v1` and `webinar_follow_up_checklist` as `deal_threads.public_webinar_follow_up_checklist.v1`, renders `Webinar claim audit` and `Webinar follow-up checklist` in HTML plus `## Webinar Claim Audit` and `## Webinar Follow-Up Checklist` in Markdown, and keeps `/api/v1/webinar` JSON/Markdown exports aligned with the same contract. Public safety now proves the webinar is buyer-education-only, draft-follow-up-only, read-only, uses public links only, links to no protected surfaces, mutates no buyer state, creates no beta client, sends no email, transmits no external data, runs no paid lookup, claims no live results, market-ready status, customer ROI, or guaranteed lift, and requires buyer context, buyer confirmation, signed pilot terms, client-domain install proof, first beta profile, rep feedback, and protected market-gate clearance before stronger proof or outcome claims. SQLite-backed `npm run qa:launch` was not rerun because PID `11981` still appears to listen on `4173` while `curl` cannot connect to `127.0.0.1:4173`, and prior new listener attempts in this sandbox returned `EPERM`. Railway deploy was not performed because `railway status` still returns `invalid_grant`/unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public pilot-launch claim-audit verification passed, but deployment and final launch-verifier proof remain pending. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). The buyer-facing `/pilot-launch` packet now exposes `pilot_launch_claim_audit` as `deal_threads.public_pilot_launch_claim_audit.v1` and `launch_readiness_checklist` as `deal_threads.public_pilot_launch_readiness_checklist.v1`, renders `Pilot launch claim audit` and `Launch readiness checklist` in HTML plus `## Pilot Launch Claim Audit` and `## Launch Readiness Checklist` in Markdown, and keeps `/api/v1/pilot-launch` JSON/Markdown exports aligned with the same contract. Public safety now proves the hub is buyer-safe launch routing only, pilot-planning-only, read-only, uses public links only, links to no protected surfaces, mutates no buyer state, sends no email, transmits no external data, runs no paid lookup, claims no live results, market-ready status, customer ROI, or guaranteed lift, and requires buyer confirmation, signed pilot terms, client-domain install proof, first beta profile, rep feedback, and protected market-gate clearance before stronger proof or launch claims. SQLite-backed `npm run qa:launch` was not rerun because PID `11981` still appears to listen on `4173` while `curl` cannot connect to `127.0.0.1:4173`, and prior new listener attempts in this sandbox returned `EPERM`. Railway deploy was not performed because `railway status` still returns `invalid_grant`/unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public pilot-close-kit claim-audit verification passed, but deployment and final launch-verifier proof remain pending. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). The buyer-facing `/pilot-close-kit` packet now exposes `close_kit_claim_audit` as `deal_threads.public_pilot_close_kit_claim_audit.v1` and `buyer_forwarding_checklist` as `deal_threads.public_pilot_close_kit_forwarding_checklist.v1`, renders `Close kit claim audit` and `Buyer forwarding checklist` in HTML plus `## Close Kit Claim Audit` and `## Buyer Forwarding Checklist` in Markdown, and adds `/api/v1/pilot-close-kit` JSON/Markdown exports. Public safety now proves the close kit is pilot-planning-only, read-only, uses public links only, links to no protected surfaces, mutates no buyer state, sends no email, transmits no external data, runs no paid lookup, claims no live results, market-ready status, customer ROI, or guaranteed lift, and requires buyer confirmation, signed pilot terms, client-domain install proof, first beta profile, CRM handoff proof, rep feedback, and protected market-gate clearance before stronger proof claims. SQLite-backed `npm run qa:launch` was not rerun because PID `11981` still appears to listen on `4173` while `curl` cannot connect to `127.0.0.1:4173`, and prior new listener attempts in this sandbox returned `EPERM`. Railway deploy was not performed because `railway status` still returns `invalid_grant`/unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public pricing claim-audit verification passed, but deployment and final launch-verifier proof remain pending. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). The buyer-facing `/pricing` packet now exposes `pricing_claim_audit` as `deal_threads.public_pricing_claim_audit.v1` and `commercial_terms_checklist` as `deal_threads.public_pricing_commercial_terms_checklist.v1`, renders `Pricing claim audit` and `Commercial terms checklist` in HTML plus `## Pricing Claim Audit` and `## Commercial Terms Checklist` in Markdown, and explicitly keeps the page scoped to budgetary offer-ladder review. Public safety now proves the page is not a binding order form, collects no payment, charges no buyer, runs no paid lookup, claims no guaranteed payback, guaranteed ROI, live results, or market-ready status, and requires signed pilot agreement, buyer scope confirmation, explicit paid-enrichment approval, pilot proof, and protected market-gate clearance before stronger commercial or outcome claims. SQLite-backed `npm run qa:launch` was not rerun because the old local listener remains visible on `4173` but unreachable, direct process cleanup is denied, and new loopback listeners return `EPERM`; this is the same environment-only verifier blocker recorded above. Railway deploy was not performed because `railway status` still returns unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public sales-enablement copy-audit hardening is implemented and partially verified; deployment and final launch-verifier proof remain pending. Checks passed before the sandbox refresh: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), and `npm test` (`15/15`). Syntax was rechecked after the sandbox refresh and still passes for `server.js`, `scripts/launch-readiness.mjs`, and `test/regression.test.js`. The buyer-facing `/sales-enablement` packet now exposes `sales_copy_audit` as `deal_threads.public_sales_enablement_copy_audit.v1` and `forwarding_checklist` as `deal_threads.public_sales_enablement_forwarding_checklist.v1`, renders `Sales copy audit` and `Forwarding checklist` in HTML plus `## Sales Copy Audit` and `## Forwarding Checklist` in Markdown, and explicitly keeps the packet scoped to manually reviewed draft outreach. Public safety now proves the page sends no email, automates no outreach, is draft-copy-only, uses public links only, links to no protected surfaces, claims no guaranteed lift, customer ROI, live results, or market-ready status, and requires human review, buyer fit, pilot proof, and protected market-gate clearance before stronger result claims. The interrupted SQLite-backed `npm run qa:launch` could not be completed after the sandbox refresh because the old local listener remained visible on `4173` but unreachable, direct process cleanup was denied, and new loopback listeners return `EPERM`; the verifier failure was all `fetch failed` and does not prove an app-contract regression. Railway deploy was not performed because `railway status` still returns unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public comparison claim-audit verification passed, but deployment remains pending Railway re-auth. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), `npm test` (`15/15`), and SQLite-backed `npm run qa:launch` (`38/38`, hardening `100/100`). The buyer-facing `/compare` packet now exposes `comparison_claim_audit` as `deal_threads.public_comparison_claim_audit.v1` and `decision_fit_matrix`, renders `Comparison claim audit` and `Decision fit matrix` in HTML plus `## Comparison Claim Audit` and `## Decision Fit Matrix` in Markdown, and explicitly keeps the guide scoped to category positioning only. Public safety now proves the guide is read-only, category-level only, names no specific competitors, claims no guaranteed lift, market-ready status, live results, or customer ROI, links to no protected surfaces, and requires buyer context, pilot proof, and protected market-gate clearance before stronger outcome or live-proof claims. Regression and launch-verifier coverage assert the HTML, JSON, and Markdown contract; README now documents the public comparison claim-audit and decision-fit behavior. Railway deploy was not performed because `railway status` still returns unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public business-case ROI claim-audit verification passed, but deployment remains pending Railway re-auth. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), `npm test` (`15/15`), and SQLite-backed `npm run qa:launch` (`38/38`, hardening `100/100`). The buyer-facing `/business-case` packet now exposes `roi_claim_audit` as `deal_threads.public_business_case_roi_claim_audit.v1` and `model_sensitivity` as `deal_threads.public_business_case_sensitivity.v1`, renders `ROI claim audit` and `Model sensitivity` in HTML plus `## ROI Claim Audit` and `## Model Sensitivity` in Markdown, and explicitly blocks presenting modeled payback as guaranteed ROI, observed customer savings or revenue, conversion-rate lift, revenue lift, sales-cycle improvement, market-ready proof, or live customer outcomes from calculator assumptions alone. Public safety now proves the calculator is model-only, claims no guaranteed ROI, customer ROI, live results, market-ready status, or conversion lift, links to no protected surfaces, and requires buyer pilot proof plus protected market-gate clearance before customer-specific ROI or market claims. Regression and launch-verifier coverage assert the HTML, JSON, and Markdown contract; README now documents the public ROI claim-audit and sensitivity behavior. Railway deploy was not performed because `railway status` still returns unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local public proof-preview claim-audit verification passed, but deployment remains pending Railway re-auth. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js` (`7/7`), `npm test` (`15/15`), and SQLite-backed `npm run qa:launch` (`38/38`, hardening `100/100`). The buyer-facing `/proof-preview` packet now exposes `claim_audit` as `deal_threads.public_proof_preview_claim_audit.v1`, locks claim scope to `synthetic_template_only`, counts sample metrics, renders `Proof claim audit` in HTML and `## Public Claim Audit` in Markdown, and explicitly blocks presenting sample metrics as observed customer results, market-ready proof, guaranteed conversion lift, customer ROI, or paid-enrichment absolutes. Public safety now proves the preview uses synthetic data only, claims no live results, claims no market-ready status, claims no customer ROI or conversion lift, links to no protected surfaces, and requires protected market-gate clearance before live-proof language. Regression and launch-verifier coverage assert the HTML, JSON, and Markdown contract. README now documents the public claim-audit behavior. Railway deploy was not performed because `railway status` still returns unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local market-kit claim-scope review verification passed, but deployment remains pending Railway re-auth. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js`, `npm test` (`15/15`), and SQLite-backed `npm run qa:launch` (`38/38`, hardening `100/100`). The protected market launch kit now exposes `claim_scope_review` as `deal_threads.market_launch_kit_claim_scope_review.v1`, derives claim scope from the live-proof clearance preflight when active and otherwise from the inferred market-kit posture, mirrors the selected scope into `launch_posture.claim_scope`, and renders claim scope/source, live-proof clearance status, required evidence, disclosures, blocked external claims, and GET safety flags in JSON, HTML, and Markdown. The guardrail keeps market claims locked to `internal_beta_only`, `launch_with_disclosures`, or `market_claims_allowed`, requires zero live-proof blockers before market claims, and proves GET does not claim market-ready or live proof. Regression and launch-verifier coverage now assert the claim-scope contract for both the initial internal-beta state and the post-proof-packet-sent live-proof-clearance state. Railway deploy was not performed because `railway status` still returns unauthorized and requires `railway login` or `RAILWAY_TOKEN`; no production POSTs were run.
- Local market-ready live-proof-clearance verification passed, but deployment remains pending Railway re-auth. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js`, `npm test`, and SQLite-backed `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=DealThreadsLocalStrong!2026 LAUNCH_EXPECT_DATA_STORE=sqlite WIDGET_ID=wid_replede_demo npm run qa:launch` (`38/38`, hardening `100/100`). The market gate now exposes `launch_clearance_plan.current_action.live_proof_clearance_preflight` as `deal_threads.market_live_proof_clearance_preflight.v1` after the first proof packet is marked sent, instead of falling through to an unrelated buyer outbox action. The action uses primary GET `/api/v1/proof/live-gate?betaClientId=:id`, packet `live_proof_clearance`, proof focus `live_proof_gate`, claim scope `internal_beta_only`, `launch_with_disclosures`, or `market_claims_allowed`, required evidence for `first_proof_packet_sent`, `live_proof_gate_recheck`, `zero_live_proof_blockers`, and `market_kit_claim_scope`, plus disclosure review when warnings exist. Safety flags prove GET does not mutate buyer state, send email, create beta clients or profiles, queue reports, mark reports sent, transmit CRM data, update rep feedback, run paid lookup, claim market-ready, or claim live proof; there is no operator POST on this clearance action. Regression coverage verifies the post-proof-packet-sent state surfaces the live-proof clearance action, keeps the first proof packet preflight attached as `first_proof_packet_sent`, renders protected HTML sections for live-proof clearance/evidence/claim scope/links, and renders Markdown sections `#### Live Proof Clearance Preflight`, `#### Live Proof Clearance Evidence`, `#### Live Proof Claim Scope`, `#### Live Proof Clearance Sequence`, and `#### Live Proof Clearance Links`. A JSON-backed local `qa:launch` run was not used as the final signal because JSON persistence correctly reports hardening warnings and that fixture was advanced to `collect_rep_feedback`; the SQLite-backed run is the final local verifier. Railway deploy was not performed because `railway status` still returns unauthorized and requires `railway login` or `RAILWAY_TOKEN`.
- Local market-ready rep-feedback and first-proof-packet promotion verification passed, but deployment is pending Railway re-auth. Checks passed: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js`, and `npm test`. The market gate now promotes CRM-handoff-complete real beta profiles with no reviewed rep feedback to `launch_clearance_plan.current_action.rep_feedback_preflight` as `deal_threads.market_rep_feedback_preflight.v1`, current action `collect_rep_feedback`, packet `rep_feedback`, proof focus `rep_feedback`, primary GET `/launch/rep-feedback`, protected feedback POST/API previews at `/crm/:leadId/rep-feedback` and `/api/v1/leads/:leadId/rep-feedback`, mailto ask copy, evidence, and read-only safety. The market gate also promotes proof-packet-due clients to `proof_packet_preflight` / `first_proof_packet_preflight` as `deal_threads.market_first_proof_packet_preflight.v1`, current action `queue_first_proof_packet` when evidence is ready, packet `proof_packet`, proof focus `live_proof_gate`, primary GET `/launch/proof-packet`, explicit queue and manual-sent POST previews, evidence, and read-only safety. Stage selection now treats zero rep reviews as rep-feedback-needed and `proof_packets_due` as `live_proof_clearance`, preventing fallback to buyer outbox or premature market-ready/live-proof claims during those proof handoffs. Local `npm run qa:launch` against an empty temp state was not used as the final signal because the verifier requires seeded protected activation/lead state and failed only the state-fixture checks (`3/38`). Railway deploy was not performed because the CLI OAuth token is expired (`invalid_grant`) and no `RAILWAY_TOKEN` is available. Production GET-only smoke confirmed public health returns `200` with `status=ok`, hardening `100`, and SQLite at `/data/deal-threads-dev.sqlite`; protected market-ready with local default credentials returned `401`, so no protected production state was inspected and no production POSTs were run.
- Local and production market-ready CRM-handoff-preflight verification passed for deployment `1cf07e32-3789-4f58-be3b-516b26c49dc8`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js`, and `npm test` passed. The market gate now exposes `launch_clearance_plan.current_action.crm_handoff_preflight` as `deal_threads.market_crm_handoff_preflight.v1` when the active proof stage is `handoff_feedback` and the selected beta profile does not yet have sent CRM handoff proof. The action uses the profile handoff bridge as the read-only primary GET surface, previews the explicit `POST /crm/:leadId/crm-delivery/send` and `POST /api/v1/leads/:leadId/crm-delivery/send` actions, distinguishes queued CRM delivery records from sent handoff proof, reports whether the operator POST may transmit external CRM data, and carries required evidence for `real_beta_client`, `first_beta_profile`, `rep_ready_brief`, `explicit_crm_delivery_post`, and `crm_handoff_sent`, with `rep_feedback_after_handoff` marked downstream/recommended. Regression coverage verifies the post-first-profile state moves to `handoff_feedback`, current action `send_crm_handoff`, packet `crm_handoff`, proof focus `crm_handoff`, beta client id, lead id, profile-handoff URL, CRM delivery HTML and API POST URLs, `crm_delivery_queued` preflight status, ready-to-send true, `1` beta profile, `1` CRM delivery record, handoff sent `false`, queued deliveries `1`, external CRM on POST `false`, paid lookups `0`, required evidence contract, POST preview-only-on-GET guardrail, no CRM queue or transmission on GET, and protected HTML/Markdown rendering of `CRM handoff preflight`, `CRM handoff evidence`, `CRM handoff POST preview`, `CRM handoff payload preview`, `#### CRM Handoff Preflight`, `Evidence explicit_crm_delivery_post: required`, `Evidence crm_handoff_sent: required`, `Evidence rep_feedback_after_handoff: recommended`, `Transmits external CRM on POST: no`, and `Preview only on GET: yes`. Local targeted GET smoke on `127.0.0.1:4173` confirmed `/api/v1/health` returns `status=ok`, the persisted local market gate remains `proof_ready`, market ready `false`, current stage `buyer_confirmation_chase`, current action `send_confirmation_nudge`, packet `confirmation_nudge`, `0` paid lookups, and protected HTML/Markdown render the new CRM handoff sections safely while `crm_handoff_preflight` is not active in JSON. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, hardening `100`, data store mode `sqlite` at `/data/deal-threads-dev.sqlite`, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, current stage `buyer_confirmation_chase`, current action `send_confirmation_nudge`, packet `confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, and paid lookups `0`; because production has not reached `handoff_feedback`, `crm_handoff_preflight` is not active in JSON yet, but production HTML/Markdown now render `CRM handoff preflight`, `CRM handoff evidence`, `CRM handoff POST preview`, and `#### CRM Handoff Preflight` safely on GET. No production mutating POSTs were run.
- Local and production market-ready first-profile-capture-preflight verification passed for deployment `42d64701-41af-49be-869f-e17036229116`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js`, and `npm test` passed. The market gate now exposes `launch_clearance_plan.current_action.first_profile_capture_preflight` as `deal_threads.market_first_profile_capture_preflight.v1` whenever the active stage is `first_profile_capture`; the action uses the first-profile capture room as the read-only primary GET surface, previews the explicit `POST /launch/:betaClientId/test-lead`, requires operator review of `contactName`, `email`, `companyName`, `crm`, `pageUrl`, and `message`, and carries required evidence for `real_beta_client`, `client_domain_config_load`, `first_profile_payload`, `explicit_profile_post`, and `first_beta_profile`, with `crm_handoff_after_profile` and `rep_feedback_after_profile` marked downstream/recommended. Regression coverage verifies the post-client-domain-load state moves to `first_profile_capture`, current action `create_first_profile`, packet `first_profile`, proof focus `first_real_profile`, beta client id, first-profile room URL, operator POST URL, `first_profile_needed` preflight status, ready-to-capture true, `1` client-domain load, `0` profiles, `0` missing payload fields, paid lookups `0`, POST preview creates profile only by explicit POST, no profile creation on GET, first-profile evidence contract, blocker-clearance-map focus, and protected HTML/Markdown rendering of `First profile capture preflight`, `First profile required fields`, `First profile evidence`, `First profile POST preview`, `First profile payload preview`, `#### First Profile Capture Preflight`, `Required first-profile field: message`, `Evidence first_beta_profile: required`, `Evidence crm_handoff_after_profile: recommended`, `Creates profile: yes`, and `Preview only on GET: yes`. Local targeted GET smoke on `127.0.0.1:4173` confirmed `/api/v1/health` returns `status=ok`, hardening `100`, and the persisted local market gate renders the new first-profile sections safely while its current action remains `send_confirmation_nudge` and the active first-profile preflight is not in JSON. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, hardening `100`, data store mode `sqlite` at `/data/deal-threads-dev.sqlite`, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, current stage `buyer_confirmation_chase`, current action `send_confirmation_nudge`, packet `confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, and paid lookups `0`; because production has not reached `first_profile_capture`, `first_profile_capture_preflight` is not active in JSON yet, but production HTML/Markdown now render `First profile capture preflight`, `First profile evidence`, `First profile POST preview`, and `#### First Profile Capture Preflight` safely on GET. No production mutating POSTs were run.
- Local and production market-ready client-domain-install-preflight verification passed for deployment `8a728c39-1cfb-4af5-8f7f-ae8bba1c06f3`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js`, and `npm test` passed. The market gate now exposes `launch_clearance_plan.current_action.client_domain_install_preflight` as `deal_threads.market_client_domain_install_preflight.v1` whenever the active stage is `client_domain_install`; the action uses the install proof workbench as the read-only primary GET surface, keeps source checks as explicit POST-only operator actions when applicable, and carries required evidence for `launch_packet_delivery`, `tokenized_install_handoff`, `tested_page_url`, `client_domain_config_load`, and `install_activity_last_page_url`, with `source_check_result` and `first_beta_profile_after_install` marked recommended/downstream rather than live-install proof. Regression coverage verifies the post-kickoff state moves to `client_domain_install`, current action key `client_domain_install`, packet `install_proof`, beta client id, workbench URL, `install_handoff_needed` preflight status, `0` client-domain loads, `0` hosted loads, launch packet `sent`, paid lookups `0`, the client-domain load blocker, the hosted-load-does-not-count guardrail, source-check POST surface, install evidence contract, blocker-clearance-map focus, and protected HTML/Markdown rendering of `Client-domain install preflight`, `Client-domain install evidence`, `Client-domain install sequence`, `Client-domain install links`, `#### Client-Domain Install Preflight`, `Evidence client_domain_config_load: required`, and `Evidence source_check_result: recommended`. Local targeted GET smoke on `127.0.0.1:4173` confirmed `/api/v1/health` returns `status=ok`, hardening `100`, and the persisted local market gate renders the new client-domain install sections safely while its current action remains `send_confirmation_nudge` and the active install preflight is not in JSON. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, hardening `100`, data store mode `sqlite` at `/data/deal-threads-dev.sqlite`, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, current stage `buyer_confirmation_chase`, current action `send_confirmation_nudge`, packet `confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, and paid lookups `0`; because production has not reached `client_domain_install`, `client_domain_install_preflight` is not active in JSON yet, but production HTML/Markdown now render `Client-domain install preflight`, `Client-domain install evidence`, and `#### Client-Domain Install Preflight` safely on GET. No production mutating POSTs were run.
- Local and production market-ready kickoff-install-handoff-preflight verification passed for deployment `83008786-c99b-4137-9274-cc7f9512ea32`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js`, and `npm test` passed. The market gate now exposes `launch_clearance_plan.current_action.kickoff_install_handoff_preflight` as `deal_threads.market_kickoff_install_handoff_preflight.v1` whenever the active stage is `kickoff_real_beta`; it reuses the lead-level kickoff preflight, lists required kickoff POST fields including `launchRecipients`, requires evidence for `beta_client_id`, `launch_packet_delivery`, `tokenized_install_handoff`, `install_snippet_sent_checklist`, and `proof_transition_receipt`, expects POST result keys including `proof_transition`, and records safety flags showing GET stays read-only while the explicit kickoff POST may create the beta client and send or dry-run the install handoff without claiming market-ready or live proof. Regression coverage verifies the buyer-confirmed paused state reports `ready_to_kickoff`, `0` blockers, saved reply evidence receipt, `0` paid lookups, the active kickoff POST URL, the evidence contract, and HTML/Markdown rendering of `Kickoff install handoff preflight`, `Kickoff required fields`, `Kickoff evidence required`, `Kickoff POST result keys`, `#### Kickoff Install Handoff Preflight`, `Required kickoff field: launchRecipients`, and `Evidence launch_packet_delivery: required`. Local targeted smoke on `127.0.0.1:4173` confirmed the current local market gate remains in buyer-confirmation chase while protected JSON/HTML/Markdown still render the new kickoff sections safely; local `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=DealThreadsLocalStrong!2026 WIDGET_ID=wid_replede_demo npm run qa:launch` was not used as the final signal because the local environment still reports hardening warnings and the persisted JSON fixture is advanced to `collect_rep_feedback` while the state-sensitive first-beta handoff check expects `send_confirmation_nudge` (`4/38` failed checks). Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, hardening `100/100`, SQLite live, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, current stage `buyer_confirmation_chase`, current action `send_confirmation_nudge`, packet `confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, and `0` paid lookups on the active reply apply preflight. Because production has not reached `kickoff_real_beta`, `kickoff_install_handoff_preflight` is not active in JSON yet; production HTML/Markdown now render `Kickoff install handoff preflight`, `Kickoff evidence required`, `#### Kickoff Install Handoff Preflight`, and `Kickoff POST creates beta client` safely on GET. No production mutating POSTs were run.
- Local and production market-ready buyer-reply-apply-preflight verification passed for deployment `80f42278-d522-4737-9ab7-bf7462084623`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --check test/regression.test.js`, `node --test test/regression.test.js`, and `npm test` passed. The confirmation reply preview now emits `reply_evidence_receipt` as `deal_threads.confirmation_reply_evidence_receipt.v1`, includes `replyEvidenceReceiptJson` in the apply POST contract, and persists the receipt inside `close_workflow.confirmation_review.reply_evidence_receipt`; the kickoff preflight now checks `buyer_confirmation_review_receipt` before a reply-preview-applied confirmation can move to install kickoff. Local targeted smoke on `127.0.0.1:4173` confirmed protected `/api/v1/launch/market-ready.launch_clearance_plan.current_action.reply_apply_preflight` is `deal_threads.market_buyer_reply_apply_preflight.v1`, status `waiting_for_buyer_reply`, requires `replyEvidenceReceiptJson`, applies only by POST, recommends `0` paid lookups, and protected HTML/Markdown render `Buyer reply apply preflight`, `Reply apply required fields`, `#### Buyer Reply Apply Preflight`, and `Required apply field: replyEvidenceReceiptJson`; the same local smoke confirmed a protected reply-preview POST returns the receipt without mutating buyer state. Local `QA_BASE_URL=http://127.0.0.1:4173 ADMIN_USERNAME=admin ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` was not used as the final signal because the local admin secret reports hardening warnings and the persisted local JSON state is still advanced to `collect_rep_feedback` while the first-beta launch handoff check expects `send_confirmation_nudge`; the state-sensitive verifier failed those local fixture checks at `4/38` failed checks. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, hardening `100/100`, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, current stage `buyer_confirmation_chase`, current action `send_confirmation_nudge`, packet `confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, and `0` real beta profiles; production `reply_apply_preflight` is `waiting_for_buyer_reply`, has receipt saved `false`, includes `replyEvidenceReceiptJson`, applies only by POST, recommends `0` paid lookups, and production HTML/Markdown render the buyer-reply apply preflight plus the existing manual send evidence recorder. No production mutating POSTs were run.
- Local and production market-ready manual-mailbox-evidence verification passed for deployment `63797ad3-c9a3-48b6-8897-9ce28c2d4c89`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed. Local `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` was not used as the final signal because the persisted local JSON state is still advanced to `collect_rep_feedback` while the first-beta launch handoff check expects `send_confirmation_nudge`; the state-sensitive verifier correctly failed that mismatched fixture at `1/38` failed checks. Local targeted smoke confirmed the market gate recorder now exposes required `mailboxSentAt`, required `senderMailbox`, required `recipients`, required `subjectSnapshot`, optional `messageId`, and required `note` fields, with HTML rendering `Mailbox sent timestamp`, `Sender mailbox or sending system`, `Subject snapshot`, and `Mailbox message-id`; regression coverage also proves the manual confirmation-nudge sent POST persists `manual_evidence` with mailbox timestamp, sender mailbox, recipient snapshot, subject snapshot, message-id, and evidence note without sending from the server. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, SQLite live, and hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, and `0` paid lookups needed now. The production `manual_send_evidence_recorder` remains `deal_threads.market_manual_send_evidence_recorder.v1`, status `ready_after_mailbox_send`, method `POST`, `returnTo=/launch/market-ready`, action `/activation/prospects/lead_a6f44a24/mark-confirmation-nudge-sent`, API `/api/v1/activation/prospects/lead_a6f44a24/confirmation-nudge/sent`, no server send on POST, actual-mailbox-send-first guardrail true, and HTML/Markdown render the timestamp, sender, subject, message-id, and no-server-send proof. Activation outbox remains `confirmation_nudge_ready` with `1` manual mailto draft, `1` manual sent recorder, external email disabled, dry-run mode enabled, and paid lookups `0`. No production POSTs were run.
- Local and production market-ready manual-send-evidence-recorder verification passed for deployment `679178c9-013d-46c1-92d0-844cb024eb3f`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed. Local `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` was not used as the final signal because the persisted local JSON state is still advanced to `collect_rep_feedback` while the first-beta launch handoff check expects `send_confirmation_nudge`; the state-sensitive verifier correctly failed that mismatched fixture at `1/38` failed checks. Targeted local GET smoke confirmed protected `/api/v1/launch/market-ready.launch_clearance_plan.current_action.manual_send_evidence_recorder` is `deal_threads.market_manual_send_evidence_recorder.v1`, status `ready_after_mailbox_send`, ready to record after mailbox send `true`, method `POST`, `returnTo=/launch/market-ready`, fields `returnTo`, `recipients`, and `note`, sends from server `false`, requires actual mailbox send first `true`, and HTML/Markdown render `Manual send evidence recorder`, `Record manual nudge sent`, `Mailbox send evidence note`, and `#### Manual Send Evidence Recorder`. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, SQLite live, and hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, `current_action_status=nudge_due`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, and `0` paid lookups needed now. The production manual send evidence recorder is `ready_after_mailbox_send` with `POST /activation/prospects/lead_a6f44a24/mark-confirmation-nudge-sent`, API URL `/api/v1/activation/prospects/lead_a6f44a24/confirmation-nudge/sent`, submit label `Record manual nudge sent`, required evidence fields `mailbox_sent_timestamp`, `sender_mailbox`, `recipient_snapshot`, `subject_snapshot`, and `send_note`, and safety flags confirming read-only `GET`, no email send on GET, no buyer-state mutation on GET, no beta-client creation, no CRM transmission, no paid enrichment, no live-proof claim, no server send on the recorder POST, and no sent record without an actual mailbox send. Production HTML and Markdown render the recorder and mailbox-first guardrails; activation outbox remains `confirmation_nudge_ready` with `1` manual mailto draft, `1` manual sent recorder, external email disabled, dry-run mode enabled, and paid lookups `0`; confirmation watch remains `chase_due`. No production POSTs were run.
- Local and production market-ready buyer-completion-payload verification passed for deployment `5a29ca7a-7abb-4161-94ef-c576ba2f401f`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed. Local `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` was not used as the final signal because the persisted local JSON state is already advanced to `collect_rep_feedback` while the first-beta launch handoff check expects `send_confirmation_nudge`; the state-sensitive verifier correctly failed that mismatched fixture at `1/38` failed checks. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, SQLite live, and hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, and `0` paid lookups needed now. The new `launch_clearance_plan.current_action.buyer_completion_payload` is `deal_threads.market_buyer_confirmation_completion_payload.v1` with status `missing_details`, `3` missing required details, `2` saved required details, partial-save available, a copy-ready buyer completion reply, buyer-safe public links, and safety flags confirming read-only `GET`, no email send, no buyer-state mutation, no beta-client creation, no CRM transmission, no paid lookup, and no live-proof claim. Public `/confirm/:token/status` and `/confirm/:token/request-kit` expose `completion_payload` as `deal_threads.public_buyer_confirmation_completion_payload.v1`; both report status `missing_details`, `3` missing required details, `2` saved required details, partial-save available, and paid lookups `0`, while the request kit also surfaces `2` stakeholder asks. Protected and public HTML/Markdown render the new buyer completion payload sections and copy-ready completion reply. Copy safety remains safe to forward with protected link leaks `0` and premature claim flags `0`. No production POSTs were run.
- Local and production market-ready dispatch-commitment-checklist verification passed for deployment `f3e4a329-5d4b-44a9-9c80-b4befef0040c`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed. Local `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` was not used as the final signal because the persisted local JSON state is already advanced to `collect_rep_feedback` while the first-beta launch handoff check expects `send_confirmation_nudge`; the state-sensitive verifier correctly failed that mismatched fixture at `1/38` failed checks. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, SQLite live, and hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, `current_action_status=nudge_due`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, `3` demo clients excluded, and `0` paid lookups needed now. The new `launch_clearance_plan.current_action.dispatch_commitment_checklist` is `deal_threads.market_dispatch_commitment_checklist.v1` with status `ready_to_send_then_record`, `ready_to_send=true`, `ready_to_record_after_send=true`, `actual_send_recorded=false`, manual sent recorder ready, `3` missing required details, `2` stakeholder asks needed, `0` paid lookups, `7` checks, evidence fields `mailbox_sent_timestamp`, `sender_mailbox`, `recipient_snapshot`, `subject_snapshot`, and `send_note`, plus the guardrail `no_sent_record_without_mailbox_send=true`. Protected HTML renders `Dispatch commitment checklist`, `Dispatch evidence required`, and `Mailbox sent timestamp`; Markdown renders `#### Dispatch Commitment Checklist`, `#### Dispatch Evidence Required`, `Dispatch commitment ready to send: yes`, and `Dispatch actual send recorded: no`. Copy safety remains safe to forward with protected link leaks `0`, premature claim flags `0`, and GET safety still confirms no email send, buyer-state mutation, beta-client creation, CRM transmission, paid lookup, or live-proof claim. No production POSTs were run.
- Local and production market-ready stakeholder-chase-map verification passed for deployment `3b1d4e83-63cc-41da-b151-f8f2904c1100`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed. Local `qa:launch` was not used as the final signal because the persisted local JSON state had already advanced into rep-feedback while a fresh SQLite fixture had no protected activation lead; the state-sensitive verifier correctly failed those mismatched fixtures. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, SQLite live, and hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, packet `confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, `3` demo clients excluded, and `0` paid lookups recommended now. The new `launch_clearance_plan.current_action.stakeholder_chase_map` is `deal_threads.market_buyer_confirmation_chase_map.v1` with status `stakeholder_input_needed`, `3` missing required details, `2` stakeholder asks needed, `2` saved required details, blocking details `target_page`, `implementation_owner`, and `proof_recipients`, saved details `crm_owner` and `routing_owner`, needed roles `website_owner` and `proof_recipients`, quick reply and request kit ready, paid lookups `0`, and safety flags confirming GET sends no email, mutates no buyer state, creates no beta client, transmits no CRM data, runs no paid lookup, and claims no live proof. Protected HTML renders `Stakeholder chase map`, `Stakeholder chase asks`, and `Stakeholder chase sequence`; Markdown renders `#### Stakeholder Chase Map` and `#### Stakeholder Chase Asks`. Copy safety remains safe to forward with protected link leaks `0`, premature claim flags `0`, and paid enrichment recommended now `0`. No production POSTs were run.
- Local and production market-ready copy-safety-audit verification passed for deployment `e09ac128-6e5b-4e7d-a9f3-bba126c4eb1b`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`. Local GET smoke confirmed protected `/api/v1/launch/market-ready.launch_clearance_plan.current_action.manual_dispatch_packet.copy_safety_audit` is `deal_threads.market_dispatch_copy_safety_audit.v1`, `safe_to_forward=true`, body links `13`, protected link leaks `0`, premature claim flags `0`, paid enrichment recommended now `0`, and buyer-safe/proof-claim checks pass; protected HTML renders `Copy safety audit` and `Copy link audit`; Markdown renders `#### Copy Safety Audit`, `#### Copy Link Audit`, and `Copy safe to forward: yes`. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`, SQLite live, and hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, packet `confirmation_nudge`, dispatch status `ready_to_send`, dispatch send ready `true`, manual copy ready `true`, `2` recipients, copy audit type `deal_threads.market_dispatch_copy_safety_audit.v1`, copy safe to forward `true`, body links `12`, protected link leaks `0`, premature claim flags `0`, paid enrichment recommended now `0`, `7` market blockers, `1` warning, `0` real beta clients, `0` real beta profiles/leads, and `3` demo clients excluded; protected HTML and Markdown render the copy-safety and link-audit sections. No production POSTs were run.
- Local and production market-ready manual-dispatch-packet verification passed for deployment `22e82dd2-c760-4850-8c81-91c28b002605`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`. Local GET smoke confirmed protected `/api/v1/launch/market-ready.launch_clearance_plan.current_action.manual_dispatch_packet` exposes the full copy-ready packet for the active manual buyer follow-up with recipients, subject, full body, mailto URL, copy blocks, buyer-safe links, operator links, manual sent POST recording metadata, post-send plan, quick-reply fallback, paid enrichment needed now `0`, and `manual_record_requires_actual_send_first=true`; protected HTML renders `Manual dispatch packet`, copy-ready textareas, and the actual-send-first recording guardrail; Markdown renders `#### Manual Dispatch Packet` and the full body inside a fenced text block. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok` with hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, packet `confirmation_nudge`, manual dispatch status `copy_ready`, copy ready `true`, `2` recipients, body length `2194`, recording method `POST`, record requires actual send first `true`, paid enrichment recommended now `0`, `7` market blockers, `1` warning, `0` real beta clients, and `0` real beta profiles. No production POSTs were run.
- Local and production market-ready dispatch-preflight verification passed for deployment `f1e0beb1-ce7d-4bb0-abba-9150db2f1169`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`. Local GET smoke confirmed protected `/api/v1/launch/market-ready.launch_clearance_plan.current_action.dispatch_preflight` exposes a read-only send preflight for the current packet with `send_ready=true`, `0` blockers, `0` warnings, `15` checks, required buyer/operator link inventory, explicit send POST check, manual sent recorder check, operator sequence, and paid enrichment recommended now `0`; protected HTML renders `Dispatch preflight`, `Dispatch links`, and `Dispatch sequence`; Markdown renders `#### Dispatch Preflight Checks`, `#### Dispatch Operator Sequence`, and dispatch paid lookups `0`. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok` with hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, packet `confirmation_nudge`, dispatch status `ready_to_send`, dispatch send ready `true`, `0` dispatch blockers, `0` dispatch warnings, `15` dispatch checks, `6` dispatch links, paid enrichment recommended now `0`, `7` market blockers, `1` warning, `0` real beta clients, and `0` real beta profiles. No production POSTs were run.
- Local and production market-ready blocker-clearance-map verification passed for deployment `4bb9b5cf-4d2f-4a57-8c5b-a1dbb9bdb109`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`. Local GET smoke confirmed protected `/api/v1/launch/market-ready` exposes `blocker_clearance_map` and `launch_clearance_plan.blocker_clearance_map` with counts matching the market gate, current focus `buyer_confirmation`, first hard blocker `real_beta_client`, `8` open blocker/warning items, owner/proof/surface details, current action mailto/POST context, and paid enrichment recommended now `0`; protected HTML renders `Blocker clearance map`, `First hard blocker`, and owner-by-owner clearance copy; Markdown renders `### Blocker Clearance Map`, `Can start now`, and paid lookups `0`. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok` with hardening `100/100`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta profiles, map focus `buyer_confirmation`, first hard blocker `real_beta_client`, `8` open map items, and paid enrichment recommended now `0`; protected HTML and Markdown render the blocker-clearance map. No production POSTs were run.
- Local and production market-ready buyer-completion context verification passed for deployment `f480128a-e71f-44b8-8c46-df33e098742b`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`. Local GET smoke confirmed protected `/api/v1/launch/market-ready` exposes buyer-side completion context inside `launch_clearance_plan.current_action`: missing confirmation details, post-send plan, after-send actions, acceptance criteria, quick-reply fallback when available, buyer-safe completion links, and paid enrichment needed now `0`; protected HTML and Markdown render `Missing details`, `After send`, `Acceptance criteria`, and the current action run sheet. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, packet `confirmation_nudge`, missing details `Target page`, `Implementation owner email`, and `Proof recipients`, post-send plan `ready_after_manual_send`, `5` after-send actions, `4` acceptance criteria, quick-reply fallback available, `7` blockers, `1` warning, `0` real beta clients, and paid enrichment needed now `0`. No production POSTs were run.
- Local and production market-ready current clearance action verification passed for deployment `7474ecc5-cad9-4f8e-8fa5-82611812dc21`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`. Local GET smoke confirmed protected `/api/v1/launch/market-ready` exposes `launch_clearance_plan.current_action` and `action_bundle` with a manual `mailto:` primary surface, `POST` send action, `POST` manual sent recorder, and read-only GET safety, and protected HTML/Markdown render `Current clearance action`, `Manual email draft`, `Operator POST`, and `Manual sent POST`. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `current_stage=buyer_confirmation_chase`, `current_action_key=send_confirmation_nudge`, packet `confirmation_nudge`, primary surface `Manual email draft`, primary method `MAILTO`, operator POST method `POST`, manual record method `POST`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, and `0` paid enrichment lookups needed now; protected `/launch/market-ready` and Markdown render the current clearance action section. No production POSTs were run.
- Local and production first-five first-open action-kit verification passed for deployment `44a6c555-54b1-4040-ab8c-6628f19ed65b`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`. Local GET smoke confirmed top-level `/api/v1/launch/first-five-board.first_open_slot.action_kit` exposes the first open activation prospect handoff. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok` with hardening `hardened`; protected `/api/v1/launch/first-five-board` remains `buyer_confirmation_follow_up` with first open slot `LaunchReady Co`, kind `activation_prospect`, packet `confirmation_nudge`, `mailto:` manual handoff, POST send action, and POST manual sent recorder; protected HTML renders `First open action kit` and `Open manual draft`; Markdown renders `First open action kit packet`, `First open mailto draft`, and `First open send POST`. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `1` confirmation nudge due, `7` blockers, and `1` warning. No production POSTs were run.
- Local and production confirmation watchroom primary-surface verification passed for deployment `db0fa7e5-86a0-4d07-98ae-c62318e916d0`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`. Production GET-only smoke confirmed `/api/v1/health` returns `status=ok` with hardening `hardened`; protected `/api/v1/launch/confirmation-watch` remains `chase_due` for `LaunchReady Co` with current focus `nudge_due`, `primary_surface_label=Manual email draft`, `primary_surface_method=MAILTO`, `operator_post_method=POST`, `manual_record_method=POST`, queue packet `confirmation_nudge`, and queue primary-surface method `MAILTO`; protected HTML and Markdown render `Manual email draft`, `Primary method`, `MAILTO`, and `Operator POST`. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `1` confirmation nudge due, `7` blockers, and `1` warning. No production POSTs were run.
- Local and production confirmation command primary-surface verification passed for deployment `60027fa1-36be-4e5b-97b1-f253ea88b20e`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`; local headless browser smoke saved `.artifacts/launch-readiness/confirmation-command-primary-surface-method.png` and confirmed `/launch/confirmation-command` renders `Manual email draft`, `Primary method`, `MAILTO`, and `Operator POST` in the current-action room. Production read-only smoke confirmed `/api/v1/health` remains `ok` with hardening `100/100`; protected `/api/v1/launch/confirmation-command` remains `buyer_confirmation_action_ready` for `LaunchReady Co` with packet `confirmation_nudge`, `current_action.key=send_or_record_buyer_follow_up`, `current_action.method=MAILTO`, `primary_surface_label=Manual email draft`, `primary_surface_method=MAILTO`, `operator_post_method=POST`, `manual_record_method=POST`, and a `mailto:` primary surface while send and manual-record actions remain explicit POSTs; protected HTML and Markdown both render the primary-surface metadata. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `1` confirmation nudge due, `7` blockers, and `1` warning. No production POSTs were run.
- Local and production first beta execution dynamic first-open-step verification passed for deployment `276a6e1a-5a3c-4c25-b811-54289a043f97`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`; local headless browser smoke saved `.artifacts/launch-readiness/first-beta-execution-dynamic-first-open-step.png` and confirmed `/launch/first-beta-execution` renders `First beta execution packet`, `First open step`, `Send the close packet`, and the first-open-step next action with no broken page. Production read-only smoke confirmed `/api/v1/health` remains `ok` with hardening `100/100`; protected `/api/v1/launch/first-beta-drill` is `drill_active` with `first_open_step.key=send_confirmation_nudge`, label `Send the buyer confirmation nudge`, and `recommended_packet_kind=confirmation_nudge`; protected `/api/v1/launch/first-beta-execution` is `buyer_confirmation_action_ready` with `summary.first_open_step=send_confirmation_nudge`, label `Send the buyer confirmation nudge`, and next action `Send the confirmation nudge and ask the buyer to submit the tokenized confirmation form.`; protected HTML and Markdown both render the confirmation-nudge first-open-step label and next action. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `0` ready for kickoff, `1` confirmation nudge due, `7` blockers, and `1` warning. No production POSTs were run.
- Local and production first beta next-action post-send plan verification passed for deployment `b79ed9fd-7e5c-4014-99e2-907756bfe9d3`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 LAUNCH_ADMIN_USERNAME=admin LAUNCH_ADMIN_PASSWORD=deal-threads-local WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/next-action-post-send-plan.png` and confirmed `/launch/next-action` renders `Post-send plan`, `Watch confirmation`, `Parse buyer reply`, `After send`, `Acceptance criteria`, the manual sent recorder, and read-only safety copy with no console errors. Production read-only smoke confirmed `/api/v1/health` remains `ok` with hardening `100/100` and SQLite live; protected `/api/v1/launch/next-action` remains `active_buyer_follow_up_ready` for `LaunchReady Co` with packet `confirmation_nudge`, `post_send_plan.status=ready_after_manual_send`, watch room `/launch/confirmation-watch`, reply parser `/activation/prospects/lead_a6f44a24/confirmation-reply`, POST-only manual sent recorder `/api/v1/activation/prospects/lead_a6f44a24/confirmation-nudge/sent`, `5` after-send actions, `4` acceptance criteria, and safety flags showing GET sends no email, creates no beta client, transmits no CRM data, runs no paid lookup, and claims no live proof; protected HTML and Markdown both render the post-send plan. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `0` ready for kickoff, `1` confirmation nudge due, `7` blockers, `1` warning, and `0` paid enrichment lookups. No production POSTs were run.
- Local and production buyer status partial-save verification passed for deployment `707fca28-e60d-45fb-a376-1c20a67ec4a2`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/buyer-status-save-progress-without-confirming.png` and confirmed `/confirm/:token/status` renders `Finish missing details`, `Finish buyer confirmation`, `Save progress without confirming`, the `formnovalidate` partial-save button, and safety copy. Production read-only smoke confirmed `/api/v1/health` remains `ok` with hardening `100/100` and SQLite live; public LaunchReady Co status HTML, JSON, and Markdown return `200` and expose `progress_save_action.action_value=save_progress`, `Save progress without confirming`, and safety flags showing partial save mutates buyer confirmation details only while sending no external email, creating no beta client, transmitting no CRM data, running no paid provider lookup, and claiming no live proof. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `0` ready for kickoff, `1` confirmation nudge due, `7` blockers, `1` warning, and `0` paid enrichment lookups. No production POSTs were run.
- Local and production buyer status finish-confirmation form verification passed for deployment `1c7936e4-66bd-4054-bbcd-082387f57ba7`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`; local regression verified the tokenized buyer status JSON/HTML/Markdown contract for `finish_confirmation_form`, including `POST /confirm/:token`, `action=confirm`, required target page, implementation owner, CRM owner, routing owner, and proof-recipient fields, submit safety, and no protected `/crm/` or `/api/v1` leakage; local browser smoke saved `.artifacts/launch-readiness/buyer-status-finish-confirmation-form.png` and confirmed `/confirm/:token/status` renders `Finish missing details`, the compact confirmation form, the `Finish buyer confirmation` submit button, and safety copy. Production read-only smoke confirmed `/api/v1/health` remains `ok` with hardening `100/100` and SQLite live; Railway status reports the production service online; active LaunchReady Co (`lead_a6f44a24`) has tokenized status URL `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status`; public status JSON, HTML, and Markdown all return `200` and expose the finish-confirmation form with fields `targetPageUrl`, `implementationOwnerName`, `implementationOwnerEmail`, `crmOwnerEmail`, `routingOwnerEmail`, and `reportRecipients`; the form clears the current missing details `Target page`, `Implementation owner`, and `Proof recipients`; submit may mutate buyer confirmation state, may send a confirmation receipt, and may create a beta client only through auto-kickoff, while it transmits no CRM data, runs no paid provider lookup, and claims no live proof. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `0` ready for kickoff, `1` confirmation nudge due, `7` blockers, `1` warning, and `0` paid enrichment lookups. No production POSTs were run.
- Local and production first-five outreach reply-capture verification passed for deployment `d9128eae-2af0-4d8e-b1b6-40a3213bfc08`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`; local regression verified the protected JSON reply-capture endpoint `POST /api/v1/outreach/target-accounts/:domain/activation-prospect`, the HTML form `POST /outreach/target-accounts/:domain/create-activation-prospect`, activation prospect creation without beta-client creation, target-account reply source tagging, buyer-confirmation incompleteness until proof recipients are captured, separated sent vs reply event counts, activation intake triage surfacing `target_account_reply`, and read-only GET safety; local browser smoke saved `.artifacts/launch-readiness/first-five-outreach-reply-capture.png` and confirmed `/outreach` renders `Capture interested reply`, `Create activation prospect`, `Reply capture`, and read-only safety copy. Production read-only smoke confirmed `/api/v1/health` remains `ok` with hardening `100/100` and SQLite live; protected `/api/v1/outreach/first-five` remains `pipeline_active` with `4` visible target accounts, `3` POST-only manual sent recorders, `3` POST-only activation reply-capture actions, `0` outreach sent records, `0` outreach reply records, and `0` paid lookups recommended now; the first available production reply-capture action is `POST`, creates an activation prospect, creates no beta client, sends no server email, transmits no external CRM data, runs no paid provider lookup, and is preview-only on GET; protected `/outreach` and Markdown render the reply-capture surfaces; protected `/api/v1/activation/intake-triage` remains `buyer_confirmation_follow_up` with `1` qualified activation prospect, `0` required code builds before first beta, and `0` paid lookups recommended now; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients and `7` blockers. No production POSTs were run.
- Local and production activation outbox post-send plan verification passed for deployment `fdf5b094-e47e-445b-bcc6-fbbe616cf561`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed; a temporary local widget-conversation lead passed `QA_BASE_URL=http://127.0.0.1:4174 WIDGET_ID=wid_replede_demo npm run qa:launch` at `38/38` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/activation-outbox-post-send-plan.png` and confirmed `/activation/outbox` renders `Manual draft ready`, `Post-send plan`, `After send`, `Watch confirmation`, `Parse buyer reply`, manual-delivery recording guidance, paid-enrichment deferral, and GET safety copy; Railway status reports production online; production read-only smoke confirmed `/api/v1/health` returns `ok`, hardening remains `100/100`, SQLite data store is live, protected `/api/v1/activation/outbox?kind=confirmation_nudge` remains `confirmation_nudge_ready` with `1` sendable LaunchReady Co confirmation nudge, `manual_mailto_drafts=1`, `send_rooms_ready=1`, `manual_sent_recorders=1`, `send_room.status=manual_draft_ready`, `post_send_plan.status=ready_after_manual_send`, `5` post-send actions, `4` acceptance criteria, confirmation watch room, protected reply parser, POST-only manual sent recorder, and `paid_enrichment_needed_now=0`; protected outbox HTML and Markdown both render the post-send plan. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `0` required code builds before first beta, `7` real-world proof steps, and `0` paid enrichment lookups. No production POSTs were run.
- Local and production activation outbox send-room verification passed for deployment `fc394b9b-4a51-4cf8-a9cd-e6ee04df2e12`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed; a temporary local widget-conversation lead passed `QA_BASE_URL=http://127.0.0.1:4174 WIDGET_ID=wid_replede_demo npm run qa:launch` at `38/38` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/activation-outbox-send-room.png` and confirmed `/activation/outbox` renders `Send rooms`, `Mailto drafts`, `Recorders`, `Manual draft ready`, `Open manual email draft`, and GET safety copy; Railway status reports production online; production read-only smoke confirmed `/api/v1/health` returns `ok`, hardening remains `100/100`, SQLite data store is live, protected `/api/v1/activation/outbox?kind=confirmation_nudge` remains `confirmation_nudge_ready` with `1` sendable LaunchReady Co confirmation nudge, `manual_mailto_drafts=1`, `send_rooms_ready=1`, `manual_sent_recorders=1`, top-level `manual_mailto_url` beginning with `mailto:`, `send_room.status=manual_draft_ready`, buyer-safe confirmation/status/approval-brief/request-kit links, proof effect showing buyer-approved details unlock real beta kickoff, and safety flags confirming read-only GET sends no email, mutates no buyer state, creates no beta client, transmits no CRM data, runs no paid enrichment, and claims no live proof; protected outbox HTML and Markdown both render the send-room fields. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `0` required code builds before first beta, `7` real-world proof steps, and `0` paid enrichment lookups. No production POSTs were run.
- Local and production founder timeline verification passed for deployment `f5e22d67-864b-43a7-b80f-32fe44fa10f0`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed; a temporary local qualified pilot-intake state passed `QA_BASE_URL=http://127.0.0.1:4174 WIDGET_ID=wid_replede_demo npm run qa:launch` at `38/38` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/founder-timeline-market-gate.png` and confirmed `/launch/market-ready` renders `Founder timeline answer`, `Core build is ready for the first beta`, `Software left`, `First beta estimate`, paid-enrichment deferral, `How much is left?`, and `Launch clearance plan`; Railway status reports production online; production read-only smoke confirmed `/api/v1/health` returns `ok`, hardening remains `100/100`, SQLite data store is live, protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients, `0` required code builds before first beta, `7` real-world proof steps, and `0` paid enrichment lookups, and the new founder timeline says core build is ready for first beta while remaining work is buyer confirmation and real proof; protected HTML and Markdown both render the founder timeline answer. No production POSTs were run.
- Local and production buyer approval brief verification passed for deployment `6786b7f2-acee-4335-a1c1-5aa06285f29c`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed; a temporary local confirmed pilot-intake state passed `QA_BASE_URL=http://127.0.0.1:4174 WIDGET_ID=wid_replede_demo npm run qa:launch` at `38/38` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/buyer-approval-brief.png` and confirmed `/confirm/:token/brief` renders the buyer-facing approval brief, decision brief, approval options, copy-ready approval reply, after-approval sequence, providerless risk control, zero forms, zero `/api/v1` links, and no protected `/crm/` or `/admin` links; Railway status reports the production service online; production read-only smoke confirmed `/api/v1/health` returns `ok`, hardening remains `100/100`, public LaunchReady Co `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/brief` returns `200` with the approval brief, decision brief, approval options, copy-ready approval reply, zero forms, no protected links, and no API links, JSON returns `deal_threads.public_buyer_confirmation_approval_brief.v1` with status `approval_needed`, `3` missing required details, paid lookups `0`, read-only safety, no operator forms, no CRM profiles, no buyer-state mutation, no beta-client creation, no external email, no external data transmission, no paid enrichment, no live-result claims, and no protected references, and Markdown export includes `Runs paid enrichment: no` and `Claims live proof: no`; protected close desk now exposes the approval brief link for the active LaunchReady Co confirmation nudge; protected market gate remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients and paid lookups `0`. No production POSTs were run.
- Local and production webinar walkthrough verification passed for deployment `877ad36b-d695-4820-923a-f389805bfc27`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed; a temporary local confirmed pilot-intake state passed `QA_BASE_URL=http://127.0.0.1:4174 WIDGET_ID=wid_replede_demo npm run qa:launch` at `38/38` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/webinar-walkthrough.png` and confirmed `/webinar` renders the buyer-facing webinar headline, agenda, stakeholder sections, providerless strategy CTA, CRM profile screenshot, zero forms, and no protected `/crm/` or `/admin` links; Railway status reports the production service online; production read-only smoke confirmed `/api/v1/health` returns `ok`, hardening remains `100/100`, public `/webinar` returns `200` with agenda and screenshot, zero forms, and no protected links, `/api/v1/webinar` returns `deal_threads.public_webinar_packet.v1` with `30` minutes, default paid enrichment spend `0`, read-only safety, no operator forms, no CRM profiles, no buyer-state mutation, no beta-client creation, no external email, no external data transmission, no paid enrichment, and no live-result claims; Markdown export includes `Runs paid enrichment: no` and `Claims live proof: no`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, first-five status remains `buyer_confirmation_follow_up`, and production still has `0` real beta clients. No production POSTs were run.
- Local and production first-five beta invite verification passed for deployment `60984caf-1eb2-469d-8e85-92adc2d91431`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed; a temporary local confirmed pilot-intake state passed `QA_BASE_URL=http://127.0.0.1:4174 WIDGET_ID=wid_replede_demo npm run qa:launch` at `37/37` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/first-five-beta-invite.png` and confirmed `/first-five-beta` renders the buyer-facing headline, beta-slot metrics, proof sequence, CRM profile screenshot, safety copy, zero forms, and no protected `/crm/`, `/admin`, or `/api/v1` links; Railway status reports the production service online; production read-only smoke confirmed `/api/v1/health` returns `ok`, hardening remains `100/100`, public `/first-five-beta` returns `200` with the invite, `Client-domain install proof`, screenshot image, zero forms, and no protected links, `/api/v1/first-five-beta` returns `deal_threads.public_first_five_beta_invite.v1` with `5` target beta clients, `5` remaining beta slots, `1` qualified activation prospect, `4` visible target accounts, target-account names hidden, read-only safety, no operator forms, no CRM profiles, no buyer-state mutation, no beta-client creation, no external email, no external data transmission, no paid enrichment, and no live-result claims; Markdown export includes `Runs paid enrichment: no` and `Claims live proof: no`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `7` blockers and `1` warning. No production POSTs were run.
- Local and production first-five outreach send-room verification passed for deployment `54d49325-f81a-4e3e-b3da-a0d215a7de7c`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/first-five-outreach-send-room.png` and confirmed `/outreach` renders `Send rooms`, `Mailto drafts`, `Open email draft`, `After send`, and read-only safety copy; production read-only smoke confirmed `/api/v1/outreach/first-five` is `pipeline_active` with `4` visible target accounts, `3` manual outreach send rooms ready, `4` manual mailto drafts, `0` paid lookups recommended now, first account send room `manual_outreach_ready`, HTML and Markdown include mailto/post-send plan surfaces, `/api/v1/health` remains `ok` with hardening `100/100`, and `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients. No production POSTs were run.
- Local and production first-five outreach manual-recorder verification passed for deployment `5e131434-1124-4d09-9c2c-2b7700343efb`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, `npm test`, and `QA_BASE_URL=http://127.0.0.1:4173 WIDGET_ID=wid_replede_demo npm run qa:launch` passed; launch QA remained `38/38` with hardening `100/100`; local regression verified the protected JSON recorder `POST /api/v1/outreach/target-accounts/:domain/sent`, the HTML form `POST /outreach/target-accounts/:domain/mark-sent`, persisted company-memory outreach events, next-check scheduling, and read-only GET safety; local browser smoke saved `.artifacts/launch-readiness/first-five-outreach-manual-recorder.png` and confirmed `/outreach` renders `Record manual send`, `Recorders`, `Sent records`, `Manual sent POST`, and read-only safety copy. Production read-only smoke confirmed `/api/v1/health` remains `ok` with hardening `100/100`; protected `/api/v1/outreach/first-five` remains `pipeline_active` with `4` visible target accounts, `3` send-ready target-account rooms, `4` manual mailto drafts, `3` POST-only manual sent recorders, `0` outreach sent records, `0` follow-ups due, and `0` paid lookups recommended now; protected `/outreach` and Markdown render the recorder surfaces; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, with `0` real beta clients and `7` blockers. No production POSTs were run.
- Local and production post-approval launch-preview verification passed for deployment `a9cd6434-dbe5-4128-8ae0-d5925f38fc57`: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js`, and `npm test` passed; a temporary local seeded confirmation-nudge state passed `WIDGET_ID=wid_replede_demo npm run qa:launch` at `36/36` with hardening `100/100`; local browser smoke saved `.artifacts/launch-readiness/buyer-confirmation-post-approval-preview.png` and confirmed the public buyer status room renders `Post-approval launch preview`, `Launch sequence after approval`, `Capture client-domain install proof`, paid enrichment now `0`, read-only safety copy, and no protected `/crm/`, `/admin`, or `/api/v1` links; production read-only smoke confirmed `/api/v1/health` returns `ok`, hardening remains `100/100`, market gate remains `buyer_confirmation_needed`, market ready remains `false`, launch claim status remains `internal_beta_only`, current stage remains `buyer_confirmation_chase`, required code builds before first beta is `0`, paid enrichment lookups needed now is `0`, confirmation nudge outbox count is `1`, external email is disabled, dry-run mode is enabled, public `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status` renders the post-approval preview and one-minute approval shortcut, JSON and Markdown exports include the preview, and preview safety flags confirm it does not create beta clients, send email, transmit CRM data, run paid enrichment, expose operator actions, expose protected CRM profiles, or claim live proof. No production POSTs were run.
- Local and production buyer-confirmation shortcut verification passed for deployment `8fc622ed-f3fc-4438-9182-a96c8d32a9ee`: local syntax checks, regression tests, full test suite, and `WIDGET_ID=wid_replede_demo npm run qa:launch` passed; local browser smoke saved `.artifacts/launch-readiness/buyer-confirmation-one-minute-shortcut.png`; production read-only smoke confirmed `/api/v1/health` returns `ok`, hardening remains `100/100`, market gate remains `buyer_confirmation_needed`, market ready remains `false`, launch claim status remains `internal_beta_only`, required code builds before first beta is `0`, paid enrichment lookups needed now is `0`, confirmation nudge outbox count is `1`, external email is disabled, dry-run mode is enabled, public `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status` renders `One-minute approval shortcut`, `Copy-ready approval reply`, and the approval line, JSON and Markdown exports include the shortcut, and the public status room exposes no protected `/crm/`, `/admin`, or `/api/v1` links. No production POSTs were run.

## Verified Gates

- `/api/v1/health` returns `status: ok`.
- Health hardening reports `hardened`, `100/100`, `9/9` checks passing.
- Public `/`, `/widget.js`, and `/api/v1/health` return `200`.
- Public `/` presents the buyer demo room with live proof metrics, trust posture, providerless positioning, and a CRM profile screenshot.
- Public `/pilot-launch` presents a buyer-safe start hub for the 14-day mid-market beta with stakeholder paths, launch sequence, proof gates, offer snapshot, providerless cost controls, buyer-safe links, JSON export, and Markdown export without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, external transmission, email sending, paid enrichment, or live-results claims.
- Public `/first-five-beta` presents a buyer-safe first-five beta invite for mid-market B2B prospects with cohort slot metrics, offer snapshot, fit criteria, pilot scope, proof sequence, buyer CTAs, CRM profile screenshot, JSON export, and Markdown export without exposing operator forms, CRM profiles, tenant exports, target-account names, buyer-state mutation, beta-client creation, external email, external data transmission, paid enrichment, or live-proof claims.
- Public `/webinar` presents a buyer-safe AI-powered lead enrichment webinar walkthrough for mid-market B2B stakeholders with 30-minute agenda, demo flow, audience fit, buyer takeaways, qualification prompts, follow-up CTAs, CRM profile screenshot, JSON export, and Markdown export without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, beta-client creation, external email, external data transmission, paid enrichment, or live-proof claims.
- Public `/pilot-intake` presents a consent-gated private beta request form that creates protected activation prospects from mid-market buyer requests, redirects successful form submissions to the tokenized `/confirm/:token/status?intake=1` buyer status room, and returns `public_handoff`, `public_next_action`, and `public_next_steps` in the JSON API response without exposing CRM profiles or operator actions.
- Public `/pilot-close-kit` presents the mid-market pilot offer, pricing ladder, install path, proof plan, providerless enrichment posture, and trust summary without exposing operator forms.
- Public `/pilot-agreement` presents the 14-day beta order packet with scope, fees, buyer responsibilities, Deal Threads responsibilities, success criteria, data/security posture, exclusions, and Markdown export without exposing operator forms or CRM profiles.
- Public `/pilot-approval-room` presents a buyer-safe buying-committee hub with executive, RevOps, website, CRM, security/legal/procurement, and sales-rep stakeholder tracks, approval checklist, launch sequence, default data posture, JSON export, and Markdown export without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/mutual-action-plan` presents a buyer-safe 14-day timeline from approval to proof review with owner milestones, buyer inputs, proof targets, risk controls, JSON export, and Markdown export without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/buyer-confirmation-guide` presents a buyer-safe pre-confirmation checklist with target-page, implementation-owner, CRM-owner, routing-owner, proof-recipient, and old-form-baseline prep, JSON export, and Markdown export without exposing confirmation tokens, operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/stakeholder-forwarding-kit` presents buyer-safe internal forwarding notes for executive, RevOps, website, CRM, security/procurement, and sales stakeholders, with JSON export and Markdown export, without exposing confirmation tokens, operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/pricing` presents buyer-safe pricing and packages for the free education offer, $500 private beta setup, $2,500/month managed service, $1,000/month intelligence reports add-on, and $5,000/month premium ICP campaigns, with JSON export and Markdown export, without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/sales-enablement` presents buyer-safe GTM support for the first five beta buyers with target segments, qualification filters, outbound email copy, LinkedIn copy, discovery questions, objection responses, demo talk track, success definition, JSON export, and Markdown export, without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/providerless-enrichment` presents the buyer-safe build-vs-buy strategy for self-built enrichment, including internal source stack, public-site role/contact cues, beta operating rules, field-level strategy, quality gates, paid escalation policy, aggregate cost metrics, JSON export, and Markdown export, without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, external transmission, paid lookup, or live-proof claims.
- Public `/compare` presents a buyer-safe category comparison against plain contact forms, generic chatbots, enterprise conversational marketing platforms, scheduling/routing tools, paid enrichment providers, and internal builds, with decision criteria, proof questions, JSON export, and Markdown export, without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/proof-preview` presents a buyer-safe synthetic proof packet preview with install evidence, profile volume, first-touch speed, rep usefulness, pipeline motion, providerless enrichment metrics, interpretation rules, buyer inputs, JSON export, and Markdown export without claiming live results or exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/business-case` presents a buyer-safe business case calculator with adjustable mid-market assumptions, modeled payback, annual service-cost comparison, pilot proof targets, JSON export, and Markdown export without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/implementation-guide` presents a buyer-safe implementation-owner guide with install prerequisites, stakeholder roles, script-tag steps, source-check QA, acceptance criteria, rollback plan, data posture, JSON export, and Markdown export without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/crm-handoff-guide` presents a buyer-safe CRM-owner guide with manual export, dry-run queue, synthetic webhook test, webhook delivery, HubSpot preview/sandbox options, field groups, go-live gates, rollback plan, JSON export, and Markdown export without exposing operator forms, CRM profiles, tenant exports, buyer-state mutation, or external transmission.
- Public `/procurement-packet` presents a buyer-safe procurement packet with approval checklist, pilot scope, commercial terms, default data posture, processor modes, public links, JSON export, and Markdown export without exposing operator forms, CRM profiles, protected tenant exports, admin routes, or buyer-state mutation.
- Tokenized public `/confirm/:token` lets a prospect confirm target page, implementation owner, CRM owner, routing owner, proof recipients, and old-form baseline with a completion checklist, missing-detail count, owner actions, request-kit/acceptance links, and browser-required fields for kickoff-gating details, without exposing protected CRM profiles, reports, operator actions, or live CRM controls; buyers can also save partial progress to the tokenized close workflow as `collecting_details` without confirming, sending email, creating a beta client, transmitting CRM data, running paid enrichment, or claiming live proof; valid confirmation submissions record a dry-run/webhook buyer-confirmation receipt for activation follow-up.
- Tokenized public `/confirm/:token/brief` gives buyer champions a concise approval brief with decision status, current blockers, captured and missing details, approval options, copy-ready approval reply, owner actions, after-approval proof sequence, forwardable summary, JSON export, and Markdown export; `GET` is read-only and does not expose operator forms, CRM profiles, tenant exports, mutate buyer state, create beta clients, send external email, transmit CRM data, run paid enrichment, or claim live proof.
- Tokenized public `/confirm/:token/status` lets the buyer review missing required confirmation details, current confirmation state, pilot acceptance state, a buying-committee brief, owner actions, risk controls, forwardable summary, buyer-safe support links, saved-progress plan, old-form baseline quick-save form, copy-ready finish-confirmation reply, JSON export, and Markdown export before submitting the confirmation form; `GET` is read-only and does not send email, create beta clients, mutate buyer state, transmit CRM data, run paid enrichment, expose protected CRM/operator links, or claim live proof.
- Tokenized public `/confirm/:token/request-kit` turns missing buyer-confirmation details into forwardable asks, one-click `mailto:` drafts, a champion reply template, and stakeholder-specific partial-save forms for website/implementation, CRM/RevOps, sales routing, and proof/report owners; each partial-save form posts only `action=save_progress` to the tokenized confirmation URL, and the proof-owner form now captures proof recipients plus structured old-form baseline metrics for first-touch minutes, meeting rate, opportunity rate, win rate, sales-cycle days, and notes. Partial saves store buyer-confirmation progress and do not confirm the pilot, send email, create a beta client, transmit CRM data, run paid enrichment, expose protected CRM/operator links, issue the install handoff, or claim live proof. JSON and Markdown exports are available, and `GET` remains read-only.
- Tokenized public `/confirm/:token/acceptance` captures the pilot signer, billing contact, billing method, setup-fee status, purchase-order/manual invoice notes, JSON export, and Markdown receipt in `close_workflow.pilot_acceptance`; `GET` is read-only and does not send email, create beta clients, mutate CRM, process payment, transmit payment data, run paid enrichment, expose protected CRM/operator links, or claim live proof.
- Local buyer-confirmation save-progress smoke passed before deployment: the public form renders `Save progress without confirming` with `formnovalidate`, a local tokenized form POST saves target page and CRM owner as `collecting_details`, leaves `buyer_confirmed_at` unset, keeps the buyer-confirmation receipt `not_sent`, creates no beta client, and the browser receipt page at `/confirm/:token/status?saved=1` shows `Confirmation progress saved` with no protected CRM/admin/API links. Screenshot: `.artifacts/launch-readiness/buyer-confirmation-save-progress.png`.
- Production read-only smoke passed against the Railway URL after deployment `5b4ccb36-9782-4115-9d70-8ba8c57a52fa`: `/api/v1/health` returns `200` with status `ok`, SQLite store live at `/data/deal-threads-dev.sqlite`, hardening remains `hardened` at `100/100`, public LaunchReady Co `/confirm/pcf_c90a184cf64c46ebb67eadba57589552` renders `Save progress without confirming`, `formnovalidate`, and save-progress safety copy with no protected links, public `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status?saved=1` renders `Confirmation progress saved` with no protected links, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Local saved-progress plan smoke passed before deployment: `/confirm/:token/status?saved=1` renders `Saved progress plan`, `Still blocking kickoff`, `Copy-ready finish reply`, and `Finish confirmation form`, with no protected CRM/admin/API links. Screenshot: `.artifacts/launch-readiness/buyer-confirmation-saved-progress-plan.png`.
- Production read-only smoke passed against the Railway URL after deployment `9060e63a-9412-47f5-acb5-439bec1b2469`: `/api/v1/health` returns `200` with status `ok`, SQLite store live at `/data/deal-threads-dev.sqlite`, hardening remains `hardened` at `100/100`, public LaunchReady Co `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status?saved=1` renders `Saved progress plan`, `Still blocking kickoff`, `Copy-ready finish reply`, and `Finish confirmation form` with no protected links, public status JSON exposes `saved_progress` with a finish-confirmation reply, read-only safety, `creates_beta_client=false`, and `runs_paid_enrichment=false`, public Markdown includes `## Saved Progress Plan`, `Copy-ready finish reply`, and close-workflow status, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Local finish-draft smoke passed before deployment: `/confirm/:token/status?saved=1` renders `Open finish draft`, the link resolves to a `mailto:` draft with the finish-confirmation subject/body, `Copy-ready finish reply` remains visible, and no protected CRM/admin/API links are exposed. Screenshot: `.artifacts/launch-readiness/buyer-confirmation-finish-draft.png`.
- Production read-only smoke passed against the Railway URL after deployment `296616c9-4133-4402-852f-11feefe6e616`: `/api/v1/health` returns `200` with status `ok`, SQLite store live at `/data/deal-threads-dev.sqlite`, hardening remains `hardened` at `100/100`, public LaunchReady Co `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status?saved=1` renders `Saved progress plan`, `Open finish draft`, and `Copy-ready finish reply` with no protected links, public status JSON exposes `saved_progress.mailto_url` beginning with `mailto:`, finish-confirmation subject `Finish LaunchReady Co Deal Threads pilot confirmation`, read-only safety, `sends_external_email=false`, `creates_beta_client=false`, and `runs_paid_enrichment=false`, public Markdown includes `Mailto draft: mailto:` and `## Saved Progress Plan`, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Local stakeholder partial-save request-kit verification passed before deployment: `/confirm/:token/request-kit` renders four stakeholder forms with `Save website / implementation update`, `Save CRM / RevOps update`, `Save routing update`, and `Save proof-recipient update`; each form posts to `/confirm/:token` with hidden `action=save_progress`, field-level inputs for only that stakeholder's details, and safety copy that it stores partial buyer-confirmation progress only. Regression tests passed `6/6`, full tests passed `14/14`, launch readiness passed `36/36` with hardening `100/100`, and browser smoke confirmed no protected admin or CRM links/actions. Screenshot: `.artifacts/launch-readiness/buyer-confirmation-request-kit-partial-save.png`.
- Production read-only smoke passed against the Railway URL after deployment `d349ba99-0aa2-432b-915d-14f3283869f7`: `/api/v1/health`, `/pilot-launch`, `/providerless-enrichment`, `/buyer-confirmation-guide`, `/stakeholder-forwarding-kit`, and `/security` return `200` with expected launch copy; the production service is online on Railway; an authenticated read-only activation probe found an existing production activation prospect with request kit `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/request-kit`; that public request kit returns `200`, renders the four stakeholder partial-save forms, includes `name="action" value="save_progress"`, and exposes no protected admin link/action or protected CRM profile link. No production POSTs were run.
- Local providerless conversion-surface verification passed before deployment: internal enrichment now detects public contact/demo forms, marketing-automation form embeds, scheduler links, chat widgets, and demo CTAs as `conversion_surface` evidence; the field is normalized into lead enrichment, CRM delivery payloads, company memory, providerless preflight, providerless build-plan coverage, providerless evidence JSON, Markdown, and HTML. `node --test test/internal-enrichment.test.js` passed `2/2`, `node --test test/regression.test.js` passed `6/6`, `npm test` passed `14/14`, `WIDGET_ID=wid_replede_demo npm run qa:launch` passed `36/36` with hardening `100/100`, and in-app browser smoke confirmed `/crm/:leadId/providerless-evidence` renders `Conversion surface`, `Replacement angle`, the safety banner, and the paid lookup firewall. Screenshot: `.artifacts/launch-readiness/providerless-evidence-conversion-surface.png`.
- Local buyer-confirmation baseline quick-save verification passed before deployment: `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js` passed `6/6`, `npm test` passed `14/14`, and `WIDGET_ID=wid_replede_demo npm run qa:launch` passed `36/36` with hardening `100/100`. In-app browser smoke confirmed `/confirm/:token/status` renders `Old-form baseline quick save`, all structured baseline fields, hidden `action=save_progress`, the safety copy, and no protected CRM/admin/API links. Screenshot: `.artifacts/launch-readiness/buyer-confirmation-baseline-quick-save.png`.
- Production read-only smoke passed against Railway deployment `6902e576-fd39-4ac0-995a-b9058abfdba9`: `/api/v1/health` returns `ok`; public `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status` renders `Old-form baseline quick save`, `Save old-form baseline progress`, all structured old-form baseline fields, and no protected CRM/admin/API links; public status JSON exposes `baseline_quick_save_form` with `action_value=save_progress`, six baseline fields, and safety flags confirming no email send, beta-client creation, CRM transmission, paid-provider lookup, or live-proof claim on submit; public status Markdown includes `## Old-Form Baseline Quick Save`; public `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/request-kit` renders structured baseline fields in the proof-recipient partial-save form; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Local providerless public-role/contact verification passed before deployment: internal enrichment now extracts named public stakeholders and public emails from same-domain team, about, contact, and leadership pages when the buyer's own site exposes role/title cues, then carries them into `decision_makers`, `buyer_profile.buying_committee`, reusable `research_evidence`, CRM buying-committee display, providerless evidence HTML, and providerless Markdown. `node --check src/internal-enrichment.js`, `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/internal-enrichment.test.js` passed `2/2`, `node --test test/regression.test.js` passed `6/6`, `npm test` passed `14/14`, and `WIDGET_ID=wid_replede_demo npm run qa:launch` passed `36/36` with hardening `100/100`. In-app browser smoke confirmed public `/providerless-enrichment` renders `Public-site role and contact cues from team, about, contact, and leadership pages`, no forms, no protected CRM/admin links, and zero paid lookups recommended. Screenshot: `.artifacts/launch-readiness/providerless-role-contact-source-stack.png`.
- Production read-only smoke passed against Railway deployment `c0576ffe-dff7-45cc-b860-80b94d068ee5`: `/api/v1/health` returns `ok`; public `/providerless-enrichment`, `/api/v1/providerless-enrichment`, and Markdown export include the public-site role/contact source line; the public page exposes `0` forms and no protected CRM/admin links; protected `/api/v1/enrichment/build-plan` includes the same source stack, paid lookup allowed by default remains `false`, manual approval remains required, and recommended paid lookups now remains `0`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Local buyer-confirmation quick-reply verification passed before deployment: the protected confirmation nudge now includes a structured `quick_reply_template`, the email body tells busy champions they can reply with the missing fields, the Markdown packet includes `## Quick Reply Fallback`, the protected confirmation workbench renders a quick-reply fallback section beside the existing reply parser, and the activation outbox exposes quick-reply fallback availability without sending email on `GET`. `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/regression.test.js` passed `6/6`, `node --test test/operator-pages.test.js` passed, `npm test` passed `14/14`, and `WIDGET_ID=wid_replede_demo npm run qa:launch` passed `36/36` with hardening `100/100`. In-app browser smoke confirmed `/activation/outbox?kind=confirmation_nudge` renders `Quick reply fallback ready`, the reply-preview link, the read-only safety banner, and no protected CRM profile links; `/activation/prospects/:leadId/confirmation-workbench` renders the quick-reply fallback and parser link. Screenshots: `.artifacts/launch-readiness/activation-outbox-quick-reply.png`, `.artifacts/launch-readiness/confirmation-workbench-quick-reply.png`.
- Production read-only smoke passed against Railway deployment `2163e743-0f96-4701-843e-4e2906796685`: `/api/v1/health` returns `ok`; protected `/api/v1/activation/prospects/lead_a6f44a24/confirmation-nudge` returns `buyer_confirmation_nudge_ready` with `quick_reply_template.enabled=true`, a protected reply-preview URL, `apply_requires_operator_post=true`, and `runs_paid_enrichment=false`; the nudge Markdown includes `## Quick Reply Fallback`; protected `/api/v1/activation/outbox?kind=confirmation_nudge` returns `1` sendable confirmation nudge with the quick-reply template, `external_email_enabled=false`, dry-run mode `true`, read-only `GET`, no buyer-state mutation, no email send, no CRM transmission, no paid-provider lookup, and no live-proof claim; protected `/activation/outbox?kind=confirmation_nudge` renders `Quick reply fallback ready`, `GET is read-only`, and `0` protected CRM profile links; protected `/api/v1/activation/prospects/lead_a6f44a24/confirmation-workbench` renders the quick-reply fallback and reply-preview path. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Local top-level buyer-confirmation quick-reply verification passed before deployment: the first beta execution packet, first beta next-action room, and launch confirmation command center now surface the active `quick_reply_template` directly, including the protected reply parser link, the operator-POST safety copy, and the buyer's copy-ready approval-line fallback. `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `npm test` passed `14/14`, and `QA_BASE_URL=http://127.0.0.1:4174 WIDGET_ID=wid_replede_demo npm run qa:launch` passed `36/36` with hardening `100/100` against a temporary local confirmation-nudge state. In-app browser smoke confirmed `/launch/next-action` and `/launch/confirmation-command` render `Quick reply fallback`, the `Approval: these details are approved for the 14-day pilot kickoff` line, `Parse buyer reply`, and read-only safety copy. Screenshots: `.artifacts/launch-readiness/next-action-quick-reply-top-level.png`, `.artifacts/launch-readiness/confirmation-command-quick-reply-top-level.png`.
- Production read-only smoke passed against Railway deployment `2034b16e-e57d-4518-8588-41a9c2b9fa70`: `/api/v1/health` returns `ok`; protected hardening remains `100/100`; protected `/api/v1/launch/next-action` remains `active_buyer_follow_up_ready` with packet `confirmation_nudge`, `quick_reply_template.enabled=true`, and protected `/launch/next-action` renders `Quick reply fallback` plus `Parse buyer reply`; protected `/api/v1/launch/confirmation-command` remains `buyer_confirmation_action_ready` with packet `confirmation_nudge`, `quick_reply_template.enabled=true`, and protected `/launch/confirmation-command` renders `Quick reply fallback` plus `Parse buyer reply`; protected `/api/v1/launch/first-beta-execution` includes the quick-reply fallback in JSON and Markdown; protected `/api/v1/activation/outbox?kind=confirmation_nudge` remains `confirmation_nudge_ready` with `1` confirmation nudge, `external_email_enabled=false`, dry-run mode `true`, and quick reply enabled. Protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, `0` required code builds before first beta, `8` real-world proof gates still incomplete or pending, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Local launch-clearance verification passed before deployment: the protected market launch readiness gate now includes a structured `launch_clearance_plan` that separates `can_start_now`, `waiting_on`, workstreams, `do_not_do_yet`, and definition-of-done requirements so the team can see what remains without confusing optional scale builds for launch blockers. `node --check server.js`, `node --check scripts/launch-readiness.mjs`, `node --test test/operator-pages.test.js` passed, `npm test` passed `14/14`, and `WIDGET_ID=wid_replede_demo npm run qa:launch` passed `36/36` with hardening `100/100` after a temporary local qualified-lead close-packet send moved the local state into confirmation-nudge mode. In-app browser smoke confirmed `/launch/market-ready` renders `Launch clearance plan`, `Can start now`, `Waiting on`, `Do not build yet`, `Definition of done`, buyer-confirmation and live-proof workstreams, paid-enrichment deferral, and read-only safety copy. Screenshot: `.artifacts/launch-readiness/market-clearance-plan.png`.
- Production read-only smoke passed against Railway deployment `9b5afb9f-304c-4bc3-b6fd-787ad40a874c`: `/api/v1/health` returns `ok`; protected hardening remains `100/100`; protected `/api/v1/launch/market-ready` includes `launch_clearance_plan.current_stage=buyer_confirmation_chase`, `launch_claim_status=internal_beta_only`, `buyer_confirmation`, `real_beta_client`, `client_domain_install`, `first_real_profile`, `crm_handoff`, `rep_feedback`, and `live_proof_gate` workstreams, `paid_enrichment_provider` in `do_not_do_yet`, `8` definition-of-done items, and read-only safety flags. Protected `/launch/market-ready` renders `Launch clearance plan`, `Can start now`, `Waiting on`, `Do not build yet`, `Definition of done`, and the GET safety copy; Markdown export includes `## Launch Clearance Plan`, `### Can Start Now`, and `### Do Not Build Yet`. The market gate still correctly reports `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `8f85147d-9d85-4e6f-8a55-2304b6f331a2`: `/api/v1/health`, `/providerless-enrichment`, `/buyer-confirmation-guide`, `/stakeholder-forwarding-kit`, and `/security` return `200` with expected launch copy; protected `/api/v1/enrichment/build-plan` includes `Conversion-surface analysis for contact forms, demo CTAs, schedulers, and chat widgets`, paid lookup allowed by default remains `false`, manual approval remains required, and recommended paid lookups now remain `0`; protected `/api/v1/leads/lead_a6f44a24/providerless-evidence`, `/crm/lead_a6f44a24/providerless-evidence`, and Markdown export render the new `Conversion Surface` section with read-only safety flags, no email send, no buyer-state mutation, no CRM transmission, no paid lookup, and no live-proof claim on `GET`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `0` client-domain config loads, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Optional `ACTIVATION_AUTO_KICKOFF_ON_CONFIRMATION=true` turns a valid buyer confirmation into an immediate beta-client launch record, launch-packet delivery attempt, and `/install/:token` redirect.
- Optional `INSTALL_AUTO_SOURCE_CHECK_ON_HANDOFF=true` runs the domain-safe source check immediately after a public `/install/:token` tested-URL submission while still requiring a real widget config load for live install proof.
- Tokenized public `/install/:token/status` gives implementation owners buyer-safe install proof with config-load counts, session counts, beta-profile counts, source-check state, rep-feedback totals, and next actions without lead IDs, protected CRM profile links, reports, or operator controls.
- Tokenized public `/install/:token/test` gives implementation owners a Deal Threads-hosted widget load test that loads the real beta-client script, records config/session evidence, refreshes the public install status, and avoids protected CRM profile links or operator controls.
- Install proof now separates hosted/test config loads from client-domain config loads. Hosted load tests prove Deal Threads config delivery, but live install proof and live-proof export readiness require the buyer's own target page/domain to load the widget config.
- Tokenized public `/handoff/:token/:leadId` gives customer reps a first-profile handoff packet with summary, qualification, call plan, discovery questions, copy blocks, providerless evidence, and Markdown export without protected CRM profile links, operator APIs, reports, tenant exports, or live CRM actions.
- Tokenized public `/handoff/:token/crm-export.csv`, `.md`, and `.json` give CRM owners a manual import fallback with captured buyer profiles and public handoff URLs before HubSpot/Salesforce/Pipedrive native sync is connected.
- Tokenized public `/handoff/:token/proof` gives buyer stakeholders a beta proof report with install evidence, profile volume, rep feedback, first-touch comparisons, providerless enrichment metrics, Markdown export, and JSON export without lead IDs, protected CRM links, reports, or operator actions.
- Public `/security` presents a buyer-safe security center with data categories, processor posture, access/privacy controls, hardening checks, providerless enrichment gates, and install boundaries without exposing operator forms, CRM profiles, tenant deletion controls, or protected exports.
- Public `/sample-profile` presents a read-only synthetic rep-ready buyer profile while keeping real CRM profiles protected.
- Public `/assets/crm-profile-preview.png` returns the product screenshot used by the buyer demo room.
- Public `/` no longer contains local-development positioning.
- Protected `/admin`, `/crm`, `/api/v1/trust/packet`, and `/api/v1/leads` return `401` without auth.
- Protected `/trust`, `/readiness`, `/proof`, `/crm`, `/activation`, `/activation/intake-triage`, `/activation/follow-ups`, `/activation/outbox`, `/activation/close-desk`, `/launch/first-five-board`, `/launch/ops`, `/launch/first-beta-drill`, `/launch/first-beta-execution`, `/launch/next-action`, `/launch/confirmation-command`, `/launch/confirmation-watch`, `/launch/first-profile`, `/launch/profile-handoff`, `/launch/rep-feedback`, `/launch/proof-capture`, `/launch/proof-packet`, `/launch/market-ready`, `/launch/proof-ledger`, `/launch/proof-handoff`, `/launch/install-queue`, `/outreach`, and `/admin` return `200` with admin auth.
- Protected CRM lead profiles include a lead-to-beta-client conversion path that can create or reuse a beta client launch record without counting the original Deal Threads sales lead as beta traffic.
- Protected `/launch/first-five-board` and `/api/v1/launch/first-five-board` provide the first-five beta board: five launch slots are filled from current evidence in order of real beta clients, qualified activation prospects, first-five outreach target accounts, and empty reserve slots. Each slot shows owner, stage, next action, proof focus, primary/supporting links, paid lookups recommended now, and read-only safety posture; activation-prospect slots include an action kit with the recommended packet, copy-ready subject/body while confirmation is pending, one-click `mailto:` draft, send POST preview, POST-only manual sent recorder when applicable, buyer-safe confirmation/status/request-kit links, and protected reply preview. Kickoff-ready activation slots switch the action kit to the beta-install kickoff POST and do not require a manual-sent recorder. The board includes a providerless launch guardrail with paid lookup disabled by default, manual approval required, review-queue counts, JSON export, and Markdown export. `GET` does not send email, mutate buyer state, mark buyer confirmation complete, create beta clients, transmit CRM data, run bulk sends, claim live proof, or run paid enrichment.
- Protected `/launch/ops` and `/api/v1/launch/ops` provide the launch operations command room: current first-five focus, outreach stage, buyer-confirmation follow-up, install queue, proof loop, recommended next actions, JSON export, Markdown export, and read-only safety flags without sending email, creating beta clients, mutating buyer state, transmitting CRM data, claiming live proof, or running paid enrichment on `GET`.
- Protected `/launch/next-action` and `/api/v1/launch/next-action` provide the first beta next-action room: active buyer follow-up copy, manual `mailto:` draft, manual sent recorder, buyer-safe confirmation/status/request links, reply preview, buyer-confirmation proof focus, after-this proof sequence, POST previews, JSON export, and Markdown export while confirmation is pending. When a buyer-confirmed prospect is `ready_for_kickoff`, the room prioritizes the explicit beta-install kickoff POST, shows `Active kickoff`, removes the manual email requirement, and keeps market-ready/live-proof claims blocked until install evidence clears. `GET` does not send email, mark buyer confirmation complete, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/confirmation-command` and `/api/v1/launch/confirmation-command` provide the active buyer confirmation-to-kickoff command center: it composes the next-action room, per-prospect confirmation workbench, reply preview, kickoff preflight, buyer-safe confirmation/status/request-kit links, manual `mailto:` draft, POST-only manual sent recorder, reviewed confirmation apply preview, beta kickoff preview, install-proof pivot, providerless enrichment firewall, JSON export, and Markdown export. Before confirmation it focuses the active follow-up; after confirmation it switches to kickoff; after kickoff it switches to install-proof mode and removes stale manual-send controls. `GET` does not send email, record manual sends, mark buyer confirmation complete, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/confirmation-watch` and `/api/v1/launch/confirmation-watch` provide the buyer-confirmation SLA watchroom: it composes the activation follow-up queue, activation outbox, activation close desk, confirmation command center, and market gate into current focus, due-now/overdue/waiting/kickoff-ready counts, next check time, missing confirmation details, per-buyer command-center links, buyer-safe status/request-kit links, providerless enrichment posture, JSON export, and Markdown export. After kickoff it pivots to install-proof focus instead of showing stale buyer follow-up controls. `GET` does not send email, record manual sends, mark buyer confirmation complete, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/first-profile` and `/api/v1/launch/first-profile` provide the first profile capture room after client-domain install proof: it selects the real beta client ready for profile capture, shows install/load/profile/CRM/rep proof counts, exposes a capture checklist, previews the POST-only launch test-lead payload, and links the launch wizard, install workbench, latest lead, feedback room, manual CRM export, proof packet, and market gate. `GET` does not create profiles, send email, mutate buyer state, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/profile-handoff` and `/api/v1/launch/profile-handoff` provide the profile handoff bridge after the first beta profile exists: it selects the real beta client and latest profile, shows the rep-ready brief, distinguishes queued CRM records from sent handoff proof, previews POST-only CRM delivery, rep alert, and rep feedback actions, and links the public rep handoff, feedback room, manual CRM export, buyer-safe proof report, proof packet, and market gate. `GET` does not create profiles, send email, send rep alerts, queue CRM delivery, transmit CRM data, update rep feedback, create beta clients, claim live proof, or run paid enrichment.
- Protected `/launch/rep-feedback` and `/api/v1/launch/rep-feedback` provide the rep feedback command room after beta profiles exist: it composes the install queue, profile handoff links, public feedback rooms, per-client/profile feedback coverage, missing-context gap status, copy-ready rep ask, `mailto:` draft, proof impact, JSON export, and Markdown export. `GET` does not send email, create profiles, mutate buyer state, create beta clients, queue CRM delivery, transmit CRM data, update rep feedback, claim live proof, or run paid enrichment.
- Protected `/enrichment` and `POST /api/v1/enrichment/preflight` provide a providerless account preflight before a beta install or target-account import: the operator submits a company website/domain, the internal enrichment engine inspects public website signals, private/local network fetch targets are blocked, paid provider usage remains `0`, JSON/Markdown packets are available, and reusable company memory is saved only on explicit POST with the save-memory option.
- Production read-only smoke passed against the Railway URL after deployment `7bedecbb-edca-4457-95df-3e4370aef948`: `/api/v1/health` returns `200`, SQLite store is live, hardening remains `100/100`, enrichment mode is `internal`, unauthenticated `/enrichment` and unauthenticated `POST /api/v1/enrichment/preflight` return `401`, protected `/enrichment` renders the providerless account preflight form and private-network fetch guard copy, protected `/api/v1/enrichment/build-plan` returns paid lookup allowed by default `false`, manual approval required `true`, and recommended paid lookups now `0`. No authenticated production preflight POST was run.
- Protected `/launch/first-beta-execution` and `/api/v1/launch/first-beta-execution` provide the first beta execution packet: active buyer action, copy-ready follow-up while confirmation is pending, buyer-safe confirmation links, confirmation reply preview, stakeholder handoff, proof ledger, install queue, providerless enrichment cost firewall, granular execution sequence, POST previews, JSON export, and Markdown export. When a buyer-confirmed prospect is `ready_for_kickoff`, the packet suppresses extra buyer-follow-up copy and makes the beta-install kickoff POST the active operator preview. `GET` does not send email, mark buyer confirmation complete, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/proof-capture` and `/api/v1/launch/proof-capture` provide the proof capture runbook after beta-client kickoff: it selects the active real beta client, shows the current proof phase, and walks client-domain install proof, first real beta profile, CRM handoff proof, rep feedback, first proof packet, and live-proof gate review with exact evidence surfaces, JSON export, Markdown export, and safety flags. If no real beta client exists, it falls back to buyer confirmation and kickoff guidance. `GET` does not send email, mark buyer confirmation complete, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/proof-packet` and `/api/v1/launch/proof-packet` provide the first proof packet workbench after the proof-capture sequence reaches packet assembly: it selects the ready real beta client, verifies strict evidence for client-domain install proof, first beta profile, CRM handoff proof, and rep feedback, previews the buyer-safe proof report, exposes POST-only proof delivery queueing, exposes POST-only manual sent recording for queued proof packets, and links the install workbench, report deliveries, market gate, and live-proof gate. `GET` does not queue a delivery, mark a report sent, send email, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/market-ready` and `/api/v1/launch/market-ready` provide the conservative market launch readiness gate: buyer assets, first-five pipeline, buyer confirmation, real beta-client conversion, install handoff, client-domain config proof, real beta profiles, CRM handoff, rep feedback, and live-proof status are combined into pass/warning/blocker gates with JSON and Markdown export; the packet now includes a readiness estimate that separates required code builds before first beta, remaining real-world proof steps, optional scale builds, and paid-enrichment lookups needed now, plus a `launch_clearance_plan.current_action` bundle that exposes the active manual email draft, explicit send POST, manual sent recorder, buyer-safe links, reply preview, missing buyer details, stakeholder chase map, quick-reply fallback, after-send actions, acceptance criteria, buyer-completion context, `buyer_completion_payload`, `reply_apply_preflight`, dispatch preflight, dispatch commitment checklist, `manual_send_evidence_recorder`, manual dispatch packet, copy-safety audit, and action safety flags from the top-level market gate; `buyer_completion_payload` separates the buyer's missing and saved confirmation details, partial-save availability, buyer-safe public links, copy-ready completion reply, and zero-paid-lookup safety; `reply_apply_preflight` verifies the protected reply parser, required apply fields including `replyEvidenceReceiptJson`, operator review, saved `confirmation_review.reply_evidence_receipt`, and zero-paid-lookup safety before a pasted buyer reply can become kickoff-ready; `stakeholder_chase_map` turns the current missing buyer-confirmation details into blocking detail keys, saved detail keys, stakeholder roles, request-kit/status-room sequence, and paid-lookup safety so the operator can see who is blocking kickoff before sending the nudge; dispatch preflight checks recipients, subject/body copy, manual mailto readiness, explicit send POST, manual sent recorder, buyer-safe confirmation/status/request-kit links, operator watch/reply-parser links, post-send plan, quick-reply fallback, and paid-enrichment guardrails before any send or sent-record action; manual dispatch packet exposes copy-ready recipients, subject, full body, mailto URL, copy blocks, buyer/operator links, post-send plan, quick-reply fallback, manual sent POST recording metadata with an actual-send-first guardrail, and `copy_safety_audit` checks for protected buyer-copy link leaks, premature market-ready/live-proof claims, required providerless/manual-CRM disclosures, and paid-enrichment guardrails; `dispatch_commitment_checklist` separates copy readiness from actual mailbox-send evidence, lists required recipient/subject/sender/timestamp evidence, shows ready-to-send and ready-to-record-after-send flags, and keeps the manual sent record blocked until the message actually leaves the mailbox; `manual_send_evidence_recorder` turns that commitment into a protected POST-only form on the market gate with required mailbox sent timestamp, required sender mailbox, recipient snapshot, subject snapshot, optional message-id, evidence note, `returnTo=/launch/market-ready`, no server send, persisted `manual_evidence`, and a hard actual-mailbox-send-first guardrail; it also includes `blocker_clearance_map` and `launch_clearance_plan.blocker_clearance_map` so every open blocker/warning has an owner, current evidence, proof condition, next action, primary/supporting surfaces, current-action POST context where applicable, and paid-lookup safety; `GET` is read-only and does not send email, create beta clients, mutate buyer state, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/market-kit` and `/api/v1/launch/market-kit` convert the market gate into launch-safe GTM guidance: current proof scope, approved claims, claims requiring disclosure, prohibited claims, website/LinkedIn/outbound/webinar copy blocks, buyer-safe CTAs, disclosure checklist, next actions, JSON export, and Markdown export; `GET` is read-only and does not send email, create beta clients, mutate buyer state, transmit CRM data, expose CRM profiles, claim live proof, or run paid enrichment.
- Protected `/launch/proof-ledger` and `/api/v1/launch/proof-ledger` provide the evidence checklist behind the market gate: buyer confirmation, real beta-client conversion, install handoff, client-domain install proof, first real profile, CRM handoff, rep feedback, and live-proof gate items map to owner, current evidence, proof condition, capture surface, verification surface, next action, JSON export, and Markdown export. `GET` does not send email, mark buyer confirmation complete, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/proof-handoff` and `/api/v1/launch/proof-handoff` provide copy-ready stakeholder asks generated from the proof ledger, grouped by buyer champion, implementation, CRM, sales, and Deal Threads operator owners, with suggested recipients, subject/body drafts, one-click `mailto:` drafts, POST-only manual sent recorders, JSON export, and Markdown export. `GET` does not send email, mark buyer confirmation complete, create beta clients, transmit CRM data, claim live proof, or run paid enrichment.
- Beta launch packets include a tokenized public `/install/:token` handoff page, `/install/:token/status` status endpoint, and `/install/:token/test` hosted widget load test for implementation owners with the snippet, config check, tested-URL confirmation form, and acceptance checklist, without exposing CRM profiles or operator actions.
- Beta launch packets include a tokenized public `/feedback/:token` room where customer reps can score beta-attributed buyer profiles without admin access, protected CRM profile links, reports, or operator actions.
- Protected launch and beta-client views can run an install source check against the submitted tested URL to confirm the page contains `/widget.js`, the widget ID, tenant ID, and matching beta client ID before asking the client to debug live loads; production can also run this automatically after public install handoff submissions.
- Protected `/activation` and `/api/v1/activation/real-beta` provide the real-beta activation cockpit: synthetic demo clients are excluded, qualified prospects are surfaced for beta-client conversion, install/source-check/config-load/live-lead gates are tracked against the live-proof export gate, and operator launch actions preserve the `/activation` return path.
- Protected `/activation/intake-triage` and `/api/v1/activation/intake-triage` provide the operator triage queue between public pilot intake and first-five beta execution: public intake submissions and qualified CRM leads are ranked by first-five fit score, fit reasons, fit concerns, missing buyer-confirmation details, source, recommended close/kickoff packet, protected CRM/workbench links, buyer-safe confirmation/status/request-kit links, JSON export, and Markdown export. `GET` is read-only and does not send email, mutate buyer state, mark buyer confirmation complete, create beta clients, transmit CRM data, claim live proof, run bulk sends, or run paid enrichment.
- Protected `/activation/follow-ups` and `/api/v1/activation/follow-ups` provide the activation follow-up queue: close-packet-needed, nudge-due, overdue-confirmation, waiting-on-buyer, and ready-for-kickoff states; per-prospect next actions, buyer-safe confirmation links, runbook/packet/workbench/handoff links, delivery audit summaries, Markdown export, and read-only safety flags without sending email, creating beta clients, mutating buyer state, claiming live proof, or running paid enrichment on `GET`.
- Protected `/activation/outbox` and `/api/v1/activation/outbox` provide a focused activation outbox for sendable buyer follow-ups that block first beta confirmation: close packets and confirmation nudges include recipients, subject/body, manual `mailto:` draft handoff, delivery audit, buyer-safe status/request links, exact send POST previews, manual sent recorder POST previews, JSON export, Markdown export, and read-only safety flags without sending email, creating beta clients, mutating buyer state, transmitting CRM data, claiming live proof, or running paid enrichment on `GET`; send and manual-record actions require explicit POST.
- Protected `/activation/close-desk` and `/api/v1/activation/close-desk` provide the activation close desk: all qualified activation prospects are batched by recommended next packet, with close-packet, confirmation-nudge, manual-confirmation, and kickoff actions; the first-five close-packet campaign plan selects the top send batch, and the buyer-confirmation chase campaign handles sent close packets that now need nudges, status-room review, request-kit forwarding, or reply parsing. Both campaign plans expose `mailto:` drafts, send POST previews, POST-only manual sent recorders, operator checklist, and market-gate effect; each item includes copy-ready subject/body, buyer-safe confirmation/status/request-kit links, protected workbench/handoff/runbook links, Markdown export, and protected `GET` safety flags without sending email, marking buyer confirmation complete, creating beta clients, transmitting CRM data, claiming live proof, running bulk sends, or running paid enrichment. Buyer-safe links rely on the existing activation confirmation-token workflow; send, capture, kickoff, and manual-record actions remain POST-only.
- Protected `/activation/prospects/:leadId/confirmation-workbench`, `/api/v1/activation/prospects/:leadId/confirmation-workbench`, and `/api/v1/activation/prospects/:leadId/pilot-acceptance` provide a focused buyer-confirmation and acceptance workbench for one activation prospect: nudge draft, suggested recipients, public confirmation form, status room, request kit, pilot acceptance receipt, missing details, stakeholder asks, manual confirmation capture defaults, kickoff preflight, market-gate effect, after-kickoff proof sequence, kickoff path, JSON export, and Markdown export; `GET` is read-only and does not send email, create beta clients, mutate buyer state, transmit CRM data, process payment, claim live proof, or run paid enrichment.
- Protected `/activation/prospects/:leadId/confirmation-reply` and `/api/v1/activation/prospects/:leadId/confirmation-reply-preview` provide a read-only confirmation reply parser for one activation prospect: pasted buyer replies are turned into a reviewable close-workflow payload with field evidence for target page, implementation owner, CRM owner, routing owner, proof recipients, and old-form baselines; the preview also emits a compact `reply_evidence_receipt` and the apply contract includes `replyEvidenceReceiptJson` so the saved confirmation review can prove why a reply was accepted. Applying the payload still requires a separate explicit POST to `/activation/prospects/:leadId/close-workflow`, and preview requests do not send email, create beta clients, mutate buyer state, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/activation/prospects/:leadId/stakeholder-handoff` and `/api/v1/activation/prospects/:leadId/stakeholder-handoff` provide a copy-ready stakeholder handoff pack for one activation prospect: champion note, website-owner note, CRM-owner note, sales-routing note, proof-recipient note, security/procurement note, buyer-safe confirmation/status/request-kit links, missing details, stakeholder asks, handoff sequence, kickoff preflight, operator links, JSON export, and Markdown export; `GET` is read-only and does not send email, create beta clients, mutate buyer state, transmit CRM data, claim live proof, or run paid enrichment.
- Protected `/launch/install-queue` and `/api/v1/launch/install-queue` provide the post-kickoff beta install queue: synthetic demo clients are excluded, real beta clients are ranked through launch-packet, install-handoff, source-check, client-domain config-load, first-profile, CRM-handoff, rep-feedback, and proof-packet states; each queue item includes proof preflight checks for client-domain install proof, first beta profile, CRM handoff proof, rep feedback, proof packet status, market-gate effect, and readiness for first proof packet versus live proof claim; JSON and Markdown exports include read-only safety flags without sending email, creating beta clients, mutating buyer state, transmitting CRM data, claiming live proof, or running paid enrichment on `GET`.
- Protected `/launch/install-queue/:betaClientId/workbench` and `/api/v1/beta-clients/:betaClientId/install-workbench` provide the per-client install proof workbench: launch state, buyer-safe public install/status/feedback/CRM/proof links, protected operator links, stakeholder copy blocks, one-click `mailto:` drafts, POST-only manual sent recorders, source-check status, public install steps, readiness checks, proof preflight, market-gate effect, JSON export, Markdown export, and read-only safety flags without sending email, creating beta clients, mutating buyer state, transmitting CRM data, claiming live proof, or running paid enrichment on `GET`.
- Protected `/outreach` and `/api/v1/outreach/first-five` provide the first-five beta outreach room: pilot target count, remaining beta slots, qualified activation prospects, imported target-account memory, account-specific first-touch email/LinkedIn copy, public buyer-safe links, prefilled `/pilot-intake` URLs, manual `mailto:` drafts, target-account send-room status, POST-only manual sent recorders, POST-only target-account reply capture into activation prospects, separated sent vs reply delivery counts, next-check/follow-up state, recipient hints, post-send plans, proof-effect summaries, Markdown export, and read-only safety flags without sending email, creating beta clients, mutating buyer state, transmitting CRM data, or running paid enrichment on `GET`.
- Protected `/api/v1/activation/prospects/:leadId/runbook` gives operators a JSON or Markdown activation runbook with the current next action, close-packet/nudge/confirmation/kickoff checklist, missing buyer-confirmation details, recommended beta-client defaults, buyer-safe links, protected endpoints, providerless enrichment posture, and safety flags without sending email, creating a beta client, or claiming live proof on `GET`.
- Protected `/api/v1/activation/prospects/:leadId/close-packet` generates a real-beta prospect close packet with a ready email draft, pilot close kit, pilot approval room, mutual action plan, buyer confirmation guide, stakeholder forwarding kit, pilot agreement, buyer confirmation form, buyer confirmation status room, buyer confirmation request kit, pilot acceptance room, security, and sample-profile links, buyer confirmation checklist, and operator next steps before conversion.
- Protected `/api/v1/activation/prospects/:leadId/close-packet/send` and `/activation/prospects/:leadId/send-close-packet` send or dry-run the prospect close packet through the email adapter and record delivery attempts on the prospect close workflow.
- Protected `/api/v1/activation/prospects/:leadId/confirmation-nudge`, `/api/v1/activation/prospects/:leadId/confirmation-nudge/send`, `/api/v1/activation/prospects/:leadId/confirmation-nudge/sent`, `/activation/prospects/:leadId/send-confirmation-nudge`, and `/activation/prospects/:leadId/mark-confirmation-nudge-sent` generate, send/dry-run, or record manual delivery of buyer-confirmation follow-up packets with the tokenized confirmation form, status room, request kit, pilot acceptance room, buyer confirmation guide, stakeholder forwarding kit, mutual action plan, implementation guide, CRM handoff guide, security center, and a separate nudge delivery audit without marking buyer confirmation complete.
- Protected `/activation/prospects/:leadId/close-workflow` and `/api/v1/activation/prospects/:leadId/close-workflow` can mark the prospect close packet sent, capture buyer-confirmed target page, owners, report recipients, and old-form baseline, then reuse those details as beta-client conversion defaults.
- Buyer confirmation receipt attempts are stored on `close_workflow.buyer_confirmation_delivery`, so activation follow-up is auditable even when email remains in dry-run mode.
- Protected `/api/v1/activation/prospects/:leadId/kickoff` and `/activation/prospects/:leadId/kickoff` convert a confirmed prospect into a beta client and send the beta install packet in one dry-run/webhook-tracked action; the API response includes a proof-transition delta with moved gates, remaining blockers, next proof actions, install/proof URLs, and safety flags that keep market-ready and live-proof claims blocked.
- Protected `/enrichment` and `/api/v1/enrichment/build-plan` provide field-level build-vs-buy decisions that rank rep-missing fields, internal coverage, sourced evidence, build-next actions, and paid-provider triggers while recommending zero paid lookups by default.
- Protected `/enrichment/review-queue` and `/api/v1/enrichment/review-queue` provide the providerless enrichment review queue: open manual-review profiles, reasons, confidence, rep-missing fields, suggested public/manual evidence, protected CRM-profile links, field build-vs-buy guidance, paid-firewall rules, JSON export, and Markdown export; `GET` is read-only and does not send email, mutate buyer state, create beta clients, transmit CRM data, run paid lookups, or claim live proof.
- Protected `/crm/:leadId/providerless-evidence` and `/api/v1/leads/:leadId/providerless-evidence` provide a per-lead providerless evidence packet: first-party chat context, website/internal signals, inferred buyer profile, sourced research evidence, reusable company memory, rep feedback gaps, missing-evidence actions, field build-vs-buy decisions, copy blocks, JSON export, and Markdown export; `GET` is read-only and does not send email, mutate buyer state, create beta clients, transmit CRM data, run paid lookups, or claim live proof.
- Internal providerless enrichment extracts public-site company size language, about/team buying-role signals, careers-page hiring growth, common tech stack markers, funding language, and reusable source evidence before any paid provider is considered.
- Protected `/api/v1/beta-clients/:id/crm-delivery/test` and `/beta-clients/:id/crm-delivery/test` send a synthetic CRM webhook test payload, record dry-run/webhook status on the beta client, and surface failures as readiness blockers before real buyer profiles are transmitted.
- Protected `/proof`, `/readiness`, and `/api/v1/proof/live-gate` include a live-proof export gate that excludes synthetic demo records and blocks buyer-facing proof export until real beta traffic exists.
- Public widget fallback submissions require explicit consent and return `consent_required` before lead creation when consent is missing.
- Public pilot-intake submissions require explicit consent and can prefill prospect close workflow details for `/activation`.
- Public `/pilot-intake` supports query-string prefill for account-specific outreach links while still requiring consent before creating any protected activation prospect.
- Security headers include `x-content-type-options: nosniff`, `x-frame-options: DENY`, and `referrer-policy: no-referrer`.
- Disallowed widget origins return `403`.
- Client-specific widget config now enforces beta-client domain authorization: an unknown `betaClientId` returns `404`, and a globally allowed domain cannot load another beta client's config. The Deal Threads hosted/test host remains allowed for hosted proof, while live install proof still requires the buyer's own domain.
- Invalid widget JSON returns `400`.
- Live widget conversation created a qualified CRM lead with internal enrichment and no paid-provider usage.
- First beta launch handoff path is verified: `/launch`, selected beta client wizard, launch packet JSON/Markdown, launch-packet domain authorization policy, public install handoff page, public install status endpoint, public hosted widget load test, public rep feedback room, public rep handoff packet when a buyer profile exists, manual CRM export CSV/Markdown, buyer-safe beta proof report, tested-URL confirmation capture, hosted/test vs client-domain install attribution, beta-client-matched allowed-domain config, invalid beta-client guard, test lead, and CRM handoff proof.
- Mid-market demo scenario is loaded and marked as synthetic data.
- Local `npm test` passed with 14/14 checks, including beta-client runtime domain authorization, invalid beta-client config rejection, tokenized pilot acceptance room, manual billing receipt persistence, protected pilot acceptance API, payment-transmission safety flags, buyer confirmation status linkage, workbench acceptance visibility, rep feedback command room states, market launch kit, providerless account preflight safety, first beta launch drill, launch proof ledger, activation intake triage, and persistence coverage.
- Local `npm run qa:browser` passed and refreshed browser QA screenshots in `.artifacts/browser-qa`.
- Local seeded `npm run qa:launch` passed with 36/36 checks and hardening `100/100`, including beta-client domain authorization assertions, launch-packet runtime policy, public pilot launch hub, public providerless enrichment, first-five beta board, first beta launch drill, first beta next-action room, launch confirmation command center, launch confirmation watchroom, first profile capture room, profile handoff bridge, rep feedback command room, first beta execution packet, market launch kit, kickoff-ready action kits, manual `mailto:` draft handoff while confirmation is pending, manual sent recorder previews where applicable, providerless launch cost firewall, launch proof ledger, launch proof owner handoff, activation intake triage, activation outbox, activation close desk, first-five close-packet campaign plan, buyer-confirmation chase campaign plan, confirmation reply preview, stakeholder handoff pack, tokenized pilot acceptance room, protected pilot acceptance API, providerless evidence packet auth, HTML, JSON, Markdown, safety, and zero-paid-lookup checks.
- Local in-app browser smoke passed on `/launch?client=...`: the protected first beta launch cockpit renders the new Domain authorization section and runtime policy for the selected beta client, the selected client reports `domain_authorization.status=authorized`, no console errors were observed, and the screenshot is saved at `.artifacts/launch-readiness/domain-authorization-selected-launch.png`.
- Local in-app browser smoke passed on `/confirm/:token/status`: the tokenized buyer status room renders the new buying-committee brief, forwardable summary, providerless risk-control copy, public primary confirmation CTA, zero forms, and no protected `/crm/`, `/admin`, or `/api/v1` links. Screenshot: `.artifacts/launch-readiness/buyer-status-committee-brief.png`.
- Local in-app browser smoke passed on `/confirm/:token`: the tokenized buyer confirmation form renders the completion checklist, owner actions, request-kit and pilot-acceptance links, five required kickoff-gating inputs, the intended form only, and no protected `/crm/`, `/admin`, or `/api/v1` links. Screenshot: `.artifacts/launch-readiness/buyer-confirmation-form-checklist.png`.
- Production read-only smoke passed against the Railway URL after deployment `d867f159-a1dd-472b-bde1-de193bafc262`: `/api/v1/health` returns `200` with status `ok` and hardening `100/100`; the public LaunchReady Co tokenized `/confirm/pcf_c90a184cf64c46ebb67eadba57589552` form loads with the completion checklist, owner actions, request-kit link, pilot-acceptance link, required kickoff-gating fields, one intended form, and no protected `/crm/`, `/admin`, or `/api/v1` links; public status JSON remains buyer-safe with `decision_status=buyer_input_needed` and `3` missing required details; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `1a7424a6-21ae-4a50-b02e-8109e0ee3327`: `/api/v1/health` returns `200` with status `ok` and hardening `100/100`; the public LaunchReady Co tokenized `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status` HTML and JSON load with the buying-committee brief, forwardable summary, providerless risk-control copy, `0` forms, no protected `/crm/`, `/admin`, or `/api/v1` links, and `3` missing required details; unauthenticated `/api/v1/launch/market-ready` returns `401`; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `023cba60-b334-4910-986b-2ade1b460200`: `/api/v1/health` returns `200` with status `ok`, SQLite store live, hardening remains `100/100`, public widget config with an unknown `betaClientId` returns `404 beta_client_not_found`, unauthenticated `/api/v1/launch/market-ready` returns `401`, protected `/launch/market-ready` HTML loads, protected `/api/v1/launch/market-ready` JSON and Markdown load, protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Local in-app browser smoke passed on `/launch/market-kit`: the protected market launch kit renders launch-safe claims, approved claims, claims needing disclosure, prohibited claims, copy blocks, and read-only safety copy.
- Production read-only smoke passed against the Railway URL after deployment `232d9841-2241-46f1-b3bb-87259f4c649a`: `/api/v1/health` returns `200` with status `ok`, SQLite store live, hardening remains `100/100`, unauthenticated `/launch/market-kit` and `/api/v1/launch/market-kit` return `401`, protected `/launch/market-kit` HTML loads with launch-safe claims, approved claims, claims needing disclosure, prohibited claims, copy blocks, and read-only safety copy, protected `/api/v1/launch/market-kit` JSON loads as `deal_threads.market_launch_kit.v1` with status `internal_beta_only`, disclosure required `true`, required code builds before first beta `0`, paid enrichment lookups needed now `0`, providerless approved claim present, guaranteed-lift prohibited claim present, and protected Markdown export loads with external-claim review safety. No production POSTs were run.
- Local in-app browser smoke passed on `/confirm/:token/acceptance`: the buyer-safe pilot acceptance room renders the acceptance-needed state, setup fee, managed pilot price, `Payment data` / `Not sent` posture, and no protected `/crm/` or `/api/v1/` links.
- Production read-only smoke passed against the Railway URL after deployment `71a43403-fbfc-4b82-b573-60bf4f85069e`: `/api/v1/health` returns `200` with status `ok`, SQLite store live, hardening remains `100/100`, unauthenticated `/api/v1/activation/prospects/lead_000/pilot-acceptance` returns `401`, protected `/api/v1/activation/prospects/lead_a6f44a24/confirmation-workbench` exposes the tokenized `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/acceptance` link and protected pilot acceptance API, public acceptance HTML/JSON/Markdown load with payment-transmission safety flags, no protected CRM/admin/API links, no paid-provider lookup by default, and protected pilot acceptance JSON/Markdown load. No production POSTs were run.
- Local in-app browser smoke passed on `/launch/confirmation-command`: the protected command center renders the safety banner, current action, reply parser, providerless enrichment firewall, Markdown link, and expected operator forms; screenshot saved at `.artifacts/launch-readiness/confirmation-command-center.png`.
- Local in-app browser smoke passed on `/launch/confirmation-watch`: the protected watchroom renders the safety banner, current focus, confirmation queue, providerless posture, command-center link, Markdown link, and zero forms; screenshot saved at `.artifacts/launch-readiness/confirmation-watchroom.png`.
- Local in-app browser smoke passed on `/launch/first-profile`: the protected first profile capture room renders the safety banner, current action, POST-only test-lead form, capture checklist, proof unlocks, paid lookups metric at `0`, and no profile creation on page load; screenshot saved at `.artifacts/launch-readiness/first-profile-capture.png`.
- Local in-app browser smoke passed on `/launch/profile-handoff` after creating a local-only first beta profile: the protected profile handoff bridge renders the safety banner, current CRM handoff action, rep-ready brief, CRM handoff form, rep alert form, protected rep feedback form, buyer-safe links, handoff checklist, paid lookups metric at `0`, and no handoff action on page load; screenshot saved at `.artifacts/launch-readiness/profile-handoff-bridge.png`.
- Local in-app browser smoke passed on `/launch/rep-feedback`: the protected rep feedback command room renders the safety banner, current action, copy-ready rep ask, public feedback rooms, profile feedback queue, Markdown link, paid lookups metric at `0`, and no console errors; screenshot saved at `.artifacts/launch-readiness/rep-feedback-command-room.png`.
- Production read-only smoke passed against the Railway URL after deployment `8fa030c9-6587-4294-9571-f611a77f901b`: `/api/v1/health` returns `200` with status `ok`, SQLite store live, hardening remains `100/100`, unauthenticated `/launch/rep-feedback` and `/api/v1/launch/rep-feedback` return `401`, protected `/launch/rep-feedback` HTML loads with the command-room heading, copy-ready rep ask, public feedback rooms, read-only safety copy, and paid lookups metric, protected `/api/v1/launch/rep-feedback` JSON and Markdown load as `deal_threads.launch_rep_feedback_command.v1`, rep-feedback status is `needs_real_beta_client`, real beta clients `0`, beta profiles `0`, pending profiles `0`, paid lookups recommended now `0`, safety flags confirm read-only `GET`, no email send, no profile creation, no beta-client creation, no CRM queueing, no CRM transmission, no rep-feedback update, no paid-provider lookup, and no live-proof claim; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` real beta leads, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `f1a59e37-23bd-4c94-9252-6562183d88f2`: `/api/v1/health` returns `200` with status `ok`, SQLite store live, hardening remains `100/100`, unauthenticated `/launch/profile-handoff` and `/api/v1/launch/profile-handoff` return `401`, protected `/launch/profile-handoff` HTML loads with the safety banner, current action, handoff checklist, CRM handoff, and rep handoff, protected `/api/v1/launch/profile-handoff` JSON and Markdown load as `deal_threads.launch_profile_handoff_bridge.v1`, production profile-handoff status is `needs_real_beta_client`, selected client is `null`, selected profile is `null`, beta profiles `0`, CRM handoff sent `false`, rep feedback reviewed `false`, required real-world steps `7`, paid lookups recommended now `0`, safety flags confirm read-only `GET`, no profile creation, no email send, no rep alert, no CRM delivery queueing, no CRM transmission, no rep-feedback update, no beta-client creation, no paid-provider lookup, and no live-proof claim, and CRM delivery, rep alert, and rep feedback all require `POST`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `e60d2a25-22ee-4f20-b143-f8725e3e2035`: `/api/v1/health` returns `200` with status `ok`, SQLite store live at `/data/deal-threads-dev.sqlite`, hardening remains `100/100`, unauthenticated `/launch/first-profile` and `/api/v1/launch/first-profile` return `401`, protected `/launch/first-profile` HTML loads with the safety banner, current action, capture checklist, and proof unlocks, protected `/api/v1/launch/first-profile` JSON and Markdown load as `deal_threads.launch_first_profile_capture.v1`, first-profile status is `needs_real_beta_client`, selected client is `null`, profiles `0`, CRM handoffs `0`, rep reviews `0`, required real-world steps `7`, paid lookups recommended now `0`, safety flags confirm read-only `GET`, no profile creation, no buyer-state mutation, no email send, no beta-client creation, no CRM transmission, no paid-provider lookup, and no live-proof claim, and test lead creation still requires `POST`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `cfa7e958-62fc-4a25-85f4-af9df94c30f2`: `/api/v1/health` returns `200`, hardening `100/100`, SQLite store live, unauthenticated `/api/v1/launch/confirmation-command` returns `401`, protected `/launch/confirmation-command` HTML loads, protected `/api/v1/launch/confirmation-command` JSON and Markdown load, command center type is `deal_threads.launch_confirmation_command_center.v1`, status is `buyer_confirmation_action_ready`, active mode is `buyer_confirmation`, active company is `LaunchReady Co`, required code builds before first beta is `0`, paid lookups recommended now is `0`, providerless paid lookup default is `false`, providerless manual approval is `true`, and safety flags confirm no email send, manual-sent record, buyer-confirmation completion, beta-client creation, CRM transmission, paid lookup, or live-proof claim runs on `GET`. Market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `7` required real-world proof steps.
- Production read-only smoke passed against the Railway URL after deployment `4c106f36-fb03-42f1-8dd7-1d230b8cab6e`: `/api/v1/health` returns `200`, hardening `100/100`, SQLite store live, unauthenticated `/launch/confirmation-watch` and `/api/v1/launch/confirmation-watch` return `401`, protected `/launch/confirmation-watch` HTML loads, protected `/api/v1/launch/confirmation-watch` JSON and Markdown load, watchroom type is `deal_threads.launch_confirmation_watchroom.v1`, status is `chase_due`, active mode is `buyer_confirmation`, active company is `LaunchReady Co`, total threads `1`, due now `1`, overdue `0`, waiting on buyer `0`, kickoff ready `0`, stale threads `1`, required code builds before first beta `0`, required real-world proof steps `7`, paid lookups recommended now `0`, providerless paid lookup default is `false`, providerless manual approval is `true`, and safety flags confirm no email send, manual-sent record, buyer-confirmation completion, beta-client creation, CRM transmission, paid lookup, or live-proof claim runs on `GET`. Market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `7` required real-world proof steps.
- Production read-only smoke passed against the Railway URL after deployment `0f7f1574-7abc-42c8-b91d-20b03a989817`: `/api/v1/health` returns `200`, hardening `100/100`, SQLite store live, unauthenticated `/launch/proof-handoff` and `/api/v1/launch/proof-handoff` return `401`, protected `/launch/proof-handoff` HTML loads, protected `/api/v1/launch/proof-handoff` JSON and Markdown load, owner-handoff safety flags confirm no buyer confirmation completion, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim runs on `GET`, handoff has `7` owner groups, `8` open items, `7` blockers, `1` warning, `2` suggested recipients, paid lookups recommended now remains `0`, and market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, and `7` required real-world proof steps.
- Production read-only smoke passed against the Railway URL after deployment `6b33ceda-0088-402b-960e-331e2a422ced`: `/api/v1/health` returns `200`, hardening `100/100`, unauthenticated `/launch/first-beta-drill` and `/api/v1/launch/first-beta-drill` return `401`, protected `/launch/first-beta-drill` HTML loads, protected `/api/v1/launch/first-beta-drill` JSON and Markdown load, drill safety flags confirm read-only `GET`, no buyer confirmation completion, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim, operator POSTs are previews only, drill status is `drill_active`, market ready `false`, open steps `10`, blockers `7`, warnings `3`, operator POST previews `3`, required code builds before first beta `0`, required real-world proof steps `7`, paid lookups recommended now `0`, first open step `send_close_packet` / `Send the active buyer follow-up packet`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `bb7fc068-c292-4cf7-91ae-d991ea676852`: `/api/v1/health` returns `200`, hardening `100/100`, unauthenticated `/activation/outbox` and `/api/v1/activation/outbox` return `401`, protected `/activation/outbox` HTML loads, protected `/api/v1/activation/outbox` JSON and Markdown load, outbox safety flags confirm read-only `GET`, no buyer-state mutation, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim, send actions require explicit `POST`, outbox status is `confirmation_nudge_ready`, sendable items `1`, confirmation nudges ready `1`, close packets ready `0`, dry-run mode `true`, paid lookups recommended now `0`, first item is `LaunchReady Co` / `Buyer confirmation nudge`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `fa9cf7c2-f71b-40ab-b140-d96ab66cc0fc`: `/api/v1/health` returns `200`, hardening `100/100`, SQLite store live, unauthenticated `/activation/prospects/lead_a6f44a24/confirmation-reply` and `/api/v1/activation/prospects/lead_a6f44a24/confirmation-reply-preview` return `401`, protected confirmation reply preview HTML loads, protected confirmation reply preview JSON and Markdown load, preview safety flags confirm read-only `GET`, preview mutates state `false`, no buyer-state mutation, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim, apply requires explicit `POST`, empty preview status is `waiting_for_reply_text`, ready for confirmation `false`, paid lookups recommended now `0`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `6c6f8cd6-d222-4175-a8e0-2b2ae814c6c5`: `/api/v1/health` returns `200`, unauthenticated `/launch/first-beta-execution` and `/api/v1/launch/first-beta-execution` return `401`, protected `/launch/first-beta-execution` HTML loads, protected `/api/v1/launch/first-beta-execution` JSON and Markdown load, execution safety flags confirm read-only `GET`, no buyer-confirmation completion, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim, operator POSTs are previews only, execution status is `buyer_confirmation_action_ready`, active buyer is `LaunchReady Co` / `lead_a6f44a24`, sendable follow-ups `1`, operator POST previews `3`, external owner actions `8`, required code builds before first beta `0`, required real-world proof steps `7`, paid lookups needed now `0`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, and `1` warning.
- Production read-only smoke passed against the Railway URL after deployment `d66d8e8a-bc08-45d2-bde2-82eaadf007db`: `/api/v1/health` returns `200`, unauthenticated `/api/v1/launch/first-beta-execution` returns `401`, protected `/launch/first-beta-execution` HTML loads with the providerless enrichment cost firewall, protected `/api/v1/launch/first-beta-execution` JSON and Markdown load, protected `/api/v1/enrichment/build-plan` loads, execution safety flags confirm read-only `GET`, no buyer-confirmation completion, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim, providerless status is `providerless_ready`, paid provider calls `0`, paid provider spend `$0.00`, recommended paid lookups now `0`, manual approval required `true`, execution status remains `buyer_confirmation_action_ready`, active buyer is `LaunchReady Co`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `3e307be2-f8ed-4476-9752-44a2df3dd563`: `/api/v1/health` returns `200`, unauthenticated `/api/v1/activation/outbox` returns `401`, protected `/activation/outbox` HTML loads with `Open manual email draft`, protected `/api/v1/activation/outbox` JSON and Markdown load with `mailto:` handoff URLs, protected `/launch/first-beta-execution` HTML loads with `Open manual email draft`, protected `/api/v1/launch/first-beta-execution` JSON and Markdown load with the same manual draft handoff, manual handoff method is `mailto`, `sends_from_server` is `false`, execution safety flags confirm read-only `GET`, no buyer-confirmation completion, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim, execution status remains `buyer_confirmation_action_ready`, active buyer is `LaunchReady Co`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `84b00fff-f648-4734-bd3c-13b7bf691f0d`: `/api/v1/health` returns `200`, hardening remains `100/100`, public `/providerless-enrichment` loads without forms, protected CRM links, or admin links, public `/api/v1/providerless-enrichment` JSON and Markdown load with the providerless build-vs-buy strategy, paid lookup allowed by default is `false`, manual approval required is `true`, recommended paid lookups now is `0`, no external transmission or paid lookup runs on the public GET, unauthenticated `/api/v1/launch/market-ready` returns `401`, protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, required code builds before first beta `0`, required real-world proof steps `7`, paid enrichment lookups needed now `0`, protected `/api/v1/launch/first-beta-execution` remains `buyer_confirmation_action_ready`, and providerless execution status remains `providerless_ready`.
- Production read-only smoke passed against the Railway URL after deployment `0c38ce2e-f656-4595-a74f-f5ea843d7c62`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/activation/outbox` returns `401`, protected `/activation/outbox` HTML loads with manual sent POST controls, protected `/api/v1/activation/outbox` JSON and Markdown load with one manual sent action for the active `confirmation_nudge`, `sends_from_server` is `false`, `mutates_state` is `true`, protected `/launch/first-beta-execution` HTML loads with `Manual sent POST` and the providerless enrichment cost firewall, protected `/api/v1/launch/first-beta-execution` JSON and Markdown load, operator POST previews increased to `4` including `mark_manual_follow_up_sent`, execution safety flags confirm read-only `GET`, no buyer-confirmation completion, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim, execution status remains `buyer_confirmation_action_ready`, active buyer is `LaunchReady Co`, outbox status is `confirmation_nudge_ready`, sendable items `1`, confirmation nudges ready `1`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `9e9666a4-411f-4d45-bc90-fc6f2729abb5`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/launch/next-action` returns `401`, protected `/launch/next-action` HTML loads with the shortest-path action room, manual email draft, manual sent POST, buyer-safe links, and proof focus, protected `/api/v1/launch/next-action` JSON and Markdown load, next-action status is `active_buyer_follow_up_ready`, active buyer is `LaunchReady Co`, packet kind is `confirmation_nudge`, manual sent action is ready and points to `confirmation-nudge/sent`, operator POST previews remain `4`, safety flags confirm read-only `GET`, no buyer-confirmation completion, beta-client creation, paid-provider lookup, CRM transmission, email send, or live-proof claim, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `9adab166-d357-435c-9d61-0e4c874f1300`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/launch/next-action` returns `401`, protected `/api/v1/launch/next-action` returns `active_buyer_follow_up_ready` for `LaunchReady Co`, public `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/request-kit` HTML loads with the buyer confirmation request kit, champion reply template, and `Open email draft` links, JSON loads as `deal_threads.public_buyer_confirmation_request_kit.v1` with 4 stakeholder asks and 4 `mailto:` drafts, Markdown loads with `## Champion Reply Template` and `Mailto draft: mailto:`, public request-kit HTML/JSON/Markdown expose no protected `/crm/`, `/admin`, or `/api/v1` links, and the market gate remains not market-ready with `market_ready: false`.
- Production read-only smoke passed against the Railway URL after deployment `5877827c-a947-4d93-839f-62bd9bc8b314`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/launch/proof-handoff` returns `401`, protected `/launch/proof-handoff` HTML loads with the launch proof owner handoff, `Open email draft` links, and read-only safety copy, protected `/api/v1/launch/proof-handoff` JSON loads as `deal_threads.launch_proof_owner_handoff.v1` with `7` owner groups and `7` `mailto:` drafts, Markdown loads with `# Deal Threads Launch Proof Owner Handoff`, `Mailto draft: mailto:`, and `GET marks buyer confirmation complete: no`, safety flags confirm no email send, beta-client creation, paid-provider lookup, or live-proof claim runs on `GET`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `e787b97e-14e9-4bb0-9f37-561bd91c7ad6`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/launch/proof-handoff` returns `401`, protected `/launch/proof-handoff` HTML loads with the launch proof owner handoff, `Open email draft`, `Mark manual handoff sent`, and read-only safety copy, protected `/api/v1/launch/proof-handoff` JSON loads as `deal_threads.launch_proof_owner_handoff.v1` with `7` owner groups, `7` `mailto:` drafts, `7` POST-only manual sent recorders, and a numeric `manual_sent_records` audit count, Markdown loads with `# Deal Threads Launch Proof Owner Handoff`, `Mailto draft: mailto:`, `Manual sent POST`, and `GET marks buyer confirmation complete: no`, safety flags confirm no email send, beta-client creation, paid-provider lookup, or live-proof claim runs on `GET`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `eb8f0199-1039-40e7-87b2-ed1bbd0843a5`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated protected confirmation reply HTML and API return `401`, protected `/activation/prospects/lead_a6f44a24/confirmation-reply` HTML loads with the review checklist, apply button, operator acknowledgement checkbox, and hidden `confirmation_reply_preview` source, protected `/api/v1/activation/prospects/lead_a6f44a24/confirmation-reply-preview` JSON loads as `deal_threads.confirmation_reply_preview.v1` with ready review status, required `confirmationSource` and `operatorReviewComplete` form fields, paid-lookup guardrail passing, preview mutates state `false`, paid lookup by default `false`, and apply requires operator acknowledgement `true`, Markdown loads with `## Review Checklist` and `Apply requires operator acknowledgement: yes`, and the market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `0` paid enrichment lookups needed now.
- Production read-only smoke passed against the Railway URL after deployment `9c2312b6-8cb4-40e3-868a-d001c6b58099`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/activation/real-beta` returns `401`, protected activation, market gate, proof ledger, and install queue GETs load, activation has `1` protected prospect, market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, proof ledger remains `buyer_confirmation_needed` with `7` blockers and `8` open items, install queue remains `needs_beta_clients`, and paid enrichment lookups needed now remains `0`. No production POSTs were run; the new kickoff `proof_transition` response is covered by local regression tests.
- Production read-only smoke passed against the Railway URL after deployment `0696c966-161f-4aa4-b8d0-5fb4fc2124d0`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/launch/install-queue` returns `401`, protected install queue loads as `deal_threads.launch_install_queue.v1` with status `needs_beta_clients`, `0/5` real beta clients, `0` queue items, and safety flags confirming no email send, beta-client creation, CRM transmission, paid-provider lookup, or live-proof claim runs on `GET`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now; protected proof ledger remains not market-ready with `7` blockers, `8` open items, and `0` paid lookups recommended now. No production POSTs were run; the new install workbench `mailto:` drafts and POST-only manual sent recorders are covered by local regression tests and in-app browser verification until the first real beta client exists in production.
- Production read-only smoke passed against the Railway URL after deployment `0740fb44-1597-4b26-a5f5-c0ac1ce45e40`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/activation/close-desk` returns `401`, protected close desk loads as `deal_threads.activation_close_desk.v1`, the first-five campaign packet loads as `deal_threads.first_five_close_packet_campaign.v1`, live production currently has `1` qualified prospect in `confirmation_nudges_ready` state, `0` close packets ready, `0` planned close-packet campaign sends, `0` paid lookups recommended now, campaign `GET` sends email `false`, bulk send on `GET` `false`, and manual record requires POST `true`; Markdown loads with `## First-Five Close Packet Campaign` and `GET sends campaign email: no`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run; the nonzero close-packet batch path is covered by local regression, launch QA, and in-app browser verification.
- Production read-only smoke passed against the Railway URL after deployment `87435114-fd96-4933-b84b-12023b2d9292`: `/api/v1/health` returns `200`, hardening remains `100/100`, SQLite store live, unauthenticated `/api/v1/activation/close-desk` returns `401`, protected close desk JSON and Markdown load as `deal_threads.activation_close_desk.v1`, live production has `1` qualified prospect in `confirmation_nudges_ready` state, `1` planned buyer-confirmation chase send, `1` chase `mailto:` draft, `1` POST-only manual sent recorder, `0` paid lookups recommended now, the chase packet loads as `deal_threads.buyer_confirmation_chase_campaign.v1` with status `confirmation_chase_ready`, `GET` sends email `false`, bulk send on `GET` `false`, and manual record requires POST `true`; Markdown loads with `## Buyer Confirmation Chase Campaign`, `Manual sent POST:`, `GET sends campaign email: no`, and `Reply preview:`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `94b5a89d-8bdc-4321-8559-411b9f0be210`: `/api/v1/health` returns `200`, hardening remains `100/100`, SQLite store live, unauthenticated `/api/v1/launch/first-five-board` returns `401`, protected `/launch/first-five-board` HTML loads, protected `/api/v1/launch/first-five-board` JSON and Markdown load as `deal_threads.first_five_beta_board.v1`, board status is `buyer_confirmation_follow_up`, target slots `5`, occupied slots `4`, activation prospects `1`, target accounts `3`, empty slots `1`, first slot is `LaunchReady Co` / `activation_prospect` / `buyer_confirmation_chase`, providerless status is `providerless_ready`, paid lookup default is `false`, manual approval is required, paid lookups recommended now remain `0`, board safety flags confirm read-only `GET`, no email send, buyer-confirmation completion, beta-client creation, CRM transmission, bulk send, paid-provider lookup, or live-proof claim, HTML contains the first-five board, Slot 1, providerless launch guardrail, and safety copy, Markdown contains `# Deal Threads First-Five Beta Board`, `Providerless Guardrail`, `Paid lookups recommended now: 0`, and `GET sends external email: no`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `0baedea3-9da6-4f19-9c36-8ecbd3babb9b`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/api/v1/launch/first-five-board` returns `401`, protected `/launch/first-five-board` HTML loads with `Action kit`, `Open manual draft`, `Manual sent POST`, `Reply preview`, and the providerless launch guardrail; protected `/api/v1/launch/first-five-board` JSON and Markdown load as `deal_threads.first_five_beta_board.v1`, board status remains `buyer_confirmation_follow_up`, occupied slots `4/5`, activation prospects `1`, target accounts `3`, empty slots `1`, first open slot remains `LaunchReady Co` in `buyer_confirmation_chase`, the activation slot action kit is `confirmation_nudge` with a `mailto:` handoff, send `POST`, POST-only manual sent recorder, buyer status room, request kit, reply preview, no email send on `GET`, and paid lookup by default `false`; Markdown contains `Action kit packet:`, `Mailto draft: mailto:`, `Manual sent POST:`, and `Manual record requires POST: yes`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `0033d28b-19cc-4536-a2c7-408b461846c7`: `/api/v1/health` returns `200`, hardening remains `100/100`, public `/pilot-launch` HTML loads with `Deal Threads pilot launch hub`, `Mid-market beta pilot`, revenue leader, RevOps/CRM owner, implementation owner, proof gates, paid enrichment default, and the buyer-safe disclosure; public `/api/v1/pilot-launch` JSON and Markdown load as `deal_threads.public_pilot_launch_hub`, with `5` stakeholder paths, `5` launch sequence steps, `7` proof gates, `$500` setup, `$2,500/month` managed service, and safety flags confirming read-only `GET`, no operator forms, CRM profile exposure, tenant export exposure, buyer-state mutation, external transmission, email send, paid enrichment, or live-results claim; HTML and JSON expose no `/crm/` profile links or `/admin` links, Markdown contains `# Deal Threads Pilot Launch Hub`, `Stakeholder Paths`, `Read-only GET: yes`, and `Runs paid enrichment: no`; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `6672eb20-5632-4759-a237-081e703c6c40`: `/api/v1/health` returns `200`, hardening remains `100/100`, unauthenticated `/activation/intake-triage` and `/api/v1/activation/intake-triage` return `401`, protected `/activation/intake-triage` HTML loads with activation intake triage, candidate queue, providerless posture, safety copy, and zero POST forms; protected `/api/v1/activation/intake-triage` JSON and Markdown load as `deal_threads.activation_intake_triage.v1`; production triage status is `buyer_confirmation_follow_up`, total candidates `1`, qualified activation prospects `1`, first-five ready candidates `1`, ready for kickoff `0`, close packets needed `0`, buyer-confirmation follow-up `1`, first item `LaunchReady Co` / `activation_prospect` / `confirmation_nudge`, paid lookups recommended now `0`; safety flags confirm read-only `GET`, no external email send, buyer-confirmation completion, beta-client creation, external CRM transmission, paid enrichment, or live-proof claim; protected market gate remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, `7` required real-world proof steps, and `0` paid enrichment lookups needed now. No production POSTs were run.
- Production read-only smoke passed against the Railway URL after deployment `088cedcc-f08c-4989-8918-c68478c1234e`: `/api/v1/health` returns `200`, hardening remains `100/100`, SQLite store live, public `/pilot-intake` returns `200` with the consent-gated request form and no protected CRM/admin links, unauthenticated `/api/v1/activation/intake-triage` returns `401`, protected `/api/v1/activation/intake-triage` loads as `deal_threads.activation_intake_triage.v1` with `1` total candidate, `1` qualified activation prospect, first item `LaunchReady Co` / `activation_prospect` / `confirmation_nudge`, paid lookups recommended now `0`, read-only safety flags confirming no email send, beta-client creation, or paid enrichment on `GET`, and the existing tokenized status room `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status?intake=1` renders the new `Pilot request received` banner, buyer confirmation status, read-only copy, confirmation form link, request kit link, zero forms, and no protected `/crm/`, `/admin`, or `/api/v1` links. No production POSTs were run; the form redirect and JSON handoff fields are covered by local regression tests and local browser smoke.
- Production read-only smoke passed against the Railway URL after deployment `d36e6baf-d226-4f5d-825a-404915cdaf4c`: `/api/v1/health` returns `200` with status `ok`, unauthenticated `/api/v1/trust/hardening` and `/api/v1/launch/next-action` return `401`, protected hardening returns `hardened` with score `100/100`, protected `/launch/next-action` HTML loads with `First beta next action`, `GET is read-only`, and the pre-confirmation manual draft path, protected `/api/v1/launch/next-action` loads as `deal_threads.first_beta_next_action.v1` with status `active_buyer_follow_up_ready`, current packet `confirmation_nudge`, active company `LaunchReady Co`, and read-only safety flags, protected `/api/v1/launch/first-beta-execution` loads as `deal_threads.first_beta_execution_packet.v1` with status `buyer_confirmation_action_ready`, active packet `confirmation_nudge`, kickoff preview present but blocked until confirmation, and paid lookups recommended now `0`, protected `/api/v1/launch/first-five-board` remains `buyer_confirmation_follow_up` with activation slot packet `confirmation_nudge`, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, `0` required code builds before first beta, and `7` required real-world proof steps. No production POSTs were run; the post-confirmation `kickoff_ready` path is covered by local regression tests, launch QA, and in-app browser smoke.
- Local post-kickoff install-proof next-action verification passed before deployment: `/api/v1/launch/next-action` returns `install_proof_ready` with `packet_kind` `install_proof`, `current_install_action.action_key` `collect_install_handoff`, `current_follow_up` `null`, `copy_block` `null`, and `manual_sent_action_ready` `false`; `/launch/next-action` HTML shows `Active install proof`, `Install public links`, `Install workbench`, and `GET is read-only`, with no stale `close-workflow` or `Mark manual draft sent` form. Verification passed `npm test` `12/12`, `WIDGET_ID=wid_replede_demo npm run qa:launch` `36/36`, hardening `100/100`, and the in-app browser smoke screenshot is `.artifacts/launch-readiness/first-beta-next-action-install-proof.png`.
- Production read-only smoke passed against the Railway URL after deployment `b32505f7-31df-4758-8b6b-74fde796ae63`: `/api/v1/health` returns `200` with status `ok`, unauthenticated `/api/v1/trust/hardening` and `/api/v1/launch/next-action` return `401`, protected hardening returns `hardened` with score `100/100`, protected `/launch/next-action` HTML loads with `First beta next action` and `GET is read-only`, protected `/api/v1/launch/next-action` loads as `deal_threads.first_beta_next_action.v1` with status `active_buyer_follow_up_ready`, current packet `confirmation_nudge`, active company `LaunchReady Co`, no `current_install_action` because production still has `0` real beta clients, and safety flags confirming no email send, beta-client creation, CRM transmission, paid lookup, or live-proof claim on `GET`; protected `/api/v1/launch/first-beta-execution` remains `buyer_confirmation_action_ready` with packet `confirmation_nudge`, kickoff preview present, and paid lookups recommended now `0`; protected `/api/v1/launch/first-five-board` remains `buyer_confirmation_follow_up`; protected `/api/v1/launch/install-queue` remains `needs_beta_clients` with `0` real beta clients and `0` queue items; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `8` required real-world proof steps. No production POSTs were run.
- Local install-queue priority verification passed after the client-domain config proof fix: when a kicked-off beta client has client-domain config loads but no beta-attributed profiles yet, `/api/v1/launch/install-queue` now reports `create_first_profile` instead of `waiting_install_handoff`, `/api/v1/launch/next-action` points `current_install_action.action_key` to `create_first_profile`, proof focus is `first_real_profile`, `manual_sent_action_ready` is `false`, and `/launch/next-action` shows `create first profile`, `Capture first beta profile`, `First real beta profile`, `GET is read-only`, zero forms, and no stale `close-workflow`. Verification passed `npm test` `12/12`, `WIDGET_ID=wid_replede_demo npm run qa:launch` `36/36`, hardening `100/100`, and the in-app browser smoke screenshot is `.artifacts/launch-readiness/first-beta-next-action-first-profile.png`.
- Production read-only smoke passed against the Railway URL after deployment `880bcb5f-aea9-475a-b95c-defef5b013c1`: `/api/v1/health` returns `200` with status `ok`, unauthenticated `/api/v1/trust/hardening` and `/api/v1/launch/next-action` return `401`, protected hardening returns `hardened` with score `100/100`, protected `/launch/next-action` HTML loads with `First beta next action` and `GET is read-only`, protected `/api/v1/launch/next-action` remains `active_buyer_follow_up_ready` with packet `confirmation_nudge`, active company `LaunchReady Co`, no `current_install_action` because production still has `0` real beta clients, and safety flags confirming no email send, beta-client creation, CRM transmission, paid lookup, or live-proof claim on `GET`; protected `/api/v1/launch/first-beta-execution` remains `buyer_confirmation_action_ready` with kickoff preview present and paid lookups recommended now `0`; protected `/api/v1/launch/first-five-board` remains `buyer_confirmation_follow_up`; protected `/api/v1/launch/install-queue` remains `needs_beta_clients` with `0` real beta clients and `0` queue items; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `8` required real-world proof steps. No production POSTs were run.
- Local proof-capture runbook verification passed before deployment: `/api/v1/launch/proof-capture` returns `deal_threads.launch_proof_capture_runbook.v1`, selects local beta client `beta_ca76a95e`, sets current phase to `first_real_profile`, marks `client_domain_install` pass, keeps first profile, CRM handoff, rep feedback, proof packet, and live-proof gate blocked until evidence exists, and exposes read-only safety flags with no beta-client creation, CRM transmission, paid lookup, or live-proof claim on `GET`; `/launch/proof-capture` loads with `Proof capture runbook`, `Current proof action`, `Proof sequence`, `First real beta profile`, `CRM handoff proof`, `Rep feedback`, `Live proof gate`, `GET is read-only`, supporting launch links, and no forms. Verification passed `npm test` `12/12`, `WIDGET_ID=wid_replede_demo npm run qa:launch` `36/36`, hardening `100/100`, and the in-app browser smoke screenshot is `.artifacts/launch-readiness/proof-capture-runbook.png`.
- Production read-only smoke passed against the Railway URL after deployment `2e0d89c0-82b7-4688-a8be-a5675b0e37d3`: `/api/v1/health` returns `200` with status `ok`, unauthenticated `/api/v1/trust/hardening` and `/api/v1/launch/proof-capture` return `401`, protected hardening returns `hardened` with score `100/100`, protected `/launch/proof-capture` HTML loads with `Proof capture runbook`, `GET is read-only`, and no forms, protected `/api/v1/launch/proof-capture` JSON and Markdown load as `deal_threads.launch_proof_capture_runbook.v1`, proof-capture status is `needs_real_beta_client` with current phase `buyer_confirmation`, safety flags confirm read-only `GET`, no beta-client creation, no paid-provider lookup, no CRM transmission, no email send, and no live-proof claim; protected `/api/v1/launch/next-action` remains `active_buyer_follow_up_ready` with packet `confirmation_nudge`, active company `LaunchReady Co`, and no `current_install_action`; protected `/api/v1/launch/install-queue` remains `needs_beta_clients` with `0` real beta clients and `0` queue items; protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `8` required real-world proof steps. No production POSTs were run.
- Local first proof packet workbench verification passed before deployment: `/api/v1/launch/proof-packet` returns `deal_threads.launch_first_proof_packet_workbench.v1`, selects the local real beta client when one exists, blocks queueing until strict evidence is ready, exposes buyer-safe proof packet preview, keeps `queues_report_delivery_on_get=false`, `marks_report_sent_on_get=false`, `sends_external_email_on_get=false`, `transmits_external_crm_on_get=false`, `paid_provider_lookup_by_default=false`, and `live_proof_claimed=false`, and exposes POST-only queue and manual-sent actions. Verification passed `npm test` `12/12`, `WIDGET_ID=wid_replede_demo npm run qa:launch` `36/36`, hardening `100/100`, and the in-app browser smoke screenshot is `.artifacts/launch-readiness/first-proof-packet-workbench.png`.
- Production read-only smoke passed against the Railway URL after deployment `d33267a3-fa43-4857-9fcf-cacc18e8d997`: `/api/v1/health` returns `200` with status `ok`, unauthenticated `/api/v1/trust/hardening` and `/api/v1/launch/proof-packet` return `401`, protected hardening returns `hardened` with score `100/100`, protected `/launch/proof-packet` HTML loads with `First proof packet workbench`, `Evidence gate`, `GET is read-only`, and no forms because production has no selected real beta client, protected `/api/v1/launch/proof-packet` JSON and Markdown load as `deal_threads.launch_first_proof_packet_workbench.v1`, proof-packet status is `needs_real_beta_client`, strict evidence ready is `false`, queued proof packets `0`, sent proof packets `0`, safety flags confirm read-only `GET`, no report queueing, no report-sent mark, no email send, no CRM transmission, no paid-provider lookup, and no live-proof claim; protected `/api/v1/launch/proof-capture` remains `needs_real_beta_client`, protected `/api/v1/launch/next-action` remains `active_buyer_follow_up_ready` with packet `confirmation_nudge`, protected `/api/v1/launch/install-queue` remains `needs_beta_clients` with `0` real beta clients and `0` queue items, and protected `/api/v1/launch/market-ready` remains `buyer_confirmation_needed`, market ready `false`, `7` blockers, `1` warning, `0` real beta clients, and `8` required real-world proof steps. No production POSTs were run.
- Launch readiness artifacts are written to `.artifacts/launch-readiness/launch-readiness-summary.json` and `.artifacts/launch-readiness/launch-readiness-summary.md`.

## Current Demo Proof

- Demo scenario: `mid_market_buyer_demo`
- Synthetic clients: 3
- Synthetic leads: 6
- Total protected leads: 7
- Report deliveries: 3
- Rep alerts: 6
- CRM deliveries: 6
- Readiness status: `demo_ready_with_gaps`
- Proof status: `winning`
- Trust packet status: `procurement_ready_with_disclosures`
- Live proof export gate: `blocked`
- Real beta activation status: `prospect_ready`
- Launch operations status: `buyer_confirmation_follow_up`
- Launch operations current focus: `LaunchReady Co` / `nudge_due`
- Launch confirmation watchroom status: `chase_due`
- Market launch readiness status: `buyer_confirmation_needed`
- Market launch readiness: not ready, 7 blockers, 1 warning
- Real activation qualified prospects: 1
- Public beta pilot intake route: ready for new mid-market requests at `/pilot-intake`; successful submissions now route buyers into their tokenized confirmation status room and expose buyer-safe public next-step links in the JSON response.
- Tokenized buyer confirmation status room: ready at `/confirm/:token/status`; local browser smoke verified the buying-committee brief, forwardable summary, providerless risk-control language, public confirmation CTA, buyer completion payload with missing/saved details, copy-ready completion reply, zero forms, and no protected `/crm/`, `/admin`, or `/api/v1` links. Screenshot: `.artifacts/launch-readiness/buyer-status-committee-brief.png`.
- Public pilot launch hub: ready at `/pilot-launch`, with JSON and Markdown variants for buyer-facing stakeholder routing, launch sequence, proof gates, offer snapshot, providerless cost controls, and safe next links.
- Protected first-five outreach room: ready at `/outreach`, with JSON and Markdown variants at `/api/v1/outreach/first-five`; production currently exposes `3` send-ready target-account rooms, `4` manual `mailto:` drafts, `3` POST-only manual sent recorders, `0` outreach sent records, `0` follow-ups due, and `0` paid lookups recommended now.
- Protected first-five beta board: ready at `/launch/first-five-board`, with JSON and Markdown variants at `/api/v1/launch/first-five-board`; local verified state fills five slots from real beta clients, activation prospects, target accounts, and empty reserve slots, includes action kits for activation slots, includes a providerless launch guardrail, keeps paid lookups recommended now at `0`, and preserves read-only `GET` safety.
- Protected activation intake triage: ready at `/activation/intake-triage`, with JSON and Markdown variants at `/api/v1/activation/intake-triage`; local verified state ranks 8 activation candidates by first-five fit, shows missing buyer-confirmation details and recommended action packets, keeps paid lookups recommended now at `0`, and renders zero POST forms on the triage page.
- Protected market launch readiness gate: ready at `/launch/market-ready`, with JSON and Markdown variants at `/api/v1/launch/market-ready`; current production state is not market-ready until buyer confirmation, real beta-client conversion, client-domain install proof, real beta profiles, CRM handoff proof, rep feedback, and live proof blockers clear. The readiness estimate reports `0` required code builds before first beta, `0` paid enrichment lookups needed now, deferred optional scale builds for native CRM sync, paid enrichment connector, and automated outbound sending, plus the live real-world proof sequence still required. The current-action manual dispatch packet includes a copy-safety audit that checks buyer-body links, protected-route leaks, premature proof claims, providerless/manual-CRM disclosures, and paid-enrichment guardrails before copy is forwarded; the current-action stakeholder chase map names the blocking buyer-side details, saved details, needed stakeholder roles, request-kit path, quick-reply fallback, and zero-paid-lookup safety before the nudge is sent; the current-action `buyer_completion_payload` separates missing/saved buyer details, partial-save status, public completion links, and copy-ready completion reply across the market gate; the current-action `reply_apply_preflight` verifies the protected reply parser, required apply fields, saved reply evidence receipt, operator review, and zero paid lookups before a pasted buyer reply can unlock kickoff; the current-action dispatch commitment checklist separates copy readiness from actual send evidence, shows the ready-to-send/ready-to-record flags, requires mailbox-sent evidence, and blocks any sent record until the buyer nudge actually leaves the mailbox; the current-action `manual_send_evidence_recorder` now renders the protected POST-only market-gate form for recording mailbox sent timestamp, sender mailbox, recipient snapshot, subject snapshot, optional message-id, and evidence note after the real mailbox send, and the manual nudge recorder persists those fields as `manual_evidence`.
- Protected first beta launch drill: ready at `/launch/first-beta-drill`, with JSON and Markdown variants at `/api/v1/launch/first-beta-drill`; it turns the current market gate, proof ledger, activation close desk, and install queue into a read-only sequence from close packet through buyer confirmation, real beta-client conversion, install proof, first profile, CRM handoff, rep feedback, and live-proof review. Operator POSTs are previews only, and `GET` performs no email send, buyer-confirmation completion, beta-client creation, CRM transmission, paid lookup, or live-proof claim.
- Protected first beta next-action room: ready at `/launch/next-action`, with JSON and Markdown variants at `/api/v1/launch/next-action`; it compresses the active buyer follow-up into a single action page with copy, manual draft, manual sent recorder, buyer-safe links, reply parser, proof focus, and POST previews.
- Protected launch confirmation watchroom: ready at `/launch/confirmation-watch`, with JSON and Markdown variants at `/api/v1/launch/confirmation-watch`; it compresses buyer-confirmation follow-up SLAs into current focus, due-now/overdue/waiting/kickoff-ready counts, next check time, per-buyer command links, buyer-safe status/request-kit links, providerless posture, and read-only safety flags with `0` paid enrichment lookups recommended now.
- Protected first profile capture room: ready at `/launch/first-profile`, with JSON and Markdown variants at `/api/v1/launch/first-profile`; it selects the beta client that has install proof but needs the first beta-attributed profile, shows the capture checklist and proof unlocks, previews the POST-only test-lead action, and keeps profile creation, email, CRM transmission, live-proof claims, and paid enrichment off `GET`.
- Protected profile handoff bridge: ready at `/launch/profile-handoff`, with JSON and Markdown variants at `/api/v1/launch/profile-handoff`; it turns the first beta profile into a rep/CRM proof workflow with rep-ready brief, POST-only CRM delivery, POST-only rep alert, POST-only feedback capture, public handoff links, and read-only `GET` safety.
- Protected first beta execution packet: ready at `/launch/first-beta-execution`, with JSON and Markdown variants at `/api/v1/launch/first-beta-execution`; it consolidates the active buyer action, copy-ready follow-up text, manual `mailto:` draft handoff, manual sent recorder preview, buyer-safe links, confirmation reply preview, stakeholder handoff, proof ledger, install queue, providerless enrichment cost firewall, and operator POST previews into one read-only run sheet with `0` paid enrichment lookups recommended now.
- Protected launch proof ledger: ready at `/launch/proof-ledger`, with JSON and Markdown variants at `/api/v1/launch/proof-ledger`; production currently has `10` ledger items, `7` blockers, `1` warning, `8` open evidence items, `4` operator POST items, `0` real beta clients, `0` real beta profiles, `0` client-domain config loads, and `0` paid enrichment lookups recommended now.
- Protected launch proof owner handoff: ready at `/launch/proof-handoff`, with JSON and Markdown variants at `/api/v1/launch/proof-handoff`; production currently has `7` owner groups, `8` open proof items, `7` blockers, `1` warning, `2` suggested recipients, one-click `mailto:` drafts, POST-only manual sent recorders, numeric manual-sent audit counts, and `0` paid enrichment lookups recommended now.
- Protected activation follow-up queue: ready at `/activation/follow-ups`, with JSON and Markdown variants at `/api/v1/activation/follow-ups`.
- Protected activation outbox: ready at `/activation/outbox`, with JSON and Markdown variants at `/api/v1/activation/outbox`; it narrows buyer confirmation work to sendable close packets and confirmation nudges, with manual `mailto:` draft handoff, send POST previews, manual sent recorder previews only on `GET`, and `0` paid enrichment lookups recommended now.
- Protected activation close desk: ready at `/activation/close-desk`, with JSON and Markdown variants at `/api/v1/activation/close-desk`; production currently has `1` close-desk item, recommended packet kind `confirmation_nudge`, and `0` paid enrichment lookups recommended now.
- Protected LaunchReady Co buyer confirmation workbench: ready at `/activation/prospects/lead_a6f44a24/confirmation-workbench`, with JSON and Markdown variants at `/api/v1/activation/prospects/lead_a6f44a24/confirmation-workbench`; status `nudge_due`, missing required details `3`, stakeholder asks needed `2`, confirmation nudge sent `false`.
- Protected LaunchReady Co confirmation reply preview: ready at `/activation/prospects/lead_a6f44a24/confirmation-reply`, with JSON and Markdown variants at `/api/v1/activation/prospects/lead_a6f44a24/confirmation-reply-preview`; preview requests are protected and read-only, apply requires a separate close-workflow POST, and paid lookups recommended now remain `0`.
- Protected LaunchReady Co stakeholder handoff pack: ready at `/activation/prospects/lead_a6f44a24/stakeholder-handoff`, with JSON and Markdown variants at `/api/v1/activation/prospects/lead_a6f44a24/stakeholder-handoff`; status `nudge_due`, missing required details `3`, stakeholder asks needed `2`, paid lookups recommended now `0`, and copy blocks ready for champion, website owner, CRM owner, sales routing owner, proof recipients, and security/procurement.
- Activation follow-up status: `nudge_due`
- Activation follow-up queued prospects: 1
- Activation follow-up close packets sent: 1
- Activation follow-up confirmation nudges sent: 0
- Activation follow-up buyer confirmations received: 0
- First-five outreach status: `pipeline_active`
- First-five outreach visible target accounts: 4
- First-five outreach qualified activation prospects: 1
- Public pilot agreement/order packet: ready at `/pilot-agreement`
- Public pilot approval room: ready at `/pilot-approval-room`, with JSON and Markdown variants for buying-committee review.
- Public mutual action plan: ready at `/mutual-action-plan`, with JSON and Markdown variants for the 14-day approval-to-proof timeline.
- Public buyer confirmation guide: ready at `/buyer-confirmation-guide`, with JSON and Markdown variants for the six details needed before the tokenized confirmation form.
- Public stakeholder forwarding kit: ready at `/stakeholder-forwarding-kit`, with JSON and Markdown variants for champion-forwarded executive, RevOps, website, CRM, security/procurement, and sales notes.
- Public pricing and packages: ready at `/pricing`, with JSON and Markdown variants for the offer ladder, buyer fit, proof gates, and cost controls.
- Public sales enablement packet: ready at `/sales-enablement`, with JSON and Markdown variants for target segments, outbound copy, discovery, objections, demo talk track, and first-five-beta success definition.
- Public providerless enrichment strategy: ready at `/providerless-enrichment`, with JSON and Markdown variants for the self-built enrichment answer, source stack, field-level strategy, build-vs-buy rules, quality gates, aggregate cost metrics, and paid escalation policy.
- Public comparison guide: ready at `/compare`, with JSON and Markdown variants for category positioning, decision criteria, and proof questions.
- Public proof preview: ready at `/proof-preview`, with JSON and Markdown variants for synthetic proof metrics, interpretation rules, buyer inputs, and report sections.
- Public business case calculator: ready at `/business-case`, with JSON and Markdown variants for buyer budget review.
- Public implementation guide: ready at `/implementation-guide`, with JSON and Markdown variants for website and RevOps owners.
- Public CRM handoff guide: ready at `/crm-handoff-guide`, with JSON and Markdown variants for CRM owners.
- Public procurement packet: ready at `/procurement-packet`, with JSON and Markdown variants for buyer review.
- Tokenized customer rep feedback room: ready at `/feedback/:token` from beta launch packets
- Tokenized first-profile rep handoff packet: ready at `/handoff/:token/:leadId` after the beta widget captures a buyer profile
- Tokenized manual CRM export: ready at `/handoff/:token/crm-export.csv` after beta-client conversion, with Markdown and JSON variants
- Tokenized buyer-safe beta proof report: ready at `/handoff/:token/proof` after beta-client conversion, with Markdown and JSON variants
- Tokenized public install status: ready at `/install/:token/status` from beta launch packets after beta-client conversion
- Tokenized hosted widget load test: ready at `/install/:token/test` from beta launch packets after beta-client conversion
- Real activation prospect runbook: ready for `LaunchReady Co` at `/api/v1/activation/prospects/lead_a6f44a24/runbook?format=markdown`, with current status `Buyer confirmation needed`, next-action guidance for close packet, confirmation nudge, buyer confirmation, kickoff, live proof, and providerless enrichment posture.
- Real activation prospect close packet: ready for `LaunchReady Co` at `/api/v1/activation/prospects/lead_a6f44a24/close-packet?format=markdown`, now including the buying-committee approval room, mutual action plan, buyer confirmation guide, stakeholder forwarding kit, status room, and request kit links.
- Real activation prospect confirmation nudge packet: ready for `LaunchReady Co` at `/api/v1/activation/prospects/lead_a6f44a24/confirmation-nudge?format=markdown`, with the tokenized buyer confirmation form, status room, request kit, buyer confirmation guide, stakeholder forwarding kit, mutual action plan, implementation guide, CRM handoff guide, and security center.
- Real activation prospect buyer confirmation workbench: ready for `LaunchReady Co` at `/activation/prospects/lead_a6f44a24/confirmation-workbench`, with the nudge draft, suggested recipients, status room, request kit, manual confirmation capture defaults, and kickoff path.
- Real activation prospect stakeholder handoff pack: ready for `LaunchReady Co` at `/activation/prospects/lead_a6f44a24/stakeholder-handoff`, with buyer-safe copy blocks, exact confirmation/status/request-kit links, security/procurement copy, stakeholder asks, and kickoff preflight.
- Real activation prospect buyer confirmation form: ready at `/confirm/pcf_c90a184cf64c46ebb67eadba57589552`
- Real activation prospect buyer confirmation status room: ready at `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/status`, with status `details_needed`, 3 missing required details, 2 saved required details, buyer completion payload, and a copy-ready completion reply.
- Real activation prospect buyer confirmation request kit: ready at `/confirm/pcf_c90a184cf64c46ebb67eadba57589552/request-kit`, with status `stakeholder_input_needed`, 3 missing required details, 2 saved required details, 2 stakeholder asks needed, buyer completion payload, one-click `mailto:` drafts, and a champion reply template.
- Real activation prospect close packet and public buyer confirmation page both link to `/pilot-approval-room`, `/mutual-action-plan`, and `/stakeholder-forwarding-kit`.
- Real activation prospect close workflow status: `sent`
- Real activation prospect close packet delivery: `sent` via `dry_run` at `2026-06-02T03:17:00.772Z`
- Real activation prospect confirmation nudge delivery: `not_sent` until the operator sends the protected nudge packet.
- Real activation prospect follow-up queue status: `nudge_due`, next action is to send the buyer confirmation nudge and ask the buyer to submit `/confirm/:token`.
- Real activation prospect buyer confirmation receipt delivery: `not_sent` until the buyer submits `/confirm/:token`
- Protected install queue status: `needs_beta_clients`
- Protected install queue real beta clients: 0
- Protected install queue demo clients excluded: 3
- Protected install queue proof preflight and install proof workbench: live and read-only; no per-client proof-preflight/workbench item exists in production until the first real beta client is converted.
- Production `ACTIVATION_AUTO_KICKOFF_ON_CONFIRMATION`: enabled, so the next valid buyer confirmation will create/reuse the beta client, dry-run the launch packet, and redirect to `/install/:token`
- Production `INSTALL_AUTO_SOURCE_CHECK_ON_HANDOFF`: enabled, so the next public install handoff URL submission will immediately run the source check and show the result on `/install/:token`
- Providerless build-plan recommendation: `Stay providerless. Review 1 high-priority low-confidence profile(s) manually before buying data.`
- Providerless fields ranked: 10
- Providerless account preflight: deployed and verified at `/enrichment` and `POST /api/v1/enrichment/preflight`; unsafe private-network targets fall back safely in local regression, do not call paid enrichment, do not save memory, and return JSON/Markdown evidence packets. Production smoke verified the protected form and paid-lookup defaults without running an authenticated production preflight POST.
- Providerless review queue: live at `/enrichment/review-queue`; current production status `high_priority_review`, 1 review candidate, 0 paid lookups recommended now.
- Providerless lead evidence packet: live at `/crm/:leadId/providerless-evidence` and `/api/v1/leads/:leadId/providerless-evidence`; protected, read-only, Markdown-exportable, and pinned to 0 paid lookups recommended by default.
- Recommended paid enrichment lookups now: 0
- Demo clients excluded from real activation: 3
- Real beta clients in live-proof scope: 0
- Real beta leads in live-proof scope: 0
- Live-proof scope client-domain config loads: 0
- Live-proof scope hosted/test config loads: 0
- Selected launch client's install source check: `not_checked`

## Remaining Before Real Buyer Traffic

- Clear the demo scenario and capture real client-specific widget traffic before exporting live beta proof.
- Use `/launch/first-five-board` as the client-by-client board for the first five beta slots so target accounts, activation prospects, real beta clients, install proof, first profiles, CRM handoff, rep feedback, and providerless guardrails stay visible in one place.
- Use `/launch/ops` as the daily first-five command room so outreach, buyer-confirmation follow-up, install work, and proof gates do not get worked from scattered pages.
- Use `/launch/first-beta-drill` when answering exactly how much remains before launch: it rehearses the one-real-beta path and keeps operator POSTs as previews only.
- Use `/launch/first-beta-execution` when actually working the next beta action: it gives the active buyer, pending-confirmation copy block, confirmation links, reply parser, stakeholder handoff, proof ledger, install sequence, and providerless enrichment build plan in one protected run sheet; after buyer confirmation, it suppresses extra follow-up copy and promotes the kickoff install POST.
- Use `/launch/next-action` for the single operator move now: before confirmation it is the buyer follow-up/manual-send surface, and after confirmation it becomes the `Active kickoff` room with the explicit beta-install POST.
- Use `/launch/market-ready` before inviting broad real traffic or claiming live proof; it must show zero blockers before the launch can be called market-ready.
- Use `/launch/proof-ledger` as the evidence checklist behind the market gate so each buyer-confirmation, install, profile, CRM, rep-feedback, and live-proof blocker has an owner, proof condition, capture surface, and verification surface.
- Use `/launch/proof-handoff` to turn open proof-ledger items into copy-ready asks and one-click `mailto:` drafts for buyer, implementation, CRM, sales, and operator owners before sending any manual follow-up; after sending, use its manual sent recorder to preserve the audit trail without claiming proof is complete.
- Use `/outreach` to work the first-five beta pipeline from target-account memory, copy account-specific first-touch emails, and send prefilled `/pilot-intake` links into the activation workflow.
- Use `/activation/follow-ups` daily to work close packets, confirmation nudges, overdue buyer-confirmation threads, and kickoff-ready prospects without hunting through individual CRM profiles.
- Use `/activation/outbox` when the next market-ready blocker is a sendable buyer follow-up; it keeps the exact recipient, copy, delivery audit, and POST preview in one place before the operator sends or pastes the nudge.
- Use `/activation/close-desk` when batching the next activation action across all qualified prospects; it keeps the close packet, confirmation nudge, manual confirmation, and kickoff candidates in one read-only desk.
- Use `/activation/prospects/:leadId/confirmation-workbench` for the active blocker before sending a nudge or manually capturing buyer-confirmed details.
- Use `/activation/prospects/:leadId/confirmation-reply` when the buyer replies by email; it previews a structured confirmation payload from pasted text and keeps the real apply action behind an explicit POST.
- Use `/activation/prospects/:leadId/stakeholder-handoff` when the buyer champion needs one copy-ready packet for internal website, CRM, routing, proof, and security stakeholders before submitting `/confirm/:token`.
- Use `/launch/install-queue` immediately after kickoff to work launch packets, install handoff, source checks, client-domain config loads, first profiles, CRM handoff proof, rep feedback, and proof packets for real beta clients.
- Use `/pilot-intake` for new mid-market beta requests so qualified buyers flow into `/activation` without manual lead creation.
- Use `/pilot-agreement` in close conversations so prospects can review scope, fees, responsibilities, success criteria, and providerless/default security posture before approving the install.
- Use `/pilot-approval-room` as the single forwardable buying-committee hub before install so executive, RevOps, website, CRM, security/legal/procurement, and sales stakeholders each know what to review and approve.
- Use `/mutual-action-plan` when stakeholders ask what exactly happens across the 14-day pilot, who owns each step, and what proof is reviewed before go/no-go.
- Use `/buyer-confirmation-guide` when the buyer asks what to prepare before submitting `/confirm/:token`; it explains the target page, owners, proof recipients, baseline examples, and post-confirmation handoff without exposing the real token.
- Send `/confirm/:token/status` with the confirmation nudge when the buyer needs a quick read-only checklist of what remains missing before submitting the form.
- Send `/confirm/:token/request-kit` with the confirmation nudge when the buyer champion needs forwardable asks, one-click `mailto:` drafts, and a single champion reply template for website, CRM, routing, and proof stakeholders before submitting the form.
- Use `/stakeholder-forwarding-kit` when the buyer champion needs short forwardable notes for executive, RevOps, website, CRM, security/procurement, and sales stakeholders before submitting `/confirm/:token`.
- Use `/pricing` when buyers ask how the pilot turns into a paid service; it explains the $500 setup, $2,500/month managed pilot, $1,000/month reports add-on, $5,000/month premium expansion, proof gates, and cost-control posture.
- Use `/sales-enablement` when preparing outreach to the first five beta buyers; it gives target segments, outbound email/LinkedIn copy, discovery questions, objection responses, and the success definition for moving prospects to confirmation and install.
- Use `/compare` when buyers ask why not keep the form, add generic chat, buy enterprise chat, use routing only, buy enrichment data, or build internally; it frames the decision around profile quality, proof, cost control, and safe pilot scope.
- Use `/proof-preview` when buyers ask what proof they will receive after the 14-day pilot; it previews the report structure and interpretation rules without claiming live results.
- Use `/business-case` when RevOps, sales leadership, or finance stakeholders need to model manual research savings, close-rate lift, service cost, and beta proof targets before approving the pilot.
- Use `/implementation-guide` when website, RevOps, or CRM owners need to understand the script-tag install path, source-check QA, synthetic CRM test, acceptance criteria, and rollback before the tokenized install handoff.
- Use `/crm-handoff-guide` when CRM owners need to choose manual export, dry-run queue, synthetic webhook test, webhook delivery, or HubSpot preview/sandbox before approving real buyer-profile transmission.
- Use `/procurement-packet` when RevOps, security, legal, or implementation stakeholders need a forwardable approval packet before the install conversation.
- Use `/enrichment/review-queue` before buying data: review high-priority low-confidence profiles, add public/manual evidence, save reusable memory, and keep paid lookup at 0 until rep feedback proves a missing field changes first-touch quality.
- Send the protected LaunchReady Co prospect close packet if the buyer needs a fresh copy, then collect buyer-confirmed target page, implementation owner, CRM owner, routing owner, old-form baseline, and report recipients from the tokenized `/confirm/:token` form.
- Use the protected LaunchReady Co activation runbook when deciding whether the next move is close-packet resend, confirmation nudge, manual buyer-confirmation capture, or beta install kickoff.
- Use the protected confirmation nudge packet when the close packet is sent but the buyer has not submitted `/confirm/:token`; include the status room and request kit so the buyer can gather the missing stakeholder details, while recording follow-up attempts without mutating buyer approval state.
- Use the LaunchReady Co buyer confirmation workbench to send the nudge intentionally, or to capture confirmed target page, implementation owner, CRM owner, routing owner, proof recipients, and old-form baseline if the buyer replies by email instead of using `/confirm/:token`.
- Convert the qualified real activation prospect into a beta client from `/activation`, then send the install handoff packet.
- Add the first real beta client domains to the widget allowlist before sending install snippets.
- Have the implementation owner use `/install/:token/test` for hosted config-load proof and `/install/:token/status` to watch public proof, then submit the tested client page URL from `/install/:token`; production will immediately run the source check, but final live proof still requires the client page to load the script config and capture beta-attributed traffic.
- After the first worked test or live profile, send `/feedback/:token` to the client rep so proof has measured usefulness and missing-context data instead of operator guesses.
- Use the protected CRM conversion panel to turn qualified Deal Threads sales leads into beta client launch records; real proof starts only after each client-specific widget captures traffic.
- Connect a HubSpot sandbox token only when a buyer wants real CRM sync; generic CRM delivery can stay dry-run/manual for early beta.
- Run the HubSpot property check from `/admin` before live CRM sync.
- Keep paid enrichment disabled until rep feedback proves which missing field changes conversion.
